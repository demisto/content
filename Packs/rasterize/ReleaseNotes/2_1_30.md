#### Integrations

##### Rasterize

- Fixed an issue where rasterize commands would intermittently fail with the error: *Command 'ps aux | grep chrom | grep port= | grep -- --headless' returned non-zero exit status 1*,  during Chrome process lookups.
- Updated the Docker image to: *demisto/chromium:144.0.7559.6932580*.
