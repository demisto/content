
#### Integrations

##### Rasterize

- Improved implementation of Chrome instance management. When an existing Chrome instance with the same Chrome options fails to connect after 4 retry attempts, Rasterize will now attempt to create a new Chrome instance instead of failing.
