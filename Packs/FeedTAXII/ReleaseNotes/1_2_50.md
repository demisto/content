#### Integrations

##### TAXII 2 Feed

- Fixed an issue where STIX `labels` from indicator objects were automatically promoted to indicator tags without user action. STIX labels are now only added to indicator tags when the **Update Custom Fields** integration parameter is enabled.
