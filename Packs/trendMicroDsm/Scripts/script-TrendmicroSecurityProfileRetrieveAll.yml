comment: Get all security profiles
commonfields:
  id: TrendmicroSecurityProfileRetrieveAll
  version: -1
dependson:
  must:
  - trendmicro-security-profile-retrieve-all
name: TrendmicroSecurityProfileRetrieveAll
script: "\nresp = demisto.executeCommand(\"trendmicro-security-profile-retrieve-all\"\
  , demisto.args())\n\nif isError(resp[0]):\n    demisto.results(resp)\nelse:\n  \
  \  data = demisto.get(resp[0], \"Contents.securityProfileRetrieveAllResponse.securityProfileRetrieveAllReturn\"\
  )\n    if data:\n        data = data if isinstance(data, list) else [data]\n   \
  \     data = [{k: formatCell(row[k]) for k in row} for row in data]\n        demisto.results({\"\
  ContentsFormat\": formats[\"table\"], \"Type\": entryTypes[\"note\"], \"Contents\"\
  : data} )\n    else:\n        demisto.results(\"No results.\")\n"
scripttarget: 0
subtype: python2
system: true
tags:
- Trend Micro
timeout: 0s
toversion: 4.1.9
type: python
