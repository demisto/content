args:
- description: Number of alerts to fetch
  name: count
  required: true
comment: Get last alerts
commonfields:
  id: TrendmicroAlertStatus
  version: -1
dependson:
  must:
  - trendmicro-alert-status
name: TrendmicroAlertStatus
script: "\nresp = demisto.executeCommand(\"trendmicro-alert-status\", demisto.args())\n\
  \nif isError(resp[0]):\n    demisto.results(resp)\nelse:\n    data = demisto.get(resp[0],\
  \ \"Contents.alertStatusRetrieveResponse.alertStatusRetrieveReturn\")\n    if data:\n\
  \        data = data if isinstance(data, list) else [data]\n        data = [{k:\
  \ formatCell(row[k]) for k in row} for row in data]\n        demisto.results({\"\
  ContentsFormat\": formats[\"table\"], \"Type\": entryTypes[\"note\"], \"Contents\"\
  : data} )\n    else:\n        demisto.results(\"No results.\")\n"
scripttarget: 0
subtype: python2
system: true
tags:
- Trend Micro
timeout: 0s
toversion: 4.1.9
type: python
