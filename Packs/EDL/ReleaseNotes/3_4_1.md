
#### Integrations

##### Generic Export Indicators Service

- Fixed an issue where newly-added indicators were not reflected in the exported list for large, frequently-polled instances, because a stale cached copy could be served indefinitely. Concurrent cache-building requests now converge to the freshly-built list, and a rejected concurrent build falls back to serving the last-known-good list instead of returning an error.
