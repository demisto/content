
#### Integrations

##### Generic Export Indicators Service

- Documentation improvements: clarified that each instance now uses three consecutive ports (Listen Port, port + 1, port + 2), to help avoid `[Errno 98] Address in use` errors.
- Documentation improvements: clarified that while the cache is being populated, concurrent requests for the same entry return HTTP 429.
