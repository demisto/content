
#### Integrations

##### Generic Export Indicators Service

- Fixed an issue where running the instance test more than once could fail with an `[Errno 98] Address in use` error. The instance test now detects the already-running healthy EDL instance from the previous run and passes, instead of attempting to re-bind the ports.
