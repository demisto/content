
#### Integrations

##### Generic Export Indicators Service

- Fixed an issue where running test-module more than once could fail with an `[Errno 98] Address in use` error. Test-module now detects the already-running healthy EDL instance from the previous run and passes, instead of attempting to re-bind the ports.
