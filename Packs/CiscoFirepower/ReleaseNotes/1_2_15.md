#### Modeling Rules

##### CiscoFirepower Modeling Rule

- Updated the modeling rule to split processing into two sections:
  - CEF logs: mapped using the existing CEF field mappings.
  - Non-CEF logs: mapped using the `_json` object populated by `parse_cisco` function.

#### Parsing Rules

##### New: CiscoFirepower Parsing Rule

- New: Added a parsing rule that extracts the timestamp from Cisco Firepower logs using the `parse_cisco` function.