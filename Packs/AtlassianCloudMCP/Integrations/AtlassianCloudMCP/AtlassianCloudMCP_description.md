To obtain the Authorization code, follow these steps:

1. Save the integration instance.
2. Run the command `atlassian-cloud-mcp-generate-login-url` and follow its instructions.
3. Copy the generated Authorization code into the integration instance and save it.
4. Afterward, you can run the command `atlassian-cloud-mcp-auth-test` to verify that everything is configured correctly.
