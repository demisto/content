
#### Modeling Rules

##### Arista Switch Modeling Rules

Added support for parsing ACL (`%ACL-6-IPACCESS`) events that do not include an interface token, so the ACL action, protocol, and interface are mapped correctly whether or not an interface is present.
