#### Integrations
##### ShadowxSOCAI
- Added **API Key (Bearer) authentication** with configurable submission and check endpoints.
- Kept **cookie (HTML) mode** as fallback with robust anti-forgery token handling.
- Normalized required argument to `log`.
- Added `shadowx-help` command and improved in-product docs.
- Pinned docker image to a tag present on the server (`demisto/python3:3.11.10.116949`).