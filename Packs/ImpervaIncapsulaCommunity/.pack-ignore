# Add demisto-sdk validate/lint error-code exceptions here as they surface when this pack
# is actually run through `demisto-sdk validate` / `demisto-sdk lint` against a real content
# checkout. Intentionally left without speculative entries.
