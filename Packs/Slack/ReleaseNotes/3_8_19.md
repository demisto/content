#### Modeling Rules

##### Slack Modeling Rule

- Fixed an issue where **auth_target** and **auth_target_id** were not populated for Slack authentication events (event type 102). The authentication mapping now sets **xdm.target.resource.name** and **xdm.target.resource.id** to the constant value **Slack**, so Slack logins reach the Authentication story with a valid target.
