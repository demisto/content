
#### Modeling Rules

##### Squid Modeling Rule

- Fixed an issue where **xdm.source.ipv6** was incorrectly populated with the event timestamp instead of the client IPv6 address. The IPv6 extraction now requires a valid IPv6 structure (colon-separated hextets), so a numeric epoch timestamp can no longer match.
