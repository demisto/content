
#### Scripts

##### SetAndHandleEmpty

- Fixed an issue where the script treated the value **0** (and other falsy values such as **false**) as empty, resulting in an empty context entry. These values are now treated as valid values and written to the context.
