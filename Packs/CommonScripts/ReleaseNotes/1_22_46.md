#### Scripts

##### GetDockerImageLatestTag

- Fixed an issue where the script could return a non-runnable OCI artifact tag (e.g. a cosign signature tag ending in `.sig`, `.att`, or `.sbom`) as the latest Docker image tag, causing downstream script execution failures.
