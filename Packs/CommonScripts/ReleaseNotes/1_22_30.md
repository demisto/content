#### Scripts

##### GenericPollingScheduledTask

- Fixed an issue where polling would exit prematurely (completing the blocking task as successful) on platform versions where the JavaScript ``dq`` context query returns its result wrapped in a ``{"result": ...}`` object. The script now unwraps this envelope before evaluating the pending IDs.
- Added debug logging to aid future investigations of the polling flow.
