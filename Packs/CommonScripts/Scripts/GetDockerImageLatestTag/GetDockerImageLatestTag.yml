args:
- description: 'Docker image full name with version: For example: demisto/python'
  name: docker_image
  required: true
- auto: PREDEFINED
  defaultValue: 'yes'
  description: Use system proxy settings
  name: use_system_proxy
  predefined:
  - 'yes'
  - 'no'
- auto: PREDEFINED
  defaultValue: 'no'
  description: Trust any certificate (not secure)
  name: trust_any_certificate
  predefined:
  - 'yes'
  - 'no'
comment: Gets docker image latest tag. Script simulates the docker pull flow but doesn't
  actually pull the image. Returns an entry with the docker image latest tag if all
  is good, otherwise will return an error.
commonfields:
  id: GetDockerImageLatestTag
  version: -1
name: GetDockerImageLatestTag
script: '-'
subtype: python2
tags: []
tests:
- No test - no testplaybook, but unit tests exist
toversion: 4.1.9
type: python
