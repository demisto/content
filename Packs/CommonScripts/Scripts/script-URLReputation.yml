args:
- default: true
  description: URL to look up
  name: url
  required: true
comment: A context script for URL entities
commonfields:
  id: URLReputation
  version: -1
enabled: true
name: URLReputation
runonce: false
script: "results = demisto.executeCommand('url', {'url': demisto.get(demisto.args(),'url')})\n\
  \nfor item in results:\n    if isError(item):\n        item['Contents'] = item['Brand']\
  \ + ' returned an error.\\n' + item['Contents']\n\ndemisto.results(results)"
scripttarget: 0
subtype: python2
tags:
- enhancement
toversion: 4.1.9
type: python
