args:
- description: List of names of the users to retrieve.
  name: user_name
  isArray: true
  required: true
- description: |-
    Which integrations brands to run the command for. If not provided, the command will run for all available integrations.
    For multi-select provide a comma-separated list. For example: "Okta v2,Microsoft Graph User".
  name: brands
  auto: PREDEFINED
  predefined:
  - 'Okta v2'
  - 'Microsoft Graph User'
- description: Set to true to display human-readable output for each step of the command. Set to false (default) to only display the final result.
  name: verbose
  auto: PREDEFINED
  predefined:
  - 'true'
  - 'false'
comment: This script clears user sessions across multiple integrations for a list of usernames.
commonfields:
  id: clear-user-session
  version: -1
name: clear-user-session
outputs:
- contextPath: SessionClearResults
  description: A dictionary containing session clearance results, where each key is a username and the value is an object with the session clearance status, message, and source.
  type: unknown
script: '-'
timeout: '0'
type: python
subtype: python3
dockerimage: demisto/python3:3.11.10.111526
fromversion: 6.10.0
tests:
- No tests (auto formatted)
