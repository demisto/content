## KeyValueToJson

This script will take in an array of values and an array of keys.

It will create a JSON dictionary using the key and value pairs based on their indicies.

For example:

`!KeyValueToJson value="1,2,3" key="a,b,c"`

will return `{'a': 1, 'b':2, 'c': 3}`
