#### Integrations

##### ServiceNow v2

- Updated the Docker image to: *demisto/auth-utils:1.0.0.7837600*.

- Fixed an issue where the ***servicenow-create-item-order*** command failed with a `400` error when the integration was configured with API version **v2**. The `servicecatalog/items/{id}/order_now` endpoint only supports API v1, so the URL is now automatically downgraded to v1 for this endpoint.
- Added the optional **no_validation** argument to the ***servicenow-create-item-order*** command. When set to **true**, the `sysparm_no_validation` flag is included in the request body, allowing catalog item orders to bypass ServiceNow form validation.