
#### Modeling Rules

##### Zoom Modeling Rule

- Updated the Zoom Modeling Rule to map the Zoom sign-in (`activity_log`) events to the XDM authentication (102) story and the Zoom audit (`operation_log`) events to the XDM SaaS (501) story.
- Updated the Zoom Modeling Rule modeling rule to be supported up to Cortex XSIAM version 2.11.

##### New: Zoom Modeling Rule

- New: Added a new modeling rule version that maps the `xdm.*.identity.*` fields alongside the existing `xdm.*.user.*` fields.
<~XSIAM> (Available from Cortex XSIAM 2.11).</~XSIAM>
