<~PLATFORM>

#### Integrations

##### Cortex Platform - Core

- Documentation and metadata improvements.
<!--
- Enhanced the **core-case-update** command to gracefully handle *multiSelect* custom fields when a plain string value is provided instead of a list — the value is automatically wrapped into a single-element list. Additionally, when the field defines an enumeration of allowed values, the provided value is now validated against that set and a clear error is returned if an invalid value is supplied.
-->

</~PLATFORM>
