
#### Scripts

##### SearchIndicatorInEventsAgentix
- Updated the Docker image to: *demisto/python3:3.12.13.8428455*.

- Updated the dependency metadata to explicitly mark the ***SearchIndicatorInEvents*** command as mandatory. The script will now only be available when an integration that provides this command is configured and enabled.

##### DiagnoseSyslogCollector
- Updated the dependency metadata to explicitly mark the ***core-list-brokers*** and ***core-xql-generic-query-platform*** commands as mandatory. The script will now only be available when an integration that provides these commands is configured and enabled.

##### CoreUnisolateEndpoint

- Updated the Docker image to: *demisto/python3:3.12.13.8428455*.

- Updated the dependency metadata to explicitly mark the ***core-unisolate-endpoint*** command as mandatory. The script will now only be available when an integration that provides this command is configured and enabled.

##### CoreTerminateCausality

- Updated the Docker image to: *demisto/python3:3.12.13.8428455*.

- Updated the dependency metadata to explicitly mark the ***core-terminate-causality*** command as mandatory. The script will now only be available when an integration that provides this command is configured and enabled.

##### CoreIsolateEndpoint

- Updated the Docker image to: *demisto/python3:3.12.13.8428455*.

- Updated the dependency metadata to explicitly mark the ***core-isolate-endpoint*** command as mandatory. The script will now only be available when an integration that provides this command is configured and enabled.

##### CoreTerminateProcess

- Updated the Docker image to: *demisto/python3:3.12.13.8428455*.

- Updated the dependency metadata to explicitly mark the ***core-terminate-process*** command as mandatory. The script will now only be available when an integration that provides this command is configured and enabled.

##### CoreQuarantineFiles

- Updated the Docker image to: *demisto/python3:3.12.13.8428455*.

- Updated the dependency metadata to explicitly mark the ***core-quarantine-files*** command as mandatory. The script will now only be available when an integration that provides this command is configured and enabled.
