
#### Integrations

##### Unit 42 Feed

- Updated the Docker image to: *demisto/python3:3.12.14.12343672*.
- Updated the maximum number of indicators fetched per run to a single total limit of 20,000 to improve fetch performance.
- Updated the fetch interval to 1 hour. Existing instances created with a different interval will fail the test and must be recreated to apply the 1-hour interval.
- Updated the integration to fetch Threat Objects at most once every 24 hours, while indicators continue to be fetched every hour.
- Improved the fetch implementation to be incremental. When the fetch limit is reached and more data is available, the next fetch resumes from where the previous one stopped instead of restarting the same query. An interrupted Threat Objects fetch resumes on the next run without waiting for the 24-hour window.
