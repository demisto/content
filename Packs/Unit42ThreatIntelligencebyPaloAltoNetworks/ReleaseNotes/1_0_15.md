#### Integrations

##### Unit 42 Intelligence

- Fixed an issue in the ***url*** command where URLs wrapped in double-quote characters (e.g. `"url1","url2"`) were not correctly split and the leading quote was sent to the API, causing a 400 error.
