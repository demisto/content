#### Integrations

##### Unit 42 Intelligence

- Fixed an issue in the ***url*** command where enrichment failed with a 400 error for URLs containing special characters (such as `^` and `|`) without a URL scheme prefix.
