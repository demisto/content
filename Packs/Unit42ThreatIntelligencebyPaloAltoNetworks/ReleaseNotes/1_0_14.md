#### Integrations

##### Unit 42 Intelligence

- Fixed an issue where the ***url*** command failed with a 400 error when enriching URLs containing special characters (such as `^` and `|`) without a URL scheme prefix.
