#### Integrations

##### Unit 42 Intelligence

- Fixed an issue where the ***url*** command incorrectly parsed URLs containing commas in their parameters, causing the command to fail with an "invalid URL format" error.
