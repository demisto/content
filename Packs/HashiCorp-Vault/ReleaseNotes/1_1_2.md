
#### Integrations
##### HashiCorp Vault
- Added a new parameter to *hashicorp-configure-engine* called *aws_roles_list* that will let the user control for which roles it wants to generate credentials for. 
- Added a new parameter to *hashicorp-configure-engine* called *aws_method* that will let the user control type of request to generate credentials by. 
