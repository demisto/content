args:
- default: false
  description: Comma-separated list of incident urls
  isArray: false
  name: urls
  required: false
  secret: false
- auto: PREDEFINED
  default: false
  defaultValue: 'False'
  description: Whether to force the model to run in case the url belongs to the whitelist.
    If True the model will run in every cases. If False the model will run only if
    the url does not belongs to the whitelist
  isArray: false
  name: forceModel
  predefined:
  - 'True'
  - 'False'
  required: false
  secret: false
- default: false
  defaultValue: '1'
  description: Number of url on which we provide additional information and verdict
    explanations. The script will always provide information on malicious url at first.
  isArray: false
  name: numberDetailedReports
  required: false
  secret: false
- auto: PREDEFINED
  default: false
  defaultValue: 'False'
  description: Whether to reset the model to the model existing in the docker
  isArray: false
  name: resetModel
  predefined:
  - 'False'
  - 'True'
  required: false
  secret: false
commonfields:
  id: DBotPredictURLPhishing
  version: -1
enabled: true
name: DBotPredictURLPhishing
script: '-'
subtype: python3
system: false
timeout: '0'
type: python
dockerimage: devdemisto/mlurlphishing:1.0.0.24089
runas: DBotWeakRole
runonce: false
fromversion: 5.5.0
tests:
- No tests (auto formatted)
