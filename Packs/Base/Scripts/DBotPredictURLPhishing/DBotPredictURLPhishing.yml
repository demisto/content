args:
- default: false
  description: Comma-separated list of incident URLs.
  isArray: false
  name: urls
  required: false
  secret: false
- auto: PREDEFINED
  default: false
  defaultValue: 'False'
  description: Whether to force the model to run in case the URL belongs to the whitelist.
    If True, the model will run in every case. If False, the model will run only if
    the URL does not belongs to the whitelist.
  isArray: false
  name: forceModel
  predefined:
  - 'True'
  - 'False'
  required: false
  secret: false
- auto: PREDEFINED
  default: false
  defaultValue: 'False'
  description: Whether to reset the model to the model existing in Docker.
  isArray: false
  name: resetModel
  predefined:
  - 'False'
  - 'True'
  required: false
  secret: false
- auto: PREDEFINED
  default: false
  defaultValue: 'False'
  description: Whether to enter debug mode.
  isArray: false
  name: debug
  predefined:
  - 'False'
  - 'True'
  required: false
  secret: false
- default: false
  defaultValue: '3'
  isArray: false
  name: maxNumberOfURL
  required: false
  secret: false
- default: false
  isArray: false
  name: emailText
  required: false
  secret: false
comment: Predict phishing URLs using pre-trained model.
commonfields:
  id: DBotPredictURLPhishing
  version: -1
enabled: true
name: DBotPredictURLPhishing
outputs:
- contextPath: DBotPredictURLPhishing.URL
  description: URL on which the model runned.
  type: String
- contextPath: DBotPredictURLPhishing.FinalVerdict
  description: Final verdict of the URL.
  type: String
- contextPath: DBotPredictURLPhishing.IsWhitelisted
  description: Wether the URL has been whitelisted bu the model.
  type: Boolean
script: '-'
subtype: python3
system: false
tags:
- ''
timeout: '0'
type: python
dockerimage: devdemisto/mlurlphishing:1.0.0.24148
runas: DBotWeakRole
runonce: false
tests:
- No tests (auto formatted)
fromversion: 5.5.0
