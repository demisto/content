
#### Scripts

##### CommonServerPython

- Added Unified Connector (UCP) parameter interpolation: connector field values are now reshaped into the parameter structure integrations expect (for example, folding username and password into a single `credentials` object).
##### CommonServerPowerShell

- Added Unified Connector (UCP) parameter interpolation: connector field values are now reshaped into the parameter structure integrations expect (for example, folding username and password into a single `credentials` object).
- Updated the Docker image to: *demisto/powershell:7.5.0.9017890*.
##### CommonServer

- Added Unified Connector (UCP) parameter interpolation: connector field values are now reshaped into the parameter structure integrations expect (for example, folding username and password into a single `credentials` object).
