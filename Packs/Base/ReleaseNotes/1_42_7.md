
#### Scripts

##### CommonServerPython

Fixed an issue where the retry mechanism in the base client raised a *"Retry.__init__() got an unexpected keyword argument 'backoff_jitter'"* ***TypeError*** on environments running an older *urllib3* version that does not support the *backoff_jitter* parameter, causing *test-module* and all HTTP requests with retries configured to fail.
