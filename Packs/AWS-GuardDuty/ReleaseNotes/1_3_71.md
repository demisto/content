
#### Integrations

##### AWS - GuardDuty Event Collector

- Fixed an issue where GuardDuty findings sharing the same ``UpdatedAt`` second could be silently dropped between fetch cycles. The integration now tracks every finding ID at the cursor second instead of a single ID, eliminating same-second sibling loss.
