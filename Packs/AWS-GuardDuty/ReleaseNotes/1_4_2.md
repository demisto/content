
#### Integrations

##### AWS - GuardDuty Event Collector

- Fixed an issue where findings sharing the same second could be re-fetched every cycle, causing duplicate events.
- Fixed an issue where fetching could stall on a detector when the only findings returned had already been collected, preventing newer findings from being fetched.
- Added debug logging to simplify troubleshooting.
