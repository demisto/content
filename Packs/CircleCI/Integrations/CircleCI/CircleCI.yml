category: Utilities
commonfields:
  id: CircleCI
  version: -1
configuration:
- defaultvalue: https://circleci.com/api/v2
  display: Your server URL
  name: url
  required: true
  type: 0
- display: API Key
  additionalinfo: The API Key to use for connection
  name: api_key
  required: true
  type: 4
- display: Trust any certificate (not secure)
  name: insecure
  required: false
  type: 8
- display: Use system proxy settings
  name: proxy
  required: false
  type: 8
description: '[Enter a comprehensive, yet concise, description of what the integration
  does, what use cases it is designed for, etc.]'
display: CircleCI
name: CircleCI
script:
  commands:
  - arguments:
    - default: false
      description: Pipeline ID to retrieve its info.
      isArray: false
      name: pipeline_id
      required: false
      secret: false
    - name: vcs_type
      required: false
      description: 
      default: true
      auto: PREDEFINED
      predefined:
      - github
      - bitbucket
      defaultValue: github
    - default: false
      description: Organization to pipelines from. Defaults to organization instance parameter if none is geven.
      isArray: false
      name: organization
      required: false
      secret: false
    - default: false
      description: Project to pipelines from. Defaults to project instance parameter if none is geven.
      isArray: false
      name: project
      required: false
      secret: false
    deprecated: false
    description: Get info of pipelines.
    execution: false
    name: circleci-pipelines-list
    outputs:
    - contextPath: BaseIntegration.Output
      description: '[Enter a description of the data returned in this output.]'
      type: String
  isfetch: false
  runonce: false
  script: '-'
  type: python
  subtype: python3
  dockerimage: demisto/python3:3.9.5.21272
fromversion: 5.5.0
tests:
- No tests (auto formatted)
