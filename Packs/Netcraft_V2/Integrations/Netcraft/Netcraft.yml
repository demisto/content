commonfields:
  id: Netcraft
  version: -1
name: Netcraft
display: Netcraft
category: Utilities
description: Netcraft takedown, submission and screenshot management.
sectionOrder:
- Connect
- Collect
configuration:
- display: Takedown Server URL
  name: takedown_url
  defaultvalue: "https://takedown.netcraft.com/api/v1"
  type: 0
  required: true
  section: Connect
- display: Submission Server URL
  name: submission_url
  defaultvalue: "https://report.netcraft.com/api/v3"
  type: 0
  required: true
  section: Connect
- name: credentials
  type: 9
  required: true
  displaypassword: API Key
  hiddenusername: true
  section: Connect
- name: region
  type: 0
  required: true
  displaypassword: Region
  section: Connect
- display: Trust any certificate (not secure)
  name: insecure
  type: 8
  section: Connect
  advanced: true
  required: false
- display: Use system proxy settings
  name: proxy
  type: 8
  section: Connect
  advanced: true
  required: false
- display: Fetch incidents
  name: isFetch
  type: 8
  section: Collect
  required: false
- display: Incident type
  name: incidentType
  type: 13
  section: Collect
  defaultvalue: Netcraft Attack
  required: false
- defaultvalue: '10'
  display: Maximum number of incidents per fetch
  name: max_fetch
  type: 0
  section: Collect
  required: false
- defaultvalue: 3 days
  display: First fetch time
  name: first_fetch
  type: 0
  section: Collect
  required: true
- defaultvalue: '1'
  display: 'Incidents Fetch Interval'
  name: incidentFetchInterval
  type: 19
  section: Collect
  advanced: true
  required: false
script:
  commands:
  - name: 'netcraft-attack-report'
    description: |
      Report a new attack or authorise an existing attack in the Takedown service.
      If a takedown for the attack already exists in the system it will be authorised, otherwise, a new takedown will be added and authorised.
    arguments:
    - name: attack
      required: true
      description: The location of the attack to take down, e.g. a phishing URL, or a fraudulent email address.
    - name: comment
      required: true
      description: 	The reason for the report, such as a description of the attack.
    - description: The brand to report the takedown under. If no brand is specified, the brand of the provided "region" will be used.
      name: brand
    - defaultValue: phishing_url
      description: |
        The type of attack being reported.
        Run the command "netcraft-attack-type-list" to get the list of available types, use the "Name" field of the type for this argument.
      name: attack_type
    - auto: PREDEFINED
      description: |
        Set to "true" if the attack is not currently active.
        This will place the takedown directly into the "Inactive (Monitoring)" status, which can be used to monitor suspicious sites.
      name: inactive
      defaultValue: 'false'
      predefined:
      - 'true'
      - 'false'
    - auto: PREDEFINED
      description: |
        If set to true, Netcraft will be authorised to start the takedown as soon as the report has been processed.
        If set to false, the takedown will only be authorised if you have automatic authorisation enabled for the given attack type, or if the takedown is manually authorised later through the web interface.
      name: force_auth
      defaultValue: 'true'
      predefined:
      - 'true'
      - 'false'
    - auto: PREDEFINED
      description: |
        Should be set to true if the reported content contains or is related to a computer virus.
        This is used to determine the correct attack type in the case where the type parameter has not been provided.
        This argument is required for all malware attacks where the type parameter has not been provided.
      name: malware
      predefined:
      - 'true'
      - 'false'
    - auto: PREDEFINED
      defaultValue: 'false'
      description: |
        Should be set to true if you believe that the domain name has been registered as part of the fraud.
        This will ensure that the registrar is contacted to seek suspension of the domain name.
      name: suspected_fraud_domain
      predefined:
      - 'true'
      - 'false'
    # - description: |
    #     "The password to extract any archived evidence provided via the evidence\_\
    #     parameter, if necessary."
    #   name: password
    # AWAITING TPM RESPONSE
    - description: |
        The URL where the phishkit archive was found.
        This parameter is required for attacks of type "phishkit_email".
      name: phishkit_fetch_url
    - description: |
        The URL of the phishing attack which uses the referenced phishkit.
        This parameter is required for attacks of type "phishkit_email" and/or "phishkit_archive".
      name: phishkit_phish_url
    - description: A free-text field which can be used to keep track of particular attacks.
      name: customer_label
    - description: A comma-separated list of tags to apply to the attack.
      isArray: true
      name: tags
    - description:
        The name of the region to create a takedown under.
        If not provided, the region specified in the instance configuration will be used.
      name: region
    outputs:
    - contextPath: Netcraft.Takedown.id
      description: The ID of the takedown. (this key will only appear if the takedown has been created and verified).
      type: String
  - name: 'netcraft-takedown-list'
    arguments:
    - name: id
      description: Filter to the takedown with the specified ID.
    - name: statuses
      description: Filter to takedowns that are currently in the given status. Multiple values may be provided as a comma-separated list.
      isArray: true
      auto: PREDEFINED
      predefined:
      - unverified
      - inactive_monitoring
      - verified
      - contacted_hosting
      - contacted_police
      - contacted_upstream
      - resolved_monitoring
      - resolved
      - stale
      - invalid
    - name: url
      description: Filter by URL, email, hostname, domain or IP.
    - name: false_positive
      description: whether to filter to takedowns which have been incorrectly marked as malicious.
      auto: PREDEFINED
      predefined:
      - 'true'
      - 'false'
    - name: ip
      description: Filter to attacks that are hosted on the given IPv4 address, or within the given IPv4 CIDR range. Please note that partial IP addresses will not be matched.
    - description: Filter to takedowns that were submitted before the specified takedown ID.
      name: id_before
    - description: |
        Filter to takedowns that were submitted after the specified takedown
        ID. When using this argument we recommend that you also set the "sort" argument
        to id to ensure that no results are missed.
      name: id_after
    - description: |
        Filter to takedowns that were submitted on or after the date/time
        provided.Values should be supplied as YYYY-MM-DD HH:MM:SS\_in UTC. If no time
        information is provided, the system will default to YYYY-MM-DD 00:00:00. Relative
        date/time formats are also supported, for example\_5 days ago,\_and monday
        this week"
      name: date_from
    - description: |
        Filter to takedowns that were submitted on or before the date/time
        provided. Values should be supplied as "YYYY-MM-DD HH:MM:SS" in UTC. If no time
        information is provided, the system will default to "YYYY-MM-DD 00:00:00". Relative
        date/time formats are also supported, for example "5 days ago", and "monday this week"
      name: date_to
    - description: Filter to takedowns that were reported by the given user.
      name: reporter_email
    - description: Filter to takedowns that were reported through the given mechanism.
      name: report_source
      auto: PREDEFINED
      predefined:
      - Interface
      - Phishing Feed
      - Referer
      - Forensic
      - Api
      - Email Feed
      - Fraud Detection
    - description: |
        Filter to takedowns of the given attack type. Multiple values may be provided as a comma-separated list.
        Run the command "netcraft-attack-type-list" to get the list of available types, use the "Name" field of the type for this argument.
      name: attack_types
      isArray: true
    - description: Filter based on whether and by who the takedown has been authorised.
      name: auth_given
      auto: PREDEFINED
      predefined:
      - 'Yes'
      - Yes Customer
      - Yes Netcraft
      - 'No'
    - description: Filter based on whether and by who the takedown has been escalated.
      name: escalated
      auto: PREDEFINED
      predefined:
      - 'Yes'
      - Yes Customer
      - Yes Netcraft
      - 'No'
    - defaultValue: Status
      description: The key that the list of takedowns should be sorted by.
      name: sort
      auto: PREDEFINED
      predefined:
      - Auth Given
      - Customer Label
      - Date Submitted
      - Hoster
      - Id
      - Ip
      - Language
      - Last Updated
      - Registrar
      - Status
    - defaultValue: asc
      auto: PREDEFINED
      predefined:
      - asc
      - desc
      description: The direction to sort takedowns in with the key specified in the "sort" argument.
      name: sort_direction
    - defaultValue: '50'
      description: The maximum number of takedowns to return. The max value is 100,000.
      name: limit
    - auto: PREDEFINED
      defaultValue: 'false'
      description: |
        Whether to retrieve all takedowns that match the filters.
        If true, the "limit" argument will be ignored. The maximum takedowns returned in one call is 100,000.
      name: all_results
      predefined:
      - 'false'
      - 'true'
    - description:
        The name of the region of which to list takedowns.
        If not provided, the region specified in the instance configuration will be used.
      name: region
    description: |
      Get a list of takedown objects.
      There is a limit of 1,000,000 objects returned within a 24 hour period (moving time window) per email address.
    outputs:
    - contextPath: Netcraft.Takedown.id
      description: The ID of the takedown.
      type: String
    - contextPath: Netcraft.Takedown.group_id
      description: The ID of the group that the takedown belongs to. can potentially be the same a ID, or empty if there is no group.
      type: String
    - contextPath: Netcraft.Takedown.attack_url
      description: The location of the attack being taken down. This field contains a canonicalised value. see the reported_url field for the exact location that was reported to takedown.
      type: String
    - contextPath: Netcraft.Takedown.reported_url
      description: The location of the attack as reported to takedown. See the attack_url field for the formatted location of the attack being taken down.
      type: String
    - contextPath: Netcraft.Takedown.ip
      description: The IPv4 address of the attack.
      type: String
    - contextPath: Netcraft.Takedown.country_code
      description: ISO country code of the advertised hosting location.
      type: String
    - contextPath: Netcraft.Takedown.date_submitted
      description: The date and time that the takedown was reported, in UTC.
      type: Date
    - contextPath: Netcraft.Takedown.last_updated
      description: The date and time of the last action taken on the takedown, in UTC.
      type: Date
    - contextPath: Netcraft.Takedown.region
      description: The name of the area that the takedown resides in.
      type: String
    - contextPath: Netcraft.Takedown.target_brand
      description: The name of the brand being targeted by the attack.
      type: String
    - contextPath: Netcraft.Takedown.authgiven
      description: Indicates whether the takedown has been authorised.
      type: String
    - contextPath: Netcraft.Takedown.host
      description: The name of the company responsible for the IP address.
      type: String
    - contextPath: Netcraft.Takedown.registrar
      description: The name of the registrar responsible for the domain name used in the attack.
      type: String
    - contextPath: Netcraft.Takedown.customer_label
      description: A custom field which may be provided along with the takedown report.
      type: String
    - contextPath: Netcraft.Takedown.date_authed
      description: The date and time that the takedown was authorised, in UTC.
      type: Date
    - contextPath: Netcraft.Takedown.stop_monitoring_date
      description: |
        The date and time that the takedown system stopped monitoring the attack, in UTC. 
        If the attack is still being monitored, an empty string is given.
      type: Date
    - contextPath: Netcraft.Takedown.domain
      description: |
        The domain of the url or email address being taken down.
        This will be blank for an attack with no domain name.
      type: String
    - contextPath: Netcraft.Takedown.language
      description: The language used in the attack (if it can be determined).
      type: String
    - contextPath: Netcraft.Takedown.date_first_actioned
      description: |
        The date and time of the first action taken by netcraft
        after the takedown was reported, in UTC. This is calculated as the first time
        that the takedown was moved out of the "unverified" status.
      type: Date
    - contextPath: Netcraft.Takedown.escalated
      description: Indicates whether the takedown has been escalated.
      type: String
    - contextPath: Netcraft.Takedown.first_contact
      description: The date and time that the takedown first entered a contacted state, in UTC.
      type: Date
    - contextPath: Netcraft.Takedown.first_inactive
      description: The date and time that the takedown first entered the inactive (monitoring) state.
      type: Date
    - contextPath: Netcraft.Takedown.is_redirect
      description: |
        Whether or not the attack redirects to another location.
        Possible values:
          "final" - the attack is the final destination of another redirect.
          "redirect" - the attack redirects to another location.
          "no_redirect" - the attack does not redirect.
      type: String
    - contextPath: Netcraft.Takedown.attack_type
      description: The type of attack being taken down.
      type: String
    - contextPath: Netcraft.Takedown.certificate
      description: |
        HTTPS certificate details for the hostname.
        The value returned is the output of PHP's "openssl_x509_parse" function with the additional keys spki_sha256 and spki_sha1, (See https://www.php.net/manual/en/function.openssl-x509-parse.php).
      type: Unknown
    - contextPath: Netcraft.Takedown.deceptive_domain_score
      description: |
        The deceptive domain score of the domain. 
        e.g. for the URL https://l0gin.examp1eb4nk.com/, this value will contain the deceptive domain score for examp1eb4nk.com.
      type: String
    - contextPath: Netcraft.Takedown.domain_risk_rating
      description: |
        A score from 0 to 10 which represents the risk that the domain is hosting a malicious website.
        e.g. for the url https://l0gin.examp1eb4nk.com/, this value wil contain the risk rating for "examp1eb4nk.com".
        This score is distinct from the "deceptive domain score", and takes a range of factors into account,
        such as the reputation of the hosting provider, age of the domain name, search engine rankings and more.
      type: String
    - contextPath: Netcraft.Takedown.final_outage
      description: |
        The duration (hh:mm:ss) between when the takedown was
        authorised, and the final time that the attack went offline (final_resolved - date_authed).
      type: String
    - contextPath: Netcraft.Takedown.final_resolved
      description: The date and time that the attack went offline for the
        final time, in UTC.
      type: Date
    - contextPath: Netcraft.Takedown.first_outage
      description: The duration (hh:mm:ss) between when the takedown was authorised, and the first time that the attack went offline (first_resolved - date_authed).
      type: Date
    - contextPath: Netcraft.Takedown.first_resolved
      description: The date and time that the attack first went offline, in UTC.
      type: Date
    - contextPath: Netcraft.Takedown.fwd_owner
      description: The owner of the forward DNS infrastructure.
      type: String
    - contextPath: Netcraft.Takedown.has_phishing_kit
      description: Indicating whether the takedown has an associated phishing kit.
      type: String
    - contextPath: Netcraft.Takedown.hostname
      description: The full hostname of the URL or email address being taken down. this will be blank for attacks with no hostname.
      type: String
    - contextPath: Netcraft.Takedown.hostname_ddss_score
      description: |
        The deceptive domain score of the hostname.
        e.g. for the URL https://l0gin.examp1eb4nk.com/, this value will contain the deceptive domain score for l0gin.examp1eb4nk.com.
      type: String
    - contextPath: Netcraft.Takedown.evidence_url
      description: A url to the public incident report for this attack.
      type: String
    - contextPath: Netcraft.Takedown.domain_attack
      description: |
        Whether or not the domain name used in the attack is believed to be fraudulent.
        Possible values (non exhaustive):
          "all" - All attacks.
          "yes" - There is a high confidence that the domain name is fraudulent.
                  the domain registrar will be contacted, and the webmaster will not be contacted.
          "yes_low_confidence" - The domain is likely fraudulent. The domain registrar
                                will be contacted, and the webmaster will still be contacted.
          "no" - The domain name is not believed to be fraudulent, this is likely compromised site.
      type: String
    - contextPath: Netcraft.Takedown.false_positive
      description: Indicates whether the reported content was incorrectly flagged as malicious.
      type: Boolean
    - contextPath: Netcraft.Takedown.hostname_attack
      description: |
        Whether or not the hostname used in the attack is believed to be fraudulent.
        Possible values (non exhaustive):
          "all" - All attacks.
          "yes" - There is a high confidence that the hostname is fraudulent. the certificate authority will be contacted. 
          "yes_low_confidence" - The hostname is likely fraudulent. 
          "no" - The hostname is not believed to be fraudulent, this is likely compromised site.
      type: String
    - contextPath: Netcraft.Takedown.malware_category
      description: The category of malware detected. Only set for malware attack types. May be empty if category cannot be determined.
      type: String
    - contextPath: Netcraft.Takedown.malware_family
      description: The family of malware detected. only set for malware attack types. may be empty if family cannot be determined.
      type: String
    - contextPath: Netcraft.Takedown.phishing_kit_hash
      description: The sha1 hashes of all phishing kits available for download which are related to this takedown. 
      type: Unknown
    - contextPath: Netcraft.Takedown.report_source
      description: The method through which the takedown was submitted.
      type: String
    - contextPath: Netcraft.Takedown.reporter
      description: Person/account that submitted the takedown. This will be the email address of the user, or "netcraft" for any reports made by Netcraft.
      type: String
    - contextPath: Netcraft.Takedown.rev_owner
      description: The owner of the reverse DNS infrastructure.
      type: String
    - contextPath: Netcraft.Takedown.reverse_dns
      description: The output of a reverse DNS lookup on the IP of the attack.
      type: String
    - contextPath: Netcraft.Takedown.certificate_revoked
      description: Not revoked if the SSL certificate has been revoked, then the date this was detected (in UTC) is returned, else "not revoked" is returned.'
      type: String
    - contextPath: Netcraft.Takedown.screenshot_url
      description: |
        The URL(s) at which a screenshot of the attack can be found.
        When returning a single URL as a string (the default behaviour) the returned URL will be the best guess of the screenshot which displays the live attack.
        When returning multiple URLs, the list will be sorted by the time the screenshot was requested, with the earliest first.
      type: String
    - contextPath: Netcraft.Takedown.status_change_uptime
      description: |
        The total duration (hh:mm:ss) that the attack was available for after authorisation, as determined by the takedown status changes.
        i.e. the total amount of time since authorisation that an attack was not in the resolved or resolved (monitoring)  state.'
      type: String
    - contextPath: Netcraft.Takedown.status
      description: |
        The status of the takedown.
        Possible values:
          "Unverified" - The report has not yet been verified as fraudulent by Netcraft.
          "Inactive (Monitoring)" - The attack went offline before Netcraft was authorised to start the takedown process, and is being monitored in case it returns.
          "Verified" - The report has been verified as fraudulent, but no takedown notices have been sent.
          "Contacted Hosting" - Takedown notices have been sent to the hosting provider
          "Contacted Police" - The takedown has been escalated to local law enforcement.
          "Contacted Upstream" - The takedown has been escalated to the organisation providing connectivity to the hosting provider.
          "Monitoring" - The attack is offline, as is being monitored in case it returns.
          "Resolved" - The attack has been offline for 7 consecutive days, and is no longer being monitored.
          "Stale" - The attack went offline before Netcraft was authorised to start the takedown process, and is no longer being monitored.
          "Invalid" - The report is not a valid takedown target.
      type: String
    - contextPath: Netcraft.Takedown.tags
      description: List of tags applied to the group.
      type: String
    - contextPath: Netcraft.Takedown.targeted_url
      description: The URL which this attack is masquerading as, e.g. the URL of the legitimate login form that a phishing attack is targeting.
      type: String
    - contextPath: Netcraft.Takedown.site_risk_rating
      description: |
        A score from 0 to 10 which represents the risk that the hostname is hosting a malicious website.
        e.g. for the URL https://l0gin.examp1eb4nk.com/, this value will contain the risk rating for l0gin.examp1eb4nk.com.
      type: String
    - contextPath: Netcraft.Takedown.whois_server
      description: The WHOIS data for the takedown.
      type: String
    - contextPath: Netcraft.Takedown.authorisation_source
      description: |
        The source of authorisation for the takedown. will be blank if the takedown has not bee authorised.  customer
        Possible values: "customer" "netcraft" 
      type: String
    - contextPath: Netcraft.Takedown.escalation_source
      description: |
        The source of escalation for the takedown. will be blank if the takedown has not been escalated.
        Possible values: "customer" "netcraft"
      type: String
    - contextPath: Netcraft.Takedown.restart_date
      description: |
        The latest date and time, in UTC, that the takedown was restarted, i.e. went from the "resolved (monitoring)" status to a contacted status.
        Will be empty if the takedown ha never been restarted.'
      type: String
    - contextPath: Netcraft.Takedown.gsb_block_status
      description: |
        An array of objects containing the Google Safe Browsing block status on all platforms (iOS, Android and Desktop).
        Will be an empty array if the takedown is not a Phishing URL takedown, or if Netcraft hasn't tested the GSB block status for the takedown.
      type: Unknown
    - contextPath: Netcraft.Takedown.gsb_first_blocked
      description: |
        An array of objects containing the first time that the URL was seen blocked in Google Safe Browsing (GSB) by Netcraft.
        Will be an empty array if the URL was not seen blocked by GSB on any platform.
      type: Unknown
    - contextPath: Netcraft.Takedown.managed
      description: Indicates whether the takedown is being performed under the managed service.
      type: Boolean
    - contextPath: Netcraft.Takedown.date_escalated
      description: The date and time that the takedown entered the managed state, in UTC.
      type: Date
    - contextPath: Netcraft.Takedown.logged_credential_injections
      description: An array of objects containing the type and value of each marked account injection associated with the takedown.
      type: String
    - contextPath: Netcraft.Takedown.whois_data
      description: The WHOIS data for the takedown.
      type: String
  - name: 'netcraft-takedown-update'
    arguments:
    - name: takedown_id
      description: The ID of the takedown to update.
      required: true
    - name: customer_label
      description: A free-text field which can be used to keep track of particular attacks.
    - name: description
      description: The reason for the report, such as a description of the attack.
    - name: brand
      description: The brand to move the takedown under.
    - auto: PREDEFINED
      description: Should be set to true if it is believed that the domain name has been registered as part of the fraud. This will ensure that the registrar is contacted to seek suspension of the domain name.
      name: suspected_fraud_domain
      predefined:
      - 'true'
      - 'false'
    - auto: PREDEFINED
      description: 
      name: suspected_fraud_hostname
      predefined:
      - 'true'
      - 'false'
    - description: A comma separated list tags to add to the takedown group.
      isArray: true
      name: add_tags
    - description: |
        A comma separated list tags to remove from the takedown group.
        Removing a tag from a group which already doesn't have that tag is permitted.
        However, including the same tag in both "add_tags" and "remove_tags" will return an error.
      isArray: true
      name: remove_tags
    # TODO: what to do with this arg.
    # - description:
    #     The name of the region of which to list takedowns.
    #     If not provided, the region specified in the instance configuration will be used.
    #   name: region
    description: Update one or more fields related to a takedown.
    outputs: []
  - name: 'netcraft-takedown-escalate'
    arguments:
    - name: takedown_id
      required: true
      description: The ID of the automated takedown to escalate.
    description: Escalate an automated takedown to a managed takedown.
    outputs: []
  - name: 'netcraft-takedown-note-create'
    arguments:
    - description: The ID of the takedown to add the note to.
      name: takedown_id
      required: true
    - description: The contents of the new note.
      name: note_text
      required: true
    - auto: PREDEFINED
      defaultValue: 'true'
      description: |
        When set to true, the note will also be raised to Netcraft's operations team for further investigation.
        This should be set if you require a Netcraft staff member to read and respond to your note.
      name: notify
      predefined:
      - 'true'
      - 'false'
    description: Add a new note to an existing takedown.
    outputs:
    - contextPath: Netcraft.TakedownNote.note_id
      description: The ID of the note added to the takedown.
      type: Number

  - name: 'netcraft-takedown-note-list'
    arguments:
    - description: |
        ''
      name: takedown_id
    - description: |
        ''
      name: author_mail
    - defaultValue: '50'
      description: |
        Default value - 50
      name: limit
    - auto: PREDEFINED
      description: |
        Default - false
      name: all_results
      predefined:
      - 'true'
      - 'false'
    description: |
      ''
    outputs: []
  - name: 'netcraft-attack-type-list'
    arguments:
    - auto: PREDEFINED
      description: |
        Filter to attack types where automated takedowns are available.
      name: automated
      predefined:
      - 'true'
      - 'false'
    - auto: PREDEFINED
      description: |
        Filter to attack types which you have chosen to escalate to managed
        takedowns after the configured escalation period.
      name: auto_escalation
      predefined:
      - 'true'
      - 'false'
    - auto: PREDEFINED
      description: |
        Filter to attack types which you have chosen to automatically authorize
        takedowns against.
      name: auto_authorise
      predefined:
      - 'true'
      - 'false'
    - description: |
        ''
      name: region
    - description: |
        Default value 50
      name: limit
    - auto: PREDEFINED
      description: |
        Default value false.
      name: all_result
      predefined:
      - 'true'
      - 'false'
    description: |
      ''
    outputs: []
  - name: 'netcraft-file-report-submit'
    polling: true
    arguments:
    - description: |
        ''
      name: reporter_email
      required: true
    - description: |
        'The format of content_file is A base64 encoded string of the file"files":
        [{"content": "file_content","filename": "file_name"}]The purpose of this argument
        is to support reporting of multiple files with entry_id OR single file by file_content&file_name'
      name: file_content
    - description: |
        ''
      name: file_name
    - description: |
        ''
      isArray: true
      name: entry_id
    - description: |
        ''
      name: reason
    - auto: PREDEFINED
      description: |
        "Default value = trueIf the user changed to false we will not \_\
        execute the polling mechanism."
      name: polling
      predefined:
      - 'true'
      - 'false'
    - description: |
        Default 30
      name: interval_in_seconds
    - description: |
        Default 600
      name: timeout_in_seconds
    description: |
      ''
    outputs: []
  - name: 'netcraft-url-report-submit'
    arguments:
    - description: |
        ''
      name: reporter_email
      required: true
    - description: |
        ''
      isArray: true
      name: urls
      required: true
    - description: |
        ''
      name: reason
    - auto: PREDEFINED
      description: |
        "Default value = trueIf the user changed to false we will not \_\
        execute the polling mechanism."
      name: polling
      predefined:
      - 'true'
      - 'false'
    - description: |
        Default 30
      name: interval_in_seconds
    - description: |
        Default 600
      name: timeout_in_seconds
    description: |
      ''
    outputs: []
  - name: 'netcraft-email-report-submit'
    arguments:
    - description: |
        ''
      name: reporter_email
      required: true
    - description: |
        Either a plain text string of the malicious mail in MIME format or
        if the password paramater is provided, a base64 encoded AES-256-CBC encrypted
        mail in MIME format. Max message size is 20MiB.
      name: message
      required: true
    - description: |
        The password used to encrypt/decrypt the MIME mail. Should not be
        provided if the mail is not encrypted.
      name: password
    - auto: PREDEFINED
      description: |
        "Default value = trueIf the user changed to false we will not \_\
        execute the polling mechanism."
      name: polling
      predefined:
      - 'true'
      - 'false'
    - description: |
        Default 30
      name: interval_in_seconds
    - description: |
        Default 600
      name: timeout_in_seconds
    description: |
      ''
    outputs: []
  - name: 'netcraft-submission-list'
    arguments:
    - description: |
        "An optional argument to get a specific submission.In case that the\
        user provided this value we will execute the \u201Cget submission details\u201D\
        \_request, and ignore all the others arguments.Note: the response of the get\
        request contains fields that do not exist in the list command, make sure to\
        add them to the yaml file as well."
      name: submission_uuid
    - description: |
        Filter submissions by start date
      name: date_start
    - description: |
        Filter submissions by end date
      name: date_end
    - description: |
        ''
      name: source_name
    - description: |
        ''
      name: state
    - description: |
        ''
      name: submission_reason
    - description: |
        ''
      name: submitter_email
    - description: |
        Default 50API argument - page_size
      name: limit
    - description: |
        API argument - marker
      name: next_token
    - description: |
        ''
    outputs: []
  - name: 'netcraft-submission-file-list'
    arguments:
    - description: |
        "Add description that the user can get this value by running the\
        command \u201Csubmission-list\u201D."
      name: submission_uuid
      required: true
    - description: |
        Default 50API argument - count
      name: limit
    - description: |
        ''
      name: page
    - description: |
        Default 50API argument - count
      name: page_size
    description: |
      ''
    outputs: []
  - name: 'netcraft-file-screenshot-get'
    arguments:
    - description: |
        "Add description that the user can get this value by running the\
        command \u201Csubmission-list\u201D."
      name: submission_uuid
      required: true
    - description: |
        "Add description that the user can get this value by running the\
        command \u201Csubmission-file-list\u201D."
      name: file_hash
      required: true
    description: |
      ''
    outputs: []
  - name: 'netcraft-submission-mail-list'
    arguments:
    - description: |
        Note
      name: Argument name
    - description: |
        "Add description that the user can get this value by running the\
        command \u201Csubmission-list\u201D."
      name: submission_uuid
      required: true
    description: |
      ''
    outputs: []
  - name: 'netcraft-mail-screenshot-get'
    arguments:
    - description: |
        Note
      name: Argument name
    - description: |
        "Add description that the user can get this value by running the\
        command \u201Csubmission-list\u201D."
      name: submission_uuid
      required: true
    description: |
      ''
    outputs: []
  - name: 'netcraft-submission-url-list'
    arguments:
    - description: |
        Note
      name: Argument name
    - description: |
        "Add description that the user can get this value by running the\
        command \u201Csubmission-list\u201D."
      name: submission_uuid
      required: true
    - description: |
        Default 50API argument - count
      name: limit
    - description: |
        ''
      name: page
    - description: |
        Default 50API argument - count
      name: page_size
    description: |
      ''
    outputs: []
  - name: 'netcraft-url-screenshot-get'
    arguments:
    - description: |
        Note
      name: Argument name
    - description: |
        "Add description that the user can get this value by running the\
        command \u201Csubmission-list\u201D."
      name: submission_uuid
      required: true
    - description: |
        "Add description that the user can get this value by running the\
        command \u201Csubmission-URL-list\u201D."
      name: url_uuid
      required: true
    - description: |
        "An MD5 hash of the screenshot in question.Add description that the\
        user can get this value by running the command \u201Csubmission-URL-list\u201D\
        ."
      name: screenshot_hash
      required: true
    description: |
      ''
    outputs: []
  script: '-'
  type: python
  dockerimage: demisto/python3:3.10.12.63474
  subtype: python3
  isfetch: true
  ismappable: true
  isremotesyncin: true
  isremotesyncout: true
  runonce: false
fromversion: 6.9.0
tests:
- No tests (deprecated)
defaultmapperin: Netcraft Incoming Mapper
defaultmapperout: Netcraft Outgoing Mapper