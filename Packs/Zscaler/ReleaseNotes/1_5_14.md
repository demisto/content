
#### Modeling Rules

##### Zscaler Internet Access Modeling Rule

- Added mapping of the `appclass` field to *xdm.target.resource.type* for the *zscalernss-web* source type.
- Updated the mapping of *xdm.alert.original_alert_id* for the *zscalernss-web* source type to read the DLP incident identifier from the `dlpincidentnumber` field (falling back to `dlpidentifier`).
- Fixed an issue where *xdm.target.resource_before.value* and *xdm.target.resource.value* were null for the *zscalernss-audit* source type by explicitly extracting the `preaction` and `postaction` JSON objects.
