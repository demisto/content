
#### Modeling Rules

##### Zscaler Internet Access Modeling Rule

- Improved the *zscalernss-web* mapping by adding the `appclass` field to *xdm.target.resource.type* and sourcing the DLP incident identifier for *xdm.alert.original_alert_id* from `dlpincidentnumber`.
- Improved the *zscalernss-audit* mapping to populate *xdm.target.resource_before.value* and *xdm.target.resource.value* from the `preaction` and `postaction` objects.
