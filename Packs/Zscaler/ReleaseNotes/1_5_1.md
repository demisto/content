
#### Integrations

##### Zscaler Internet Access

- Updated the **zscaler-edit-ip-destination-group** command to support the `override` query parameter. When set to `true` (default), the existing IP destination group configuration is replaced with the new values. When set to `false`, the existing configuration is preserved and new entries are appended.
