#### Integrations

##### Zscaler Internet Access

- Added the `override` argument to the **zscaler-edit-ip-destination-group** command. When set to `true` (default), the existing IP destination group configuration is replaced with the new values. When set to `false`, the existing configuration is preserved, and new entries are appended. This prevents unintentional overwriting of existing destination IP group configurations.
