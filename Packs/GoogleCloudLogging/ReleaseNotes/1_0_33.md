
#### Parsing Rules

##### Google Cloud Logging Parsing Rule

- Fixed an issue where the timestamp parsing rule failed validation due to a type mismatch between the `timestamp` and `receiveTimestamp` fields in the `coalesce` function.
