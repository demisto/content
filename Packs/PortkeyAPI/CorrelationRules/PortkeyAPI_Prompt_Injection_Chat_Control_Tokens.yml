name: Portkey - Prompt Injection Using Chat Control Tokens - by GoCortexIO
description: Detects a prompt containing the chat template control tokens a model uses to separate system, user and assistant turns. A user has no legitimate reason to send them, so their presence indicates an attempt to forge a turn boundary and issue instructions as the system.
severity: SEV_040_HIGH
xql_query: |-
  datamodel dataset = portkey_llm_requests_raw
  | alter prompt = lowercase(to_string(xdm.target.resource.value))
  | filter prompt contains "<|im_start|>" or prompt contains "<|im_end|>" or prompt contains "[/inst]"
      or prompt contains "### system:" or prompt contains "### user:" or prompt contains "<<sys>>"
      or prompt contains "<|system|>" or prompt contains "<|start_header_id|>" or prompt contains "</user>"
      or prompt contains "<system>" or prompt contains "</system>"
  | fields xdm.observer.vendor, xdm.observer.product, xdm.observer.action, xdm.observer.name, xdm.event.id, xdm.event.type, xdm.event.tags, xdm.event.original_event_type, xdm.event.operation, xdm.event.outcome, xdm.event.outcome_reason, xdm.event.description, xdm.source.user.username, xdm.source.user.identifier, xdm.target.resource.type, xdm.target.resource.name, xdm.target.resource.sub_type, xdm.target.resource.parent_id, xdm.target.host.hostname, xdm.network.http.url, xdm.network.http.response_code, xdm.*
alert_name: Portkey - Prompt Injection Using Chat Control Tokens - $xdm.source.user.username
alert_category: User Defined
user_defined_category: xdm.alert.category
alert_description: A prompt sent by $xdm.source.user.username to $xdm.target.resource.name contained chat template control tokens, such as an im_start marker or a system tag. These delimit the turns of a conversation, so a user supplying them is attempting to close the user turn and open a forged system turn, making their text look like an instruction from the application rather than input from a person. Review the prompt and confirm whether the model acted on the injected content.
alert_domain: DOMAIN_SECURITY
mitre_defs:
  TA0005 - Defense Evasion:
  - T1027 - Obfuscated Files or Information
suppression_fields:
- xdm.source.user.identifier
- xdm.target.resource.parent_id
global_rule_id: f1c507e3-e1ce-4165-9c25-b408e7a4219a
is_enabled: true
alert_type: null
alert_fields: {}
execution_mode: SCHEDULED
search_window: 2 hours
timezone: Australia/Melbourne
crontab: '*/10 * * * *'
suppression_enabled: true
suppression_duration: 1 hours
dataset: alerts
investigation_query_link: dataset = portkey_llm_requests_raw | filter id = $xdm.event.id
drilldown_query_timeframe: ALERT
mapping_strategy: AUTO
action: ALERTS
fromversion: 8.4.0
