
#### Modeling Rules

##### VMware ESXi Modeling Rule

- Seeded a deliberate GR105 case: the two versions of the rule now overlap on version and declare region-exclusive supportedFeatures (harness_v1 in us, harness_v2 in eu), so the duplicate ID is provably never active in the same region and must pass.
