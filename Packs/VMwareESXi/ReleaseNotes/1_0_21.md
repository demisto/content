
#### Modeling Rules

##### VMware ESXi Modeling Rule

Fixed an issue where ESXi crond process-spawn command-execution logs (*USER \<user\> pid \<pid\> cmd \<command\>*) populated the *outcome* field with an invalid value. A crond start line reports no result, so no outcome is now set for these events.
