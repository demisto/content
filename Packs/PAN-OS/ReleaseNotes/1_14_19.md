
#### Playbooks
##### PAN-OS - Block URL - Custom URL Category
- Fixed a bug where URLs were being added to a custom URL category for blocking, regardless of whether they were already there or not.
##### PAN-OS - Block IP - Static Address Group
- Fixed a bug where IPs were being added to a static address group for blocking, regardless of whether they were already there or not.
