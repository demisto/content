
#### Integrations

##### Palo Alto Networks PAN-OS

- Added the argument **show-detail** to the commands **pan-os**, and **pan-os-query-logs**, which gives the ability to return the full data of the fields *after-change-preview*, and *before-change-preview*, under the fields *after-change-detail*, and *before-change-detail* in the raw response.
