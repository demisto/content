
#### Playbooks

##### New: PAN-OS DAG Configuration

- Replaced the deprecated sub-playbook `PAN-OS Commit Configuration` with the sub-playbook `PAN-OS Commit Configuration v2`.

##### New: PAN-OS - Block URL - Custom URL Category

- Replaced the deprecated sub-playbook `PAN-OS Commit Configuration` with the sub-playbook `PAN-OS Commit Configuration v2`.

##### New: PAN-OS - Block IP - Static Address Group

- Replaced the deprecated sub-playbook `PAN-OS Commit Configuration` with the sub-playbook `PAN-OS Commit Configuration v2`.
