category: Analytics & SIEM
sectionOrder:
- Connect
- Collect
commonfields:
  id: MongoDBAtlasEventCollector
  version: -1
configuration:
- display: Server URL
  name: url
  required: true
  type: 0
  section: Connect
- displaypassword: Public Key
  additionalinfo: The Public Key to use for connection
  name: public_key
  required: true
  hiddenusername: true
  type: 9
  section: Connect
- displaypassword: Private Key
  additionalinfo: The Private Key to use for connection
  name: private_key
  required: true
  hiddenusername: true
  type: 9
  section: Connect
- display: Group ID
  name: group_id
  required: true
  type: 0
  section: Connect
- additionalinfo: Defines the maximum number of alerts or events fetched per type in each fetch cycle.
  defaultvalue: 2500
  display: Maximum number of events per fetch
  name: max_events_per_fetch
  required: true
  type: 0
  section: Collect
- display: Trust any certificate (not secure)
  name: insecure
  type: 8
  required: false
- display: Use system proxy settings
  name: proxy
  type: 8
  required: false
description: 'MongoDBAtlas is integration that supports fetching and managing alerts and events within XSIAM.'
display: MongoDBAtlas
name: MongoDBAtlasEventCollector
script:
  commands:
  - name: mongo-db-atlas-get-events
    description: Retrieves a list of events from the MongoDB Atlas instance.
    arguments:
    - auto: PREDEFINED
      defaultValue: 'false'
      description: If true, the command will create events, otherwise it will only display them.
      name: should_push_events
      predefined:
      - 'true'
      - 'false'
      required: true
    - description: Maximum number of events to return.
      name: limit
      required: true
  runonce: false
  script: '-'
  type: python
  subtype: python3
  isfetchevents: true
  dockerimage: demisto/python3:3.10.14.100715
fromversion: 5.5.0
marketplaces:
- marketplacev2
tests:
- No tests (auto formatted)
