
 # Fetch should test 3 case:
 # 1: There are less events than page_size on first request
 # 2: There are more than page_size events but there are less than max_fetch events
 # 3: There are more than max_fetch events