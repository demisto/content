#### Triggers Recommendations

##### A mail forwarding rule was configured in Google Workspace

- Updated the A mail forwarding rule was configured in Google Workspace triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### A Successful login from TOR

- Updated the A Successful login from TOR triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### A successful SSO sign-in from TOR

- Updated the A successful SSO sign-in from TOR triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-A user created a pfx in a suspicious folder for the first time

- Updated the silent-A user created a pfx in a suspicious folder for the first time triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### A user deleted multiple users for the first time

- Updated the A user deleted multiple users for the first time triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### A user executed multiple LDAP enumeration queries

- Updated the A user executed multiple LDAP enumeration queries triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### AppleScript Process Executed With Rare Command Line

- Updated the AppleScript Process Executed With Rare Command Line triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Authentication method added to an Azure account

- Updated the Authentication method added to an Azure account triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Azure AD account unlock or password reset

- Updated the Azure AD account unlock or password reset triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Azure AD Administrator Role Assignment

- Updated the silent-Azure AD Administrator Role Assignment triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Azure AD PIM role settings change

- Updated the Azure AD PIM role settings change triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Azure Temporary Access Pass (TAP) registered to an account

- Updated the Azure Temporary Access Pass (TAP) registered to an account triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Compromise Accounts - User has rejected numerous SSO MFA attempts

- Updated the Compromise Accounts - User has rejected numerous SSO MFA attempts triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Credential Dumping using a known tool

- Updated the Credential Dumping using a known tool triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### CVE-2025-31324 - SAP NetWeaver Visual Composer

- Updated the CVE-2025-31324 - SAP NetWeaver Visual Composer triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### CVE-2025-49704 and CVE-2025-49706 and CVE-2025-53770 and CVE-2025-53771 - Microsoft SharePoint ToolShell vulnerability chain

- Updated the CVE-2025-49704 and CVE-2025-49706 and CVE-2025-53770 and CVE-2025-53771 - Microsoft SharePoint ToolShell vulnerability chain triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### CVE-2025-59287 - Microsoft WSUS Remote Code Execution

- Updated the CVE-2025-59287 - Microsoft WSUS Remote Code Execution triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Endpoint initiated uncommon remote scheduled task creation

- Updated the Endpoint initiated uncommon remote scheduled task creation triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Event Log Was Cleared

- Updated the Event Log Was Cleared triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Excessive User Account Lockouts

- Updated the Excessive User Account Lockouts triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Exchange forwarding rule configured

- Updated the Exchange forwarding rule configured triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Exchange User Mailbox Forwarding

- Updated the Exchange User Mailbox Forwarding triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Execution of an uncommon process at an early startup stage

- Updated the Execution of an uncommon process at an early startup stage triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Execution of an uncommon process at an early startup stage by Windows system binary

- Updated the silent-Execution of an uncommon process at an early startup stage by Windows system binary triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### External Login Password Spray

- Updated the External Login Password Spray triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### First Azure AD PowerShell operation for a user

- Updated the First Azure AD PowerShell operation for a user triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-First seen Azure admin consent to an application

- Updated the silent-First seen Azure admin consent to an application triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Impossible Traveler SSO

- Updated the silent-Impossible Traveler SSO triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Intense SSO failures with suspicious characteristics

- Updated the Intense SSO failures with suspicious characteristics triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### MFA was disabled for an Azure identity

- Updated the MFA was disabled for an Azure identity triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Msiexec execution of an executable from an uncommon remote location

- Updated the Msiexec execution of an executable from an uncommon remote location triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Multiple discovery commands on a Windows host

- Updated the silent-Multiple discovery commands on a Windows host triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Netcat Makes or Gets Connections

- Updated the Netcat Makes or Gets Connections triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-NTDS.dit file written by an uncommon executable

- Updated the silent-NTDS.dit file written by an uncommon executable triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Office process creates a scheduled task via file access

- Updated the Office process creates a scheduled task via file access triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Process attempts to kill a known security or AV tool

- Updated the silent-Process attempts to kill a known security or AV tool triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Recurring rare domain access from an unsigned process

- Updated the silent-Recurring rare domain access from an unsigned process triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Remote WMI Process Execution

- Updated the Remote WMI Process Execution triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Rundll32.exe executes a rare unsigned module

- Updated the silent-Rundll32.exe executes a rare unsigned module triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-SaaS File External Access

- Updated the silent-SaaS File External Access triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Scheduled task created with HTTP or FTP reference

- Updated the Scheduled task created with HTTP or FTP reference triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Script file added to startup-related Registry keys

- Updated the Script file added to startup-related Registry keys triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### SPNs cleared from a machine account

- Updated the SPNs cleared from a machine account triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### SSO Authentication With Suspicious Characteristics

- Updated the SSO Authentication With Suspicious Characteristics triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### SSO Brute Force

- Updated the SSO Brute Force triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### SSO Brute Force Activity

- Updated the SSO Brute Force Activity triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### SSO Password Spray

- Updated the SSO Password Spray triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Successful guest user invitation

- Updated the Successful guest user invitation triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Uncommon creation or access operation of sensitive shadow copy by a high-risk process

- Updated the Uncommon creation or access operation of sensitive shadow copy by a high-risk process triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Suspicious certutil command line

- Updated the Suspicious certutil command line triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Suspicious Conditional Access operation for an identity

- Updated the Suspicious Conditional Access operation for an identity triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Suspicious execution from tmp folder

- Updated the Suspicious execution from tmp folder triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Alert Trigger - Suspicious Hidden User Created

- Updated the Alert Trigger - Suspicious Hidden User Created triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Suspicious LDAP search query

- Updated the Suspicious LDAP search query triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Suspicious local administrator login

- Updated the Suspicious local administrator login triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Suspicious local user account creation

- Updated the silent-Suspicious local user account creation triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Suspicious Port Scan

- Updated the silent-Suspicious Port Scan triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Suspicious Powershell Command Execution

- Updated the silent-Suspicious Powershell Command Execution triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Suspicious process execution by scheduled task on a sensitive server

- Updated the Suspicious process execution by scheduled task on a sensitive server triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Suspicious process spawns MSBuild

- Updated the silent-Suspicious process spawns MSBuild triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Suspicious SaaS Access From a TOR Exit Node

- Updated the Suspicious SaaS Access From a TOR Exit Node triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Suspicious User access to multiple resources via SSO

- Updated the silent-Suspicious User access to multiple resources via SSO triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Uncommon execution of ODBCConf

- Updated the Uncommon execution of ODBCConf triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Uncommon remote scheduled task created

- Updated the Uncommon remote scheduled task created triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Unprivileged process opened a registry hive

- Updated the Unprivileged process opened a registry hive triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Unsigned and unpopular process performed an injection

- Updated the Unsigned and unpopular process performed an injection triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### Unusual process accessed web browser credentials and executed by a terminal process

- Updated the Unusual process accessed web browser credentials and executed by a terminal process triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### User added to local administrator group using a PowerShell command

- Updated the User added to local administrator group using a PowerShell command triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-User attempted to connect from a suspicious country

- Updated the silent-User attempted to connect from a suspicious country triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-User moved Exchange sent messages to deleted items

- Updated the silent-User moved Exchange sent messages to deleted items triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-Windows LOLBIN scripting engine connected to a rare external host

- Updated the silent-Windows LOLBIN scripting engine connected to a rare external host triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.

##### silent-WmiPrvSe.exe Rare Child Command Line

- Updated the silent-WmiPrvSe.exe Rare Child Command Line triggers recommendation to add the new id and name fields to the JSON payload, alongside the existing trigger_id and trigger_name.
