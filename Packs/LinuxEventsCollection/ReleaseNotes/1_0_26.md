#### Parsing Rules
##### Linux Events Collection Parsing Rule
Fixed an issue where the parsing rule failed to set **_time** for syslog logs containing an ISO 8601 timestamp that uses a **T** date/time separator together with a named timezone (for example `UTC`), which caused a fallback to **_insert_time**.
