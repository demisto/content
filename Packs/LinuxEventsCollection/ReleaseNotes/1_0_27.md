#### Parsing Rules

##### Linux Events Collection Parsing Rule

- Restricted ingestion to syslog records delivered under the *_raw_log* field so that only the supported Linux syslog format is stored in the dataset.
