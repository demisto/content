
#### Integrations

##### Atlassian Jira Service Management

- Fixed an issue where the ***jira-asset-object-search*** command failed with `KeyError: 'objectEntries'` when the API returned a response without the `objectEntries` key (e.g., on a 404 error). The command now returns a "No objects found." message instead of raising an unhandled exception.
