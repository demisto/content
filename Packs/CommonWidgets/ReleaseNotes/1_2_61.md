#### Widgets

##### Page Break Widget

- Changed the ***dataType*** from ***incidents*** to ***scripts*** to prevent unnecessary database queries against the incidents table when the widget is rendered.

##### Text Widget

- Changed `dataType` from `incidents` to `scripts` to prevent unnecessary database queries against the incidents table when the widget is rendered.
