comment: A no-op placeholder script used by visual-only widgets (e.g. Page Break, Text Widget) that require a script dataType but do not need to query any data source. Returns a mock result to prevent unnecessary database queries.
commonfields:
  id: WidgetNoOp
  version: -1
name: WidgetNoOp
script: '-'
tags:
- widget
timeout: '0'
type: python
subtype: python3
dockerimage: demisto/python3:3.12.12.6204436
fromversion: 6.1.0
tests:
- No test
