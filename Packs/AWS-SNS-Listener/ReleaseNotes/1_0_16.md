#### Integrations

##### AWS-SNS-Listener
- Added a new configuration parameter **Process notifications and fetch in parallel** (default disabled). When enabled, incoming SNS notifications are acknowledged with HTTP 200 immediately and incident creation runs in the background, satisfying the AWS SNS 15-second delivery SLA during high-volume bursts and preventing duplicate incidents from AWS retries. When disabled, the integration retains the previous behavior.
