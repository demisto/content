
#### Scripts

##### CoreXQLApiModule

- Fixed an issue where fields containing the substring "time" (such as `reopenedbuffertime`) were incorrectly converted to timestamps. Now only actual timestamp fields (ending with `_time`, `timestamp`, or exactly named `time`) are converted to date format.
