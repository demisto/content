
#### Scripts

##### CoreXQLApiModule

- Fixed an issue where the *xdr-xql-generic-query* command silently succeeded when an XQL query failed, instead of returning an error.
- Fixed an issue where the *xdr-xql-generic-query* command entered an infinite loop when all XQL query workers were busy. The retry mechanism now properly respects the timeout by staying within the same polling chain.