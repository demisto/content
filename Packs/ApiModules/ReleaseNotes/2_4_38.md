
#### Scripts

##### NGINXApiModule

- Fixed an issue where a large, continuously-polled list could be served from a stale cache indefinitely, so newly-added items were not reflected in the served response. Concurrent cache-building requests now converge to the freshly-built response, and a rejected concurrent build falls back to serving the last-known-good cached response instead of returning an error.
