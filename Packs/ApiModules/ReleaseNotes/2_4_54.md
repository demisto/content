#### Scripts

##### FormatURLApiModule

- Fixed an issue where percent-encoded values were decoded repeatedly, causing encoded text such as *%2520* to be resolved into characters that were not present in the original URL.
- Fixed an issue where a URL containing an encoded newline (*%0D%0A*) was split across several lines, resulting in malformed extracted indicators.
