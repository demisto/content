
#### Scripts

##### ContentClientApiModule

- Fixed an issue where HTTP redirect responses (301, 302, 307, 308) caused authentication failures in integrations using ContentClient with async operations. The httpx AsyncClient now follows redirects by default, resolving issues with CrowdStrike Falcon Spotlight asset collection and other integrations that may encounter OAuth endpoint redirects.
