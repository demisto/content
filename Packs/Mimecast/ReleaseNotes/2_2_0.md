
#### Integrations

##### Mimecast v2

When calling **mimecast-query**, fields specified under `<return-fields>` now appear in context and readable output, alongside the default fields that were shown until now.
