
#### Integrations

##### Microsoft Defender for Cloud Apps Event Collector

- Fixed an issue where events from low-volume event types could be dropped and ingestion could fall behind when another event type had a large backlog.
- Improved fault isolation so that a failure while fetching one event type no longer stops the other event types from being collected.
