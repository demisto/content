
#### Integrations

##### Splunk

- Fixed an issue where fetching incidents failed to parse drilldown search objects when placeholder values contained unescaped backslashes (for example, `object="PREFIX\Admin"`), causing an `Invalid \escape` JSON parsing error.

##### SplunkPy v2

- Fixed an issue where fetching incidents failed to parse drilldown search objects when placeholder values contained unescaped backslashes (for example, `object="PREFIX\Admin"`), causing an `Invalid \escape` JSON parsing error.
