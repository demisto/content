
#### Integrations

##### GitHub

- Added support for **GitHub-revoke-credentials** command that deprecated. use ***githubrevokecredentials*** instead. revoke exposed github credentials (tokens) via the github credential revocation api. this endpoint is unauthenticated by design.
- Added support for **github-revoke-credentials** command that revoke exposed github credentials (tokens) via the github credential revocation api. this endpoint is unauthenticated by design and does not use the configured token. ratelimited to 60 unauthenticated requests per hour.
