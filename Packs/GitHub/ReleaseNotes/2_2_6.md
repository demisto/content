
#### Integrations

##### GitHub

- Added support for the **github-revoke-credentials** command, which revokes exposed GitHub credentials (tokens) via the GitHub credential revocation API; this unauthenticated endpoint does not use the configured token and is rate-limited to 60 unauthenticated requests per hour.
