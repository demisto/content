
#### Integrations

##### CyberArk Identity Security Platform

- Improved the performance of the **fetch-events** flow. Audit events are now streamed to XSIAM page-by-page on a background thread pool, so the upload of one page overlaps with the fetch of the next page instead of collecting all events in memory and sending them in a single blocking request at the end of the cycle.
