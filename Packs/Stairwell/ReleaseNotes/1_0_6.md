#### Integrations

##### Stairwell

- **Improved Context Output Management**: Added `outputs_key_field` to all CommandResults for better context deduplication and data tracking across file, network intel, and object commands.
- **Fixed YML Configuration**:
  - Updated `sha256` parameter in `stairwell-intake-upload` command from `required: true` to `required: false` to match code behavior (auto-calculates if not provided).
  - Corrected context path for intake upload from `Stairwell.Intake_Upload` to `Stairwell.Intake` for consistency.
- **Enhanced test_module() Validation**: Improved connection test logic to properly validate API responses instead of string matching.
- **Code Quality Improvements**:
  - Removed trailing comma in `get_file_variants()` function signature.
  - Added key field tracking (objectId, ip, hostname, asn, sha256, hash) to all command responses for improved context management.
- **Documentation Updates**:
  - Updated TEST_COVERAGE.md to reflect all 26 commands with comprehensive test coverage.
  - Added dev-requirements.txt for local development and testing.
- **Version Synchronization**: Aligned pack metadata version with marketplace listing requirements.
