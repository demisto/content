
#### Modeling Rules

##### AWS WAF Modeling Rule

- Seeded a deliberate ST115 negative case: the pack allows harness_v1 and harness_v2 while the rule declares only harness_v1, a valid subset, so no error is expected.
