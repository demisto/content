
#### Integrations

##### Tenable Vulnerability Management (formerly Tenable.io)

- Fixed an issue where the assets snapshot was never sealed with the correct item count, causing assets to not appear in the aggregated assets view. The vulnerability fetch handler was prematurely clearing the snapshot state (`snapshot_id` and `total_assets`) before the snapshot could be properly sealed.
