#### Integrations
##### SysAid
- Changed the request from `GET` to `POST` and the endpoint from `sr/template` to `sr` in the command `service_record_create_request`.
- Added the command `sysaid-service-record-get-file` for download the file in the War Room and in the context.