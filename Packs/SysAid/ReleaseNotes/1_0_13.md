#### Integrations
##### SysAid
- Updated the request from `GET` to `POST` and the endpoint from `sr/template` to `sr` in the command ***service_record_create_request***.
- Added the command ***sysaid-service-record-get-file*** for downloading the file in the War Room and in the context.
- Updated the Docker image to: *demisto/python3:3.10.13.80014*.