category: Data Enrichment & Threat Intelligence
commonfields:
  id: ReliaQuestGreyMatterDRPEventCollector
  version: -1
configuration:
- display: Server URL
  name: url
  required: true
  type: 0
  defaultvalue: https://api.searchlight.app
- display: Username
  name: credentials
  type: 9
  required: true
  section: Connect
- display: Account ID
  name: account_id
  type: 0
  required: true
  section: Connect
- display: Maximum number of events per fetch
  name: max_fetch_events
  type: 0
  defaultvalue: 200
  required: true
  section: Collect
- defaultvalue: 3 days
  display: First fetch timestamp (<number> <time unit>, e.g., 12 hours, 7 days)
  name: first_fetch
  type: 0
  section: Collect
  required: false
- display: Trust any certificate (not secure)
  name: insecure
  type: 8
  required: false
- display: Use system proxy settings
  name: proxy
  type: 8
  required: false
description: Digital Shadows monitors and manages an organization's digital risk across the widest range of data sources within the open, deep, and dark web.
display: ReliaQuestGreyMatterDRPEventCollector
name: ReliaQuestGreyMatterDRPEventCollector
script:
  commands:
  - arguments:
    - name: event_type
      description: 'The type of events to retrieve'
      required: true
      auto: PREDEFINED
      predefined:
        - 'incident'
        - 'alert'
        - 'all'
      defaultValue: 'all'
    - name: start_time
      description: 'From which time to get the events'
      required: false
      defaultValue: '3 days ago'
    - name: end_time
      description: 'Until which time to get the events'
      required: false
    - name: limit
      description: 'The maximum number of events to retrieve'
      required: false
      defaultValue: '200'
    description: Manual command to fetch events and display them.
    name: reila-quest-get-events
    outputs:  # add the outputs here
    - contextPath: BaseIntegration.Output
      description: '[Enter a description of the data returned in this output.]'
      type: String
  runonce: false
  script: '-'
  type: python
  subtype: python3
  dockerimage: demisto/python3:3.10.13.83255
  isfetchevents: true
fromversion: 5.5.0
tests:
- No tests (auto formatted)
