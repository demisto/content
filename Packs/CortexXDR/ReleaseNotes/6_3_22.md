
#### Integrations

##### Cortex XDR - IR

- Improved the error messages returned by the ***xdr-endpoint-scan*** and ***xdr-endpoint-scan-abort*** commands. Invalid *endpoint_id_list* values (for example, a hostname or a truncated ID) are now rejected with a clear message, and failures to create a scan action now return a specific reason instead of a generic server-side error.
