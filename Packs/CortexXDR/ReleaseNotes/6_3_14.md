#### Integrations

##### Cortex XDR - XQL Query Engine

- Fixed an issue where commands would fail due to incorrect URL building and unnecessary authentication headers when running in XSIAM/platform.

##### Palo Alto Networks Cortex XDR - Investigation and Response

- Fixed an issue where commands would fail due to incorrect URL building and unnecessary authentication headers when running in XSIAM/platform.
- Fixed an issue where, for some tenants, the connectivity test would fail even though connectivity was successful.
