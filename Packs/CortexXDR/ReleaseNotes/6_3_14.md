#### Integrations

##### Palo Alto Networks Cortex XDR - Investigation and Response

- Fixed an issue where commands would fail due to incorrect URL building and unnecessary authentication headers when running in XSIAM/platform RBAC-forwarding environments.
- Fixed an issue where the connectivity test would fail even though connectivity was successful.
