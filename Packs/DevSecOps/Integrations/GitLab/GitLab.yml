category: Utilities
commonfields:
  id: GitLab
  version: -1
configuration:
- defaultvalue: https://gitlab.com/api/v4
  display: Server URL (e.g. https://gitlab.com/api/v4)
  name: url
  required: true
  type: 0
- display: API Key
  name: api_key
  required: true
  type: 4
- display: Trust any certificate (not secure)
  name: insecure
  required: false
  type: 8
- display: Use system proxy settings
  name: proxy
  required: false
  type: 8
description: An Integration with GitLab
display: GitLab
name: GitLab
script:
  commands:
  - arguments:
    - default: false
      description: Limit results to projects stored on repository_storage. Available
        for admins only.
      isArray: false
      name: repository_storage
      required: false
      secret: false
    - default: false
      description: Limit results to projects with last_activity before specified time.
      isArray: false
      name: last_activity_before
      required: false
      secret: false
    - default: false
      description: Limit by current user minimal access level.
      isArray: false
      name: min_access_level
      required: false
      secret: false
    - default: false
      description: Return only limited fields for each project. This is a no-op without
        authentication as then only simple fields are returned.
      isArray: false
      name: simple
      required: false
      secret: false
    - default: false
      description: Return projects sorted in asc or desc order.
      isArray: false
      name: sort
      required: false
      secret: false
    - default: false
      description: Limit by projects that the current user is a member of.
      isArray: false
      name: membership
      required: false
      secret: false
    - default: false
      description: Include ancestor namespaces when matching search criteria.
      isArray: false
      name: search_namespaces
      required: false
      secret: false
    - default: false
      description: Limit by archived status.
      isArray: false
      name: archived
      required: false
      secret: false
    - default: false
      description: Return list of projects matching the search criteria.
      isArray: false
      name: search
      required: false
      secret: false
    - default: false
      description: Limit results to projects with IDs less than the specified ID.
      isArray: false
      name: id_before
      required: false
      secret: false
    - default: false
      description: Limit results to projects with last_activity after specified time
      isArray: false
      name: last_activity_after
      required: false
      secret: false
    - default: false
      description: Limit by projects starred by the current user .
      isArray: false
      name: starred
      required: false
      secret: false
    - default: false
      description: Limit results to projects with IDs greater than the specified ID.
      isArray: false
      name: id_after
      required: false
      secret: false
    - default: false
      description: Limit by projects explicitly owned by the current user.
      isArray: false
      name: owned
      required: false
      secret: false
    - default: false
      description: Return projects ordered by id, name, path, created_at, updated_at,
        or last_activity_at fields. repository_size, storage_size, or wiki_size fields
        are only allowed for admins. Default is created_at.
      isArray: false
      name: order_by
      required: false
      secret: false
    - default: false
      description: Include project statistics.
      isArray: false
      name: statistics
      required: false
      secret: false
    - default: false
      description: Limit by visibility public, internal, or private.
      isArray: false
      name: visibility
      required: false
      secret: false
    - default: false
      description: Include custom attributes in response (admins only).
      isArray: false
      name: with_custom_attributes
      required: false
      secret: false
    - default: false
      description: Limit by enabled issues feature.
      isArray: false
      name: with_issues_enabled
      required: false
      secret: false
    - default: false
      description: Limit by enabled merge requests feature.
      isArray: false
      name: with_merge_requests_enabled
      required: false
      secret: false
    - default: false
      description: Limit by projects which use the given programming language.
      isArray: false
      name: with_programming_language
      required: false
      secret: false
    deprecated: false
    description: Get a list of all visible projects across GitLab for the authenticated
      user. When accessed without authentication, only public projects with “simple”
      fields are returned.
    execution: false
    name: gitlab-get-projects
    outputs:
    - contextPath: GitLab.Projects.web_url
      description: Project Web URL.
      type: Unknown
    - contextPath: GitLab.Projects.name
      description: Project Name.
      type: Unknown
    - contextPath: GitLab.Projects.path
      description: Project Path.
      type: Unknown
    - contextPath: GitLab.Projects.created_at
      description: Project Created At.
      type: Unknown
    - contextPath: GitLab.Projects.visibility
      description: Project Visiblity.
      type: Unknown
    - contextPath: GitLab.Projects.id
      description: Project ID.
      type: Unknown
  - arguments:
    - default: false
      description: The ID or URL-encoded path of the project owned by the authenticated
        user.
      isArray: false
      name: id
      required: true
      secret: false
    deprecated: false
    description: Gets a list of access requests viewable by the authenticated user.
    execution: false
    name: gitlab-projects-get-access-requests
    outputs:
    - contextPath: GitLab.AccessRequests.id
      description: Access Request ID.
      type: Unknown
    - contextPath: GitLab.AccessRequests.username
      description: Access Request User.
      type: Unknown
    - contextPath: GitLab.AccessRequests.requested_at
      description: Access Request Create Time.
      type: Unknown
    - contextPath: GitLab.AccessRequests.state
      description: Access Request State.
      type: Unknown
  - arguments:
    - default: false
      description: The ID or URL-encoded path of the project owned by the authenticated
        user.
      isArray: false
      name: id
      required: true
      secret: false
    deprecated: false
    description: Requests access for the authenticated user to a group or project.
    execution: false
    name: gitlab-projects-request-access
    outputs:
    - contextPath: GitLab.AccessRequests.id
      description: Access Request ID.
      type: Unknown
    - contextPath: GitLab.AccessRequests.username
      description: Access Request User.
      type: Unknown
    - contextPath: GitLab.AccessRequests.requested_at
      description: Access Request Create Time.
      type: Unknown
    - contextPath: GitLab.AccessRequests.state
      description: Access Request State.
      type: Unknown
  - arguments:
    - default: false
      description: The ID or URL-encoded path of the project owned by the authenticated
        user.
      isArray: false
      name: id
      required: true
      secret: false
    - default: false
      description: The user ID of the access requester.
      isArray: false
      name: user_id
      required: true
      secret: false
    - default: false
      description: 'A valid access level (defaults: 30, developer access level)'
      isArray: false
      name: access_level
      required: false
      secret: false
    deprecated: false
    description: Approves an access request for the given user.
    execution: false
    name: gitlab-projects-approve-access
    outputs:
    - contextPath: GitLab.AccessRequests.id
      description: Access Request ID
      type: Unknown
    - contextPath: GitLab.AccessRequests.username
      description: Access Request User
      type: Unknown
    - contextPath: GitLab.AccessRequests.requested_at
      description: Access Request Create Time
      type: Unknown
    - contextPath: GitLab.AccessRequests.state
      description: Access Request State
      type: Unknown
  - arguments:
    - default: false
      description: The ID or URL-encoded path of the project owned by the authenticated
        user.
      isArray: false
      name: id
      required: true
      secret: false
    - default: false
      description: The user ID of the access requester.
      isArray: false
      name: user_id
      required: true
      secret: false
    deprecated: false
    description: Denies an access request for the given user.
    execution: false
    name: gitlab-projects-deny-access
    outputs:
    - contextPath: GitLab.AccessRequests.id
      description: Access Request ID
      type: Unknown
    - contextPath: GitLab.AccessRequests.state
      description: Access Request State
      type: Unknown
  - arguments:
    - default: false
      description: The ID or URL-encoded path of the project owned by the authenticated
        user.
      isArray: false
      name: id
      required: true
      secret: false
    - default: false
      description: Return list of branches containing the search string. You can use
        ^term and term$ to find  branches that begin and end with term respectively.
      isArray: false
      name: search
      required: false
      secret: false
    deprecated: false
    description: Get a list of repository  branches from a project, sorted by name
      alphabetically.
    execution: false
    name: gitlab-projects-get-repository-branches
    outputs:
    - contextPath: GitLab.Branches.name
      description: Branch Name
      type: Unknown
    - contextPath: GitLab.Branches.web_url
      description: Branch Web URL
      type: Unknown
    - contextPath: GitLab.Branches.commit.id
      description: Branch Head Commit ID
      type: Unknown
  - arguments:
    - default: false
      description: The ID or URL-encoded path of the project owned by the authenticated
        user.
      isArray: false
      name: id
      required: true
      secret: false
    - default: false
      description: The name of the branch.
      isArray: false
      name: branch
      required: true
      secret: false
    - default: false
      description: Branch name or commit SHA to create branch from.
      isArray: false
      name: ref
      required: true
      secret: false
    deprecated: false
    description: Create a new Branch in the repository.
    execution: false
    name: gitlab-projects-create-repository-branch
    outputs:
    - contextPath: GitLab.Branches.name
      description: Branch Name
      type: Unknown
    - contextPath: GitLab.Branches.web_url
      description: Branch Web URL
      type: Unknown
    - contextPath: GitLab.Branches.commit.id
      description: Branch Head Commit ID
      type: Unknown
  - arguments:
    - default: false
      description: The ID or URL-encoded path of the project owned by the authenticated
        user.
      isArray: false
      name: id
      required: true
      secret: false
    - default: false
      description: The name of the branch.
      isArray: false
      name: branch
      required: true
      secret: false
    deprecated: false
    description: Delete a Branch from the repository.
    execution: false
    name: gitlab-projects-delete-repository-branch
  - arguments:
    - default: false
      description: The ID or URL-encoded path of the project owned by the authenticated
        user.
      isArray: false
      name: id
      required: true
      secret: false
    deprecated: false
    description: Will delete all branches that are merged into the project’s default branch.
    execution: false
    name: gitlab-projects-delete-repository-merged-branches
  - deprecated: false
    description: Retrieve version information for this GitLab instance. Responds 200
      OK for authenticated users.
    execution: false
    name: gitlab-get-version
    outputs:
    - contextPath: GitLab.version
      description: GitLab Verion
      type: String
    - contextPath: GitLab.revision
      description: GitLab Revision
      type: String
  - arguments:
    - default: false
      description: Project ID to retrieve pipeline schedules from.
      isArray: false
      name: project_id
      required: true
      secret: false
    - default: false
      description: ID of specific pipeline schedule to retrieve its details.
      isArray: false
      name: pipeline_schedule_id
      required: false
      secret: false
    deprecated: false
    description: Get details of pipeline schedules.
    execution: false
    name: gitlab-pipelines-schedules-list
    outputs:
    - contextPath: GitLab.PipelineSchedule.id
      description: Pipeline schedule ID.
      type: Number
    - contextPath: GitLab.PipelineSchedule.description
      description: Pipeline schedule description.
      type: String
    - contextPath: GitLab.PipelineSchedule.ref
      description: Pipeline schedule ref.
      type: String
    - contextPath: GitLab.PipelineSchedule.next_run_at
      description: Pipeline schedule next run scheduled time.
      type: Date
    - contextPath: GitLab.PipelineSchedule.active
      description: Whether pipeline schedule is active.
      type: Boolean
    - contextPath: GitLab.PipelineSchedule.created_at
      description: When pipeline schedule was created.
      type: Date
    - contextPath: GitLab.PipelineSchedule.updated_at
      description: When pipeline schedule was last updated.
      type: Date
    - contextPath: GitLab.PipelineSchedule.last_pipeline.id
      description: ID of the last pipe line that was run by the scheduled pipeline.
        Relevant only when given pipeline schedule ID is given.
      type: Number
    - contextPath: GitLab.PipelineSchedule.last_pipeline.sha
      description: SHA of the last pipe line that was run by the scheduled pipeline.
        Relevant only when given pipeline schedule ID is given.
      type: String
    - contextPath: GitLab.PipelineSchedule.last_pipeline.ref
      description: Ref of the last pipe line that was run by the scheduled pipeline.
        Relevant only when given pipeline schedule ID is given.
      type: String
    - contextPath: GitLab.PipelineSchedule.last_pipeline.status
      description: Status of the last pipe line that was run by the scheduled pipeline.
        Relevant only when given pipeline schedule ID is given.
      type: String
  - arguments:
    - default: false
      description: Project ID to retrieve pipelines from.
      isArray: false
      name: project_id
      required: true
      secret: false
    - default: false
      description: ID of specific pipeline to retrieve its details.
      isArray: false
      name: pipeline_id
      required: false
      secret: false
    deprecated: false
    description: Get details of pipelines.
    execution: false
    name: gitlab-pipelines-list
    outputs:
    - contextPath: GitLab.Pipeline.id
      description: Pipeline ID.
      type: Number
    - contextPath: GitLab.Pipeline.project_id
      description: Project ID the pipeline belongs to.
      type: Number
    - contextPath: GitLab.Pipeline.status
      description: Status of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.ref
      description: Ref of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.sha
      description: SHA of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.created_at
      description: Time when pipeline was created.
      type: Date
    - contextPath: GitLab.Pipeline.updated_at
      description: Time when pipeline was last updated.
      type: Date
    - contextPath: GitLab.Pipeline.started_at
      description: Time when pipeline was started.
      type: Date
    - contextPath: GitLab.Pipeline.finished_at
      description: Time when pipeline was finished.
      type: Date
    - contextPath: GitLab.Pipeline.duration
      description: Duration of the pipeline.
      type: Number
    - contextPath: GitLab.Pipeline.web_url
      description: Web URL of the pipeline.
      type: String
  - arguments:
    - default: false
      description: Project ID to retrieve jobs details from.
      isArray: false
      name: project_id
      required: true
      secret: false
    - default: false
      description: ID of the pipeline to retrieve its jobs.
      isArray: false
      name: pipeline_id
      required: true
      secret: false
    deprecated: false
    description: Get details of jobs.
    execution: false
    name: gitlab-jobs-list
    outputs:
    - contextPath: GitLab.Job.created_at
      description: Time job was created.
      type: Date
    - contextPath: GitLab.Job.started_at
      description: Time job was started.
      type: Date
    - contextPath: GitLab.Job.finished_at
      description: Time job was finished.
      type: Date
    - contextPath: GitLab.Job.duration
      description: Duration of the job.
      type: Number
    - contextPath: GitLab.Job.id
      description: ID of the job.
      type: Number
    - contextPath: GitLab.Job.name
      description: Name of the job.
      type: String
    - contextPath: GitLab.Job.pipeline.id
      description: Pipeline job belongs to.
      type: Number
    - contextPath: GitLab.Job.pipeline.project_id
      description: Project ID job belongs to.
      type: Number
    - contextPath: GitLab.Job.pipeline.ref
      description: Ref of the pipeline job belongs to.
      type: String
    - contextPath: GitLab.Job.pipeline.sha
      description: Sha of the Pipeline job belongs to.
      type: String
    - contextPath: GitLab.Job.pipeline.status
      description: Status of the Pipeline job belongs to.
      type: String
    - contextPath: GitLab.Job.ref
      description: Ref name of the job
      type: String
    - contextPath: GitLab.Job.stage
      description: Stage of the job.
      type: String
    - contextPath: GitLab.Job.web_url
      description: Web URL of the job.
      type: String
  - arguments:
    - default: false
      description: Project ID to retrieve artifact from.
      isArray: false
      name: project_id
      required: true
      secret: false
    - default: false
      description: ID of specific job to retrieve its artifact.
      isArray: false
      name: job_id
      required: true
      secret: false
    - default: false
      description: Suffix to the path of the artifact to retrieve its data.
      isArray: false
      name: artifact_path_suffix
      required: true
      secret: false
    deprecated: false
    description: Get artifcat from given artifact path corresponding to job ID given.
    execution: false
    name: gitlab-artifact-get
    outputs:
    - contextPath: GitLab.Artifact.job_id
      description: Job ID from whom artifact was taken.
      type: String
    - contextPath: GitLab.Artifact.artifact_path_suffix
      description: Suffix of the artifact path given.
      type: String
    - contextPath: GitLab.Artifact.artifact_data
      description: Data of the artifact requested.
      type: String
  dockerimage: demisto/python3:3.9.5.21272
  feed: false
  isfetch: false
  longRunning: false
  longRunningPort: false
  runonce: false
  script: '-'
  subtype: python3
  type: python
tests:
- No tests (auto formatted)
fromversion: 6.0.0
