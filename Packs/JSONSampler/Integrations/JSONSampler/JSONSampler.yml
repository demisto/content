category: Utilities
commonfields:
  id: JSONSampler
  version: -1
configuration:
- display: Fetch incidents
  name: isFetch
  required: false
  type: 8
- display: Incident type
  name: incidentType
  required: false
  type: 13
- defaultvalue: "1"
  display: Incidents Fetch Interval
  name: incidentFetchInterval
  required: false
  type: 19
- display: The raw JSON string to use as the sample data
  name: JSON
  required: true
  type: 12
- display: The incident name to give to the created incident
  name: name
  required: false
  type: 0
description: |-
  This is a JSON Sampler.  Save a string of a JSON object into the integration and then fetch or simulate running the sampler command to receive the response.

  Currently supports only single JSON object as sample.
display: JSONSampler
name: JSONSampler
script:
  commands:
  - arguments:
    - description: The key to change.  Must also set value arguement.  Can be comma
        separated to change multiple values.
      name: key
    - description: The new key value. Must also set key argument. Can be comma separated
        to support changing multiple values.
      name: value
    description: Sample the provided JSON and return the results to the Context and
      Warroom.  Can use key and value arg to change a JSON values if desired.
    name: sampler
  dockerimage: demisto/python3:3.8.3.9324
  isfetch: true
  runonce: false
  script: ''
  subtype: python3
  type: python
fromversion: 5.5.0
tests:
- No tests (auto formatted)
