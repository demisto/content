
#### Integrations

##### Github Feed

Fixed an issue where IPv4 and IPv6 CIDR indicators with multi-digit prefix lengths (e.g., */16*, */24*, */128*) were incorrectly ingested with a truncated single-digit prefix length (e.g., */1*, */2*), causing false CIDR indicators to appear in the Indicators page.
