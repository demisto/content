
#### Github Feed

- Fixed an issue where IPv4 CIDR indicators with multi-digit prefix lengths (e.g. `/12`, `/16`, `/24`) were incorrectly truncated to a single-digit prefix (e.g. `/1`, `/1`, `/2`) due to ordered alternation in the `ipv4cidrRegex` pattern.
