commonfields:
  id: quarantine-file
  version: -1
args:
- name: endpoint_ids
  description: 'A comma-separated list of endpoint IDs.'
  required: true
  isArray: true
  type: List
- name: file_hash
  description: 'The hash of the file to quarantine. Supported types are: SHA1, SHA256.'
  required: true
  type: String
- name: file_path
  description: 'The path of the file to quarantine.'
  required: true
  type: String
- name: timeout
  description: 'The polling timeout in seconds for the quarantine commands.'
  required: false
  type: Integer
- name: quarantine_brands
  description: |-
Integrations to execute the 'quarantine-file' command. If not specified, the integration will be automatically selected based on the file hash type and the available configured instances.
To specify multiple integrations, provide a comma-separated list of integration brand IDs (e.g., "Palo Alto Networks Cortex XDR - Investigation and Response, Cortex Core - IR").
  isArray: true
- name: verbose
  description: Whether to retrieve a human-readable entry for every command. When set to false, human-readable will only summarize the final result.
  auto: PREDEFINED
  predefined:
  - 'true'
  - 'false'
  defaultValue: 'false'
comment: This script executes the 'quarantine-file' command on a specified file via the appropriate agent. This script is used to isolate files identified as suspicious.
name: quarantine-file
outputs:
- contextPath: QuarantineFile.FilePath.
  description: The path of the quarantined file.
  type: String
- contextPath: QuarantineFile.FileHash.
  description: Hash of the file.
  type: String
- contextPath: QuarantineFile.Status
  description: whether the command was running succussefully or not.
  type: Boolean
- contextPath: QuarantineFile.Result
  description: whether the command was running succussefully or not.
  type: String
- contextPath: QuarantineFile.Message
  description: If the command run successfully - message for success else message that contain the error.
  type: String
- contextPath: QuarantineFile.Brand
  description: The integration that used to run the command.
  type: String
- contextPath: QuarantineFile.EndpointID
  description: The endpoint_id which the command was executed on.
  type: String
script: '-'
timeout: '0'
type: python
subtype: python3
dockerimage: demisto/python3:3.12.11.4095827
fromversion: 6.10.0
tests:
- No tests (auto formatted)
