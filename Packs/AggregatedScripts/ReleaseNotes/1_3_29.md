
#### Scripts

##### url-enrichment

- Added URL input normalization to automatically prepend `https://` to inputs starting with `www.` or `ftp.`, or containing a path separator, improving indicator extraction accuracy.
- Fixed an issue where the script would fail when all URL inputs were invalid by adding a guard clause to skip command batches when no valid inputs are found.
