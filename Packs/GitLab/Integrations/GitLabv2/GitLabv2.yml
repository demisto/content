category: Utilities
commonfields:
  id: GitLabv2
  version: -1
configuration:
- display: Server URL (e.g., https://gitlab.com/api/v4)
  name: url
  required: true
  type: 0
- displaypassword: API Key
  additionalinfo: The API Key to use for connection.
  name: credentials
  hiddenusername: true
  required: true
  type: 9
- displaypassword: Trigger Token
  additionalinfo: The trigger token to run pipelines. For more info see https://docs.gitlab.com/ee/ci/triggers
  name: trigger_token
  hiddenusername: true
  required: false
  type: 9
- display: Project ID
  additionalinfo: The project ID, it can be found under the project name.
  name: project_id
  required: true
  type: 0
- display: Trust any certificate (not secure)
  name: insecure
  type: 8
  required: false
- display: Use system proxy settings
  name: proxy
  type: 8
  required: false
description: Integration to GitLab API.
display: GitLab v2
name: GitLabv2
script:
  commands:
  - name: gitlab-version-get
    description: Retrieve version information for this GitLab instance.
    outputs:
    - type: String
      contextPath: GitLab.Version.version
      description: The version of this GitLab instance.
    - type: String
      contextPath: GitLab.Version.revision
      description: The revision of this GitLab instance.
  - name: gitlab-file-get
    description: Gets information about a file in the repository, such as name, size, content.
    arguments:
    - name: file_path
      description: The file path.
    - name: ref
      description: The name of the branch, tag or commit.
    outputs:
    - contextPath: GitLab.File.file_name
      description: The file name.
      type: String
    - contextPath: GitLab.File.file_path
      description: The path of the file.
      type: String
    - contextPath: GitLab.File.size
      description: The size of the file.
      type: Number
    - contextPath: GitLab.File.encoding
      description: The encoding of the file.
      type: String
    - contextPath: GitLab.File.content
      description: File content.
      type: String
    - contextPath: GitLab.File.content_sha256
      description: The file after hashing the content.
      type: String
    - contextPath: GitLab.File.ref
      description: The branch the file's content was taken from.
      type: String
    - contextPath: GitLab.File.blob_id
      description: The blob ID.
      type: String
    - contextPath: GitLab.File.commit_id
      description: The commit ID of the file.
      type: String
    - contextPath: GitLab.File.last_commit_id
      description: The last commit ID of the file.
      type: String
    - contextPath: GitLab.File.execute_filemode
      description: Whether the file was executed in filemode.
      type: Boolean
  - name: gitlab-file-create
    description: Allows you to create a single file. file-path or entry_id must be specified.
    arguments:
    - name: entry_id
      description: Entry ID of the file.
    - name: file_path
      description: URL-encoded full path to the new file.
    - name: file_content
      description: The file's content.
    - name: branch
      description: Name of the new branch to create. The commit is added to this branch.
      required: true
    - name: author_email
      description: The commit author's email address.
    - name: author_name
      description: The commit author's name.
    - name: commit_message
      description: The commit message.
      required: true
    - auto: PREDEFINED
      name: execute_filemode
      description: Enables or disables the execute flag on the file.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - contextPath: GitLab.File.file_path
      description: URL-encoded full path of the new file.
      type: String
    - contextPath: GitLab.File.branch
      description: Name of the new branch to create.
      type: String
  - name: gitlab-file-update
    description: Edit an existing file in the repository.
    arguments:
    - name: file_path
      description: URL-encoded full path to the new file.
      required: true
      type: String
    - name: branch
      description: Name of the new branch to create. The commit is added to this branch.
      required: true
    - name: start_branch
      description: Name of the base branch to create the new branch from.
    - auto: PREDEFINED
      name: encoding
      description: Change encoding to base64.
      type: String
      predefined:
      - text
      - base64
    - name: author_email
      description: The commit author email address.
    - name: author_name
      description: The commit author name address.
    - name: entry_id
      description: Entry ID of the file.
    - name: file_content
      description: The file content.
      required: true
    - name: commit_message
      description: The commit message.
      required: true
    - name: last_commit_id
      description: Last known file commit ID.
    - auto: PREDEFINED
      name: execute_filemode
      description: Enables or disables the execute flag on the file.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: String
      contextPath: GitLab.File.file_path
      description: The file path.
    - type: String
      contextPath: GitLab.File.branch
      description: The name of the branch.
  - name: gitlab-file-delete
    description: Delete a file in the repository.
    arguments:
    - name: file_path
      description: URL-encoded full path of the file.
      required: true
      type: String
    - name: branch
      description: Name of the new branch to delete.
      required: true
    - name: commit_message
      description: The commit message.
      required: true
    outputs: []
  - name: gitlab-issue-list
    description: Get a list of a project's issues.
    arguments:
    - name: assignee_id
      description: The ID assigned to the user. The command gitlab-project-user-list gives a list of all relevant users.
    - name: assignee_username
      description: The username assigned to the user.
    - name: author_id
      description: The ID of the author of the issue. The command gitlab-project-user-list gives a list of all relevant users.
    - name: author_username
      description: The username of the author of the issue.
    - name: confidential
      description: Whether to filter confidential or public issues.
      type: Boolean
    - name: created_after
      description: Return issues created on or after the given time in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days), or Format - YYYY-MM-DDThh:mm:ssZ. Example - 2020-02-02T01:02:03Z.
    - name: created_before
      description: Return issues created on or before the given time in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days), or Format - YYYY-MM-DDThh:mm:ssZ. Example - 2020-02-02T01:02:03Z.
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    - auto: PREDEFINED
      name: due_date
      description: Return issues based on this due date.
      type: List
      predefined:
      - 0 (no due date)
      - any
      - today
      - tomorrow
      - overdue
      - week
      - month
      - next_month_and_previous_two_weeks
    - name: epic_id
      description: Return issues associated with the given epic ID. None returns issues that are not associated with an epic.
    - auto: PREDEFINED
      name: issue_type
      description: Filter to a given type of issue.
      predefined:
      - issue
      - incident
      - test_case
    - name: labels
      description: Comma-separated list of label names. Issues must have all labels to be returned. None lists all issues with no labels.
    - name: milestone
      description: The milestone title. None lists all issues with no milestone. Any lists all issues that have an assigned milestone.
    - auto: PREDEFINED
      name: order_by
      defaultValue: created_at
      description: The method used to order the retrieved issues.
      predefined:
      - created_at
      - updated_at
      - priority
      - due_date
      - relative_position
      - label_priority
      - milestone_due
      - popularity
      - weight
    - auto: PREDEFINED
      name: scope
      defaultValue: all
      description: Return issues for the given scope.
      predefined:
      - created_by_me
      - assigned_to_me
      - all
    - name: search
      description: Search project issues against their title and description.
    - auto: PREDEFINED
      name: sort
      defaultValue: desc
      description: Return issues sorted in asc or desc order.
      predefined:
      - desc
      - asc
    - auto: PREDEFINED
      name: state
      description: Return all issues or just those that are opened or closed.
      predefined:
      - opened
      - closed
    - name: updated_after
      description: Return issues updated on or after the given time in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days), or Format - YYYY-MM-DDThh:mm:ssZ. Example - 2020-02-02T01:02:03Z.
    - name: updated_before
      description: Return issues updated on or before the given time in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days), or Format - YYYY-MM-DDThh:mm:ssZ. Example - 2020-02-02T01:02:03Z.
    - defaultValue: 50
      name: limit
      description: Maximum number of results to return.
    - defaultValue: 1
      name: page
      description: The page number of the results to retrieve. Minimum value is 1.
    outputs:
    - type: Number
      contextPath: GitLab.Issue.iid
      description: The issue internal ID.
    - type: String
      contextPath: GitLab.Issue.title
      description: The title of the issue.
    - type: Date
      contextPath: GitLab.Issue.response.created_at
      description: The date of creation.
    - type: String
      contextPath: GitLab.Issue.author.name
      description: The name of the issue's author.
    - type: Date
      contextPath: GitLab.Issue.response.updated_at
      description: The date of the last update.
    - type: Number
      contextPath: GitLab.Issue.milestone.title
      description: The milestone title.
    - type: String
      contextPath: GitLab.Issue.state
      description: The state of the issue (closed or opened).
    - type: String
      contextPath: GitLab.Issue.assignee.name
      description: The name of the assignee.
  - name: gitlab-issue-update
    description: Updates an existing project issue. This call is also used to mark an issue as closed. The ID can be taken from gitlab-issue-list.
    arguments:
    - name: issue_iid
      description: The internal ID of a project's issue.
      required: true
      type: integer
    - name: add_labels
      description: Comma-separated list of label names to add to an issue.
    - name: assignee_ids
      description: The ID of the users to assign the issue to. The command gitlab-project-user-list gives a list of all relevant users.
    - name: confidential
      description: Updates an issue to be confidential.
    - name: description
      description: The description of an issue.
    - name: discussion_locked
      description: Flag indicating if the issue's discussion is locked. If the discussion is locked only project members can add or edit comments.
      type: Boolean
    - name: due_date
      description: The due date. Date time string in the format YYYY-MM-DD, for example 2016-03-11.
    - name: epic_id
      description: ID of the epic to add the issue to. Valid values are greater than or equal to 0.
      type: integer
    - name: epic_iid
      description: Internal ID of the epic to add the issue to. Valid values are greater than or equal to 0.
    - auto: PREDEFINED
      name: issue_type
      description: Updates the type of issue.
      predefined:
      - issue
      - incident
      - test_case
    - name: milestone_id
      description: The global ID of a milestone to assign the issue to. Set to 0 or provide an empty value to unassign a milestone.
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    - name: remove_labels
      description: Comma-separated list of label names to remove from an issue.
    - name: state_event
      description: The state event of an issue. Set "close" to close the issue and "reopen" to reopen it.
    - name: title
      description: The title of an issue.
    outputs:
    - contextPath: GitLab.Issue.id
      description: The issue ID.
      type: Number
    - contextPath: GitLab.Issue.iid
      description: The issue's internal ID.
      type: Number
    - contextPath: GitLab.Issue.project_id
      description: The project ID.
      type: Number
    - contextPath: GitLab.Issue.title
      description: The title of the project.
    - contextPath: GitLab.Issue.description
      description: The project description.
      type: Unknown
    - contextPath: GitLab.Issue.state
      description: The state of the project.
    - contextPath: GitLab.Issue.created_at
      description: Issue creation time.
      type: Date
    - contextPath: GitLab.Issue.updated_at
      description: The date of the last update.
      type: Date
    - contextPath: GitLab.Issue.closed_at
      description: The time of closing the project.
      type: Date
    - contextPath: GitLab.Issue.labels
      description: The labels of the project.
    - contextPath: GitLab.Issue.author.name
      description: The author name.
    - contextPath: GitLab.Issue.author.avatar_url
      description: The avatar URL.
      type: Unknown
    - contextPath: GitLab.Issue.author.username
      description: The username of the author.
    - contextPath: GitLab.Issue.author.id
      description: The author ID.
      type: Number
    - contextPath: GitLab.Issue.author.state
      description: The state of the issue.
    - contextPath: GitLab.Issue.author.web_url
      description: The author URL.
    - contextPath: GitLab.Issue.closed_by.state
      description: The state of the project at closing time.
    - contextPath: GitLab.Issue.closed_by.web_url
      description: The web URL of the author who closed the project.
    - contextPath: GitLab.Issue.closed_by.avatar_url
      description: The avatar URL of the author who closed the project.
      type: Unknown
    - contextPath: GitLab.Issue.closed_by.username
      description: The username of the author who closed the project.
    - contextPath: GitLab.Issue.closed_by.id
      description: The ID of the author who closed the project.
      type: Number
    - contextPath: GitLab.Issue.closed_by.name
      description: The name of the author who closed the project.
    - contextPath: GitLab.Issue.upvotes
      description: The upvotes of the project.
      type: Number
    - contextPath: GitLab.Issue.downvotes
      description: The downvotes of the project.
      type: Number
    - contextPath: GitLab.Issue.merge_requests_count
      description: The project merge requests count.
      type: Number
    - contextPath: GitLab.Issue.assignee
      description: The assignee of the project.
      type: Unknown
    - contextPath: GitLab.Issue.milestone
      description: The projects milestones.
      type: Unknown
    - contextPath: GitLab.Issue.subscribed
      description: Whether the user is subscribed to the project.
      type: Boolean
    - contextPath: GitLab.Issue.user_notes_count
      description: The user notes count.
      type: Number
    - contextPath: GitLab.Issue.due_date
      description: The due date of the project.
      type: Date
    - contextPath: GitLab.Issue.web_url
      description: The project web URL.
    - contextPath: GitLab.Issue.references.short
      description: The issue's reference's short ID.
    - contextPath: GitLab.Issue.references.relative
      description: The number of relatives of the project.
    - contextPath: GitLab.Issue.references.full
      description: The number of references.
    - contextPath: GitLab.Issue.time_stats.time_estimate
      description: The total time estimate to the project.
      type: Number
    - contextPath: GitLab.Issue.time_stats.total_time_spent
      description: The total time spent on the project.
      type: Number
    - contextPath: GitLab.Issue.time_stats.human_time_estimate
      description: The time estimate for the project.
    - contextPath: GitLab.Issue.time_stats.human_total_time_spent
      description: The total time estimate to the project.
      type: Unknown
    - contextPath: GitLab.Issue.confidential
      description: Whether the project is confidential.
      type: Boolean
    - contextPath: GitLab.Issue.discussion_locked
      description: Whether the discussion is locked.
      type: Boolean
    - contextPath: GitLab.Issue.issue_type
      description: The issue type.
    - contextPath: GitLab.Issue.severity
      description: The severity of the project.
    - contextPath: GitLab.Issue._links.self
      description: Dictionary of links.
    - contextPath: GitLab.Issue._links.notes
      description: Link notes.
    - contextPath: GitLab.Issue._links.award_emoji
      description: Award emoji of the project.
    - contextPath: GitLab.Issue._links.project
      description: Link to the project.
    - contextPath: GitLab.Issue._links.closed_as_duplicate_of
      description: Link to a closed duplicate of the project.
    - contextPath: GitLab.Issue.task_completion_status.count
      description: Dictionary of the completion status.
      type: Number
    - contextPath: GitLab.Issue.task_completion_status.completed_count
      description: Dictionary of status.
      type: Number
  - name: gitlab-commit-list
    description: Get a list of repository commits in a project.
    arguments:
    - name: commit_id
      description: If the user provided a value, a specific commit will be retrieved. otherwise, all the commits will be retrieved (SHA).
    - name: ref_name
      description: The name of a repository branch, tag, or revision range, or if not given the default branch.
    - name: created_before
      description: Only commits before or on this date are returned (until) in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days).
    - name: created_after
      description: Only commits after or on this date are returned(since) in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days).
    - name: path
      description: The file path.
    - name: all
      description: Whether to retrieve every commit from the repository.
      type: Boolean
    - name: with_stats
      type: Boolean
      description: Whether to retrieve the stats about each commit.
    - name: first_parent
      description: Follow only the first parent commit upon seeing a merge commit.
    - auto: PREDEFINED
      name: order
      description: List commits in order. Defaults to default, the commits are shown in reverse chronological order.
      type: List
      predefined:
      - default
      - topo
    - defaultValue: 50
      name: limit
      description: Total number of commits to show.
    - defaultValue: 1
      name: page
      description: The page number of the results to retrieve. Minimum value is 1.
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - contextPath: GitLab.Commit.id
      description: The commit ID.
      type: String
    - contextPath: GitLab.Commit.short_id
      description: The commit short ID.
      type: String
    - contextPath: GitLab.Commit.title
      description: The title of the commit.
      type: String
    - contextPath: GitLab.Commit.author_name
      description: The commit's author's name.
      type: String
    - contextPath: GitLab.Commit.author_email
      description: The commit's author's email.
      type: String
    - contextPath: GitLab.Commit.authored_date
      description: The commit authored date.
      type: Date
    - contextPath: GitLab.Commit.committer_name
      description: The commit committer's name.
      type: String
    - contextPath: GitLab.Commit.committer_email
      description: The commit committer's email.
      type: String
    - contextPath: GitLab.Commit.committed_date
      description: The commit committed date.
      type: Date
    - contextPath: GitLab.Commit.created_at
      description: The date the commit was created.
      type: Date
    - contextPath: GitLab.Commit.message
      description: The message attached to the commit.
      type: String
    - contextPath: GitLab.Commit.parent_ids
      description: The commit's parent IDs.
      type: String
    - contextPath: GitLab.Commit.web_url
      description: The commit's web URL.
      type: String
  - name: gitlab-merge-request-list
    description: Get all merge requests for this project.
    arguments:
    - auto: PREDEFINED
      name: state
      description: The state of the merge requests to return.
      predefined:
      - opened
      - closed
      - locked
      - merged
    - auto: PREDEFINED
      name: order_by
      defaultValue: created_at
      description: The method used to order the retrieved issues.
      predefined:
      - created_at
      - updated_at
      - title
    - auto: PREDEFINED
      name: sort
      defaultValue: desc
      description: Return requests sorted in asc or desc order.
      predefined:
      - asc
      - desc
    - name: milestone
      description: Return merge requests for a specific milestone. None returns merge requests with no milestone. Any returns merge requests that have an assigned milestone.
    - name: labels
      description: Return merge requests matching a comma-separated list of labels. None lists all merge requests with no labels. Any lists all merge requests with at least one label. Predefined names are case-insensitive.
    - name: created_after
      description: Return merge requests created on or after the given time in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days), or Format - YYYY-MM-DDThh:mm:ssZ. Example - 2020-02-02T01:02:03Z.
    - name: created_before
      description: Return merge requests created on or before the given time in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days), or Format - YYYY-MM-DDThh:mm:ssZ. Example - 2020-02-02T01:02:03Z.
    - name: updated_after
      description: Return merge requests updated on or after the given time in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days), or Format - YYYY-MM-DDThh:mm:ssZ. Example - 2020-02-02T01:02:03Z.
    - name: updated_before
      description: Return merge requests updated on or before the given time in timestamp format (<number> <time unit>, e.g., 12 hours, 7 days), or Format - YYYY-MM-DDThh:mm:ssZ. Example - 2020-02-02T01:02:03Z.
    - auto: PREDEFINED
      name: scope
      description: Return merge requests for the given scope.
      predefined:
      - created_by_me
      - assigned_to_me
      - all
    - name: author_id
      description: Returns merge requests created by the given user id. Mutually exclusive with author_username. The command gitlab-project-user-list gives a list of all relevant users.
    - name: author_username
      description: Returns merge requests created by the given username. Mutually exclusive with author_id. The command gitlab-project-user-list gives a list of all relevant users.
    - name: assignee_id
      description: Returns merge requests assigned to the given user ID. The command gitlab-project-user-list gives a list of all relevant users.
    - name: reviewer_id
      description: Returns merge requests that have the user as a reviewer with the given user ID. The command gitlab-project-user-list gives a list of all relevant users.
    - name: reviewer_username
      description: Returns merge requests that have the user as a reviewer with the given username. The command gitlab-project-user-list gives a list of all relevant users.
    - name: source_branch
      description: Return merge requests with the given source branch.
    - name: target_branch
      description: Return merge requests with the given target branch.
    - name: search
      description: Search merge requests against their title and description.
    - defaultValue: 50
      name: limit
      description: Total merge requests to show.
    - defaultValue: 1
      name: page
      description: The page number of the results to retrieve. Minimum value is 1.
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.MergeRequest.iid
      description: The internal ID of the merge request.
    - type: String
      contextPath: GitLab.MergeRequest.title
      description: The merge request's title.
    - type: String
      contextPath: GitLab.MergeRequest.created_at
      description: The merge request's creation time.
    - type: Date
      contextPath: GitLab.MergeRequest.created_by
      description: The request's author.
    - type: Date
      contextPath: GitLab.MergeRequest.updated_at
      description: When the merge request was updated.
    - type: Unknown
      contextPath: GitLab.MergeRequest.status
      description: The status of the merge request.
    - type: Unknown
      contextPath: GitLab.MergeRequest.merge_by
      description: The merge request's author.
    - type: Unknown
      contextPath: GitLab.MergeRequest.merged_at
      description: When the merge request was merged.
    - type: Number
      contextPath: GitLab.MergeRequest.reviewers
      description: The reviewers of the merge request.
  - name: gitlab-merge-request-create
    description: Creates a new merge request.
    arguments:
    - name: source_branch
      description: The merge request source branch.
      required: true
    - name: target_branch
      description: The merge request target branch.
      required: true
    - name: title
      description: The merge request title.
      required: true
    - name: assignee_ids
      description: The ID of the users to assign the merge request to. The command gitlab-project-user-list gives a list of all relevant users.
    - name: reviewer_ids
      description: The ID of the users added as a reviewer to the merge request. The command gitlab-project-user-list gives a list of all relevant users.
    - name: description
      description: Description of the merge request. Limited to 1,048,576 characters.
    - name: target_project_id
      description: The target project (numeric ID).
    - name: labels
      description: The global ID of a milestone.
    - name: milestone_id
      description: A comma-separated list of labels for a merge request.
    - name: remove_source_branch
      description: Flag indicating if a merge request should remove the source branch when merging.
    - name: allow_collaboration
      description: Allow commits from members who can merge to the target branch.
    - name: allow_maintainer_to_push
      description: Alias of allow_collaboration.
    - name: approvals_before_merge
      description: Number of approvals required before this can be merged.
    - name: squash
      description: Squash commits into a single commit when merging.
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.MergeRequest.id
      description: The merge request ID. Alias of allow_collaboration.
    - type: Number
      contextPath: GitLab.MergeRequest.iid
      description: The merge request internal ID.
    - type: Number
      contextPath: GitLab.MergeRequest.project_id
      description: The project ID of the merge request.
    - type: String
      contextPath: GitLab.MergeRequest.title
      description: The merge request title.
    - type: Unknown
      contextPath: GitLab.MergeRequest.description
      description: The merge request description.
    - type: String
      contextPath: GitLab.MergeRequest.state
      description: The merge request state.
    - type: Date
      contextPath: GitLab.MergeRequest.created_at
      description: The time the merge request was created.
    - type: Date
      contextPath: GitLab.MergeRequest.updated_at
      description: The time the merge request was updated.
    - type: Unknown
      contextPath: GitLab.MergeRequest.merged_by
      description: The time the merge request was merged.
    - type: Unknown
      contextPath: GitLab.MergeRequest.merge_user
      description: The user who closed the merge request.
    - type: Unknown
      contextPath: GitLab.MergeRequest.merged_at
      description: The time the merge request was closed.
    - type: Unknown
      contextPath: GitLab.MergeRequest.closed_by
      description: The user who closed the merge request.
    - type: Unknown
      contextPath: GitLab.MergeRequest.closed_at
      description: The time the merge request was closed.
    - type: String
      contextPath: GitLab.MergeRequest.target_branch
      description: The merge request target branch.
    - type: String
      contextPath: GitLab.MergeRequest.source_branch
      description: The merge request source branch.
    - type: Number
      contextPath: GitLab.MergeRequest.user_notes_count
      description: The merge request user notes count.
    - type: Number
      contextPath: GitLab.MergeRequest.upvotes
      description: The merge request upvotes.
    - type: Number
      contextPath: GitLab.MergeRequest.downvotes
      description: The merge request downvotes.
    - type: Number
      contextPath: GitLab.MergeRequest.author.id
      description: The merge request author's ID.
    - type: String
      contextPath: GitLab.MergeRequest.author.username
      description: The merge request author's username.
    - type: String
      contextPath: GitLab.MergeRequest.author.name
      description: The merge request author's name.
    - type: String
      contextPath: GitLab.MergeRequest.author.state
      description: The merge request author's state.
    - type: String
      contextPath: GitLab.MergeRequest.author.avatar_url
      description: The merge request author's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequest.author.web_url
      description: The merge request author's web URL.
    - type: Number
      contextPath: GitLab.MergeRequest.assignees.id
      description: The merge request assignees's ID.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.username
      description: The merge request assignees's username.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.name
      description: The merge request assignees's name.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.state
      description: The merge request assignees's state.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.avatar_url
      description: The merge request assignees's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.web_url
      description: The merge request assignees's web URL.
    - type: Number
      contextPath: GitLab.MergeRequest.assignee.id
      description: The merge request assignees's ID.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.username
      description: The merge request assignees's username.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.name
      description: The merge request assignees's name.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.state
      description: The merge request assignees's state.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.avatar_url
      description: The merge request assignees's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.web_url
      description: The merge request assignees's web URL.
    - type: Number
      contextPath: GitLab.MergeRequest.source_project_id
      description: The merge request source project ID.
    - type: Number
      contextPath: GitLab.MergeRequest.target_project_id
      description: The merge request target project ID.
    - type: Boolean
      contextPath: GitLab.MergeRequest.draft
      description: Whether the merge request is a draft.
    - type: Boolean
      contextPath: GitLab.MergeRequest.work_in_progress
      description: The merge request work in progress.
    - type: Unknown
      contextPath: GitLab.MergeRequest.milestone
      description: The global ID of a milestone.
    - type: Boolean
      contextPath: GitLab.MergeRequest.merge_when_pipeline_succeeds
      description: Whether to merge when the pipeline succeeds.
    - type: String
      contextPath: GitLab.MergeRequest.merge_status
      description: The merge status.
    - type: String
      contextPath: GitLab.MergeRequest.sha
      description: The request's SHA.
    - type: Unknown
      contextPath: GitLab.MergeRequest.merge_commit_sha
      description: The merge commit SHA.
    - type: Unknown
      contextPath: GitLab.MergeRequest.squash_commit_sha
      description: The squash commit SHA.
    - type: Unknown
      contextPath: GitLab.MergeRequest.discussion_locked
      description: The discussion locked value.
    - type: Unknown
      contextPath: GitLab.MergeRequest.should_remove_source_branch
      description: Whether to remove the source branch.
    - type: Unknown
      contextPath: GitLab.MergeRequest.force_remove_source_branch
      description: Whether to force remove the source branch.
    - type: String
      contextPath: GitLab.MergeRequest.reference
      description: The merge request's reference.
    - type: String
      contextPath: GitLab.MergeRequest.references.short
      description: The merge request's reference's short.
    - type: String
      contextPath: GitLab.MergeRequest.references.relative
      description: The merge request's reference's relative.
    - type: String
      contextPath: GitLab.MergeRequest.references.full
      description: The merge request's reference's full.
    - type: String
      contextPath: GitLab.MergeRequest.web_url
      description: The merge request web URL.
    - type: Number
      contextPath: GitLab.MergeRequest.time_stats.time_estimate
      description: The merge requests time estimate.
    - type: Number
      contextPath: GitLab.MergeRequest.time_stats.total_time_spent
      description: The merge request's total time spent.
    - type: Unknown
      contextPath: GitLab.MergeRequest.time_stats.human_time_estimate
      description: The merge request's human time estimate.
    - type: Unknown
      contextPath: GitLab.MergeRequest.time_stats.human_total_time_spent
      description: The merge request's human total time spent.
    - type: Boolean
      contextPath: GitLab.MergeRequest.squash
      description: The merge request's squash.
    - type: Number
      contextPath: GitLab.MergeRequest.task_completion_status.count
      description: The merge request's task completion status count.
    - type: Number
      contextPath: GitLab.MergeRequest.task_completion_status.completed_count
      description: The merge request's task completion status completed count.
    - type: Boolean
      contextPath: GitLab.MergeRequest.has_conflicts
      description: Whether the request has conflicts.
    - type: Boolean
      contextPath: GitLab.MergeRequest.blocking_discussions_resolved
      description: Whether the merge request blocking discussions are resolved.
    - type: Unknown
      contextPath: GitLab.MergeRequest.approvals_before_merge
      description: The approvals before merge.
    - type: Boolean
      contextPath: GitLab.MergeRequest.subscribed
      description: Whether the merge request is subscribed.
    - type: String
      contextPath: GitLab.MergeRequest.changes_count
      description: The number of changes to the merge request.
    - type: Unknown
      contextPath: GitLab.MergeRequest.latest_build_started_at
      description: The date when the latest build started.
    - type: Unknown
      contextPath: GitLab.MergeRequest.latest_build_finished_at
      description: The date when the latest build finished.
    - type: Unknown
      contextPath: GitLab.MergeRequest.first_deployed_to_production_at
      description: The date when the merge request was first deployed to production.
    - type: Unknown
      contextPath: GitLab.MergeRequest.pipeline
      description: The merge request's pipeline.
    - type: Unknown
      contextPath: GitLab.MergeRequest.head_pipeline
      description: The merge request's head pipeline.
    - type: String
      contextPath: GitLab.MergeRequest.diff_refs.base_sha
      description: The reference's base SHA.
    - type: String
      contextPath: GitLab.MergeRequest.diff_refs.head_sha
      description: The reference's head SHA.
    - type: String
      contextPath: GitLab.MergeRequest.diff_refs.start_sha
      description: The reference's start SHA.
    - type: Unknown
      contextPath: GitLab.MergeRequest.merge_error
      description: If an error exists, the error of the merge request.
    - type: Boolean
      contextPath: GitLab.MergeRequest.user.can_merge
      description: Whether the user can merge.
  - name: gitlab-merge-request-update
    description: Updates an existing merge request. You can change the target branch, title, or even close the merge request.
    arguments:
    - name: merge_request_id
      description: The ID of a merge request. Can be taken from the gitlab-merge-request-list command.
      required: true
    - name: target_branch
      description: The target branch.
      required: true
    - name: title
      description: Title of the merge request.
      required: true
    - name: assignee_ids
      description: The ID of the users to assign the merge request to. The gitlab-project-user-list command provides a list of all relevant users.
      isArray: true
    - name: reviewer_ids
      description: The ID of the users set as a reviewer to the merge request. The gitlab-project-user-list command provides a list of all relevant users.
      isArray: true
    - name: description
      description: Description of the merge request. Limited to 1,048,576 characters.
    - name: target_project_id
      description: The target project ID.
    - name: labels
      description: Comma-separated list of label names for a merge request. Set to an empty string to unassign all labels.
    - name: add_labels
      description: Comma-separated list of label names to add to a merge request.
    - name: remove_labels
      description: Comma-separated list of label names to remove from a merge request.
    - name: milestone_id
      description: The global ID of a milestone to assign the merge request to. Set to 0 or provide an empty value to unassign a milestone.
    - name: state_event
      description: New state (close/reopen).
    - name: remove_source_branch
      description: Flag indicating if a merge request should remove the source branch when merging.
      type: Boolean
    - name: squash
      description: Whether to squash commits into a single commit when merging.
      type: Boolean
    - name: discussion_locked
      description: Flag indicating if the merge request's discussion is locked. If the discussion is locked only project members can add, edit, or resolve comments.
      type: Boolean
    - name: allow_collaboration
      description: Whether to allow commits from members who can merge to the target branch.
      type: Boolean
    - name: allow_maintainer_to_push
      description: Alias of allow_collaboration.
      type: Boolean
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.MergeRequest.id
      description: The merge request ID. Alias of allow_collaboration.
    - type: Number
      contextPath: GitLab.MergeRequest.iid
      description: The merge request internal ID.
    - type: Number
      contextPath: GitLab.MergeRequest.project_id
      description: The project ID of the merge request.
    - type: String
      contextPath: GitLab.MergeRequest.title
      description: The merge request title.
    - type: Unknown
      contextPath: GitLab.MergeRequest.description
      description: The merge request description.
    - type: String
      contextPath: GitLab.MergeRequest.state
      description: The merge request state.
    - type: Date
      contextPath: GitLab.MergeRequest.created_at
      description: The time the merge request was created.
    - type: Date
      contextPath: GitLab.MergeRequest.updated_at
      description: The time the merge request was updated.
    - type: String
      contextPath: GitLab.MergeRequest.target_branch
      description: The merge request target branch.
    - type: String
      contextPath: GitLab.MergeRequest.source_branch
      description: The merge request source branch.
    - type: Number
      contextPath: GitLab.MergeRequest.upvotes
      description: The merge request upvotes.
    - type: Number
      contextPath: GitLab.MergeRequest.downvotes
      description: The merge request downvotes.
    - type: Number
      contextPath: GitLab.MergeRequest.author.id
      description: The merge request author's ID.
    - type: String
      contextPath: GitLab.MergeRequest.author.username
      description: The merge request author's username.
    - type: String
      contextPath: GitLab.MergeRequest.author.name
      description: The merge request author's name.
    - type: String
      contextPath: GitLab.MergeRequest.author.state
      description: The merge request author's state.
    - type: String
      contextPath: GitLab.MergeRequest.author.avatar_url
      description: The merge request author's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequest.author.web_url
      description: The merge request author's web URL.
    - type: Number
      contextPath: GitLab.MergeRequest.assignee.id
      description: The merge request assignees's ID.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.name
      description: The merge request assignees's name.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.username
      description: The merge request assignees's username.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.state
      description: The merge request assignees's state.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.avatar_url
      description: The merge request assignees's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequest.assignee.web_url
      description: The merge request assignees's web URL.
    - type: Number
      contextPath: GitLab.MergeRequest.assignees.id
      description: The merge request assignees's ID.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.username
      description: The merge request assignees's username.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.name
      description: The merge request assignees's name.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.state
      description: The merge request assignees's state.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.avatar_url
      description: The merge request assignees's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequest.assignees.web_url
      description: The merge request assignees's web URL.
    - type: Number
      contextPath: GitLab.MergeRequest.reviewers.id
      description: The reviewer's ID.
    - type: String
      contextPath: GitLab.MergeRequest.reviewers.username
      description: The reviewer's username.
    - type: String
      contextPath: GitLab.MergeRequest.reviewers.name
      description: The reviewer's name.
    - type: String
      contextPath: GitLab.MergeRequest.reviewers.state
      description: The reviewer's state.
    - type: String
      contextPath: GitLab.MergeRequest.reviewers.avatar_url
      description: The reviewer's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequest.reviewers.web_url
      description: The reviewer's web URL.
    - type: Number
      contextPath: GitLab.MergeRequest.source_project_id
      description: The merge request source project ID.
    - type: Number
      contextPath: GitLab.MergeRequest.target_project_id
      description: The merge request target project ID.
    - type: Unknown
      contextPath: GitLab.MergeRequest.labels
      description: Merge request's labels.
    - type: Boolean
      contextPath: GitLab.MergeRequest.draft
      description: Whether the merge request is a draft.
    - type: Boolean
      contextPath: GitLab.MergeRequest.work_in_progress
      description: Whether the merge request is a draft.
    - type: Unknown
      contextPath: GitLab.MergeRequest.milestone
      description: Milestone details.
    - type: Integer
      contextPath: GitLab.MergeRequest.milestone.id
      description: Milestone ID.
    - type: Integer
      contextPath: GitLab.MergeRequest.milestone.iid
      description: Milestone's internal ID.
    - type: Integer
      contextPath: GitLab.MergeRequest.milestone.project_id
      description: Milestone's project ID.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.title
      description: Milestone's title.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.description
      description: Milestone's description.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.
      description: Milestone's details.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.state
      description: Milestone's state.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.created_at
      description: Date the milestone was created.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.updated_at
      description: Date the milestone was updated.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.due_date
      description: Date the milestone is due.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.start_date
      description: Date the milestone started.
    - type: String
      contextPath: GitLab.MergeRequest.milestone.web_url
      description: Milestone's web URL.
    - type: Boolean
      contextPath: GitLab.MergeRequest.merge_when_pipeline_succeeds
      description: Whether to merge when the pipeline succeeds.
    - type: String
      contextPath: GitLab.MergeRequest.merge_status
      description: The merge status.
    - type: String
      contextPath: GitLab.MergeRequest.merge_error
      description: The merge error if it exists.
    - type: String
      contextPath: GitLab.MergeRequest.sha
      description: The request's SHA.
    - type: Unknown
      contextPath: GitLab.MergeRequest.merge_commit_sha
      description: The merge commit SHA.
    - type: Unknown
      contextPath: GitLab.MergeRequest.squash_commit_sha
      description: The squash commit SHA.
    - type: Integer
      contextPath: GitLab.MergeRequest.user_notes_count
      description: The merge request user notes count.
    - type: Unknown
      contextPath: GitLab.MergeRequest.discussion_locked
      description: The discussion locked value.
    - type: Unknown
      contextPath: GitLab.MergeRequest.should_remove_source_branch
      description: Whether to remove the source branch.
    - type: Unknown
      contextPath: GitLab.MergeRequest.force_remove_source_branch
      description: Whether to force remove the source branch.
    - type: Boolean
      contextPath: GitLab.MergeRequest.allow_collaboration
      description: Whether to allow collaboration.
    - type: Boolean
      contextPath: GitLab.MergeRequest.allow_maintainer_to_push
      description: Whether to allow the maintainer to push.
    - type: String
      contextPath: GitLab.MergeRequest.web_url
      description: The merge request web URL.
    - type: String
      contextPath: GitLab.MergeRequest.reference
      description: The merge request's reference.
    - type: String
      contextPath: GitLab.MergeRequest.references.short
      description: The merge request's reference's short.
    - type: String
      contextPath: GitLab.MergeRequest.references.relative
      description: The merge requests reference's relative.
    - type: String
      contextPath: GitLab.MergeRequest.references.full
      description: The merge request's reference's full.
    - type: Number
      contextPath: GitLab.MergeRequest.time_stats.time_estimate
      description: The merge request's time estimate.
    - type: Number
      contextPath: GitLab.MergeRequest.time_stats.total_time_spent
      description: The merge request's total time spent.
    - type: Unknown
      contextPath: GitLab.MergeRequest.time_stats.human_time_estimate
      description: The merge request's human time estimate.
    - type: Unknown
      contextPath: GitLab.MergeRequest.time_stats.human_total_time_spent
      description: The merge request's human total time spent.
    - type: Boolean
      contextPath: GitLab.MergeRequest.squash
      description: The merge request's squash.
    - type: Boolean
      contextPath: GitLab.MergeRequest.subscribed
      description: Whether the merge request is subscribed.
    - type: Integer
      contextPath: GitLab.MergeRequest.changes_count
      description: The number of changes to the merge request.
    - type: Integer
      contextPath: GitLab.MergeRequest.merge_user.id
      description: The merge request's merge user's ID.
    - type: String
      contextPath: GitLab.MergeRequest.merge_user.name
      description: The merge request's merge user's name.
    - type: String
      contextPath: GitLab.MergeRequest.merge_user.username
      description: The merge request's merge user's username.
    - type: String
      contextPath: GitLab.MergeRequest.merge_user.state
      description: The merge request's merge user's state.
    - type: String
      contextPath: GitLab.MergeRequest.merge_user.avatar_url
      description: The merge request's merge user's avatar URL.
    - type: Date
      contextPath: GitLab.MergeRequest.merged_at
      description: The date the merge request was merged.
    - type: String
      contextPath: GitLab.MergeRequest.closed_by
      description: The user who closed the request.
    - type: Date
      contextPath: GitLab.MergeRequest.closed_at
      description: The merge request's closing time.
    - type: Date
      contextPath: GitLab.MergeRequest.latest_build_started_at
      description: The date when the latest build started.
    - type: Date
      contextPath: GitLab.MergeRequest.latest_build_finished_at
      description: The date when the latest build finished.
    - type: Date
      contextPath: GitLab.MergeRequest.first_deployed_to_production_at
      description: The date when the merge request was first deployed to production.
    - type: Integer
      contextPath: GitLab.MergeRequest.pipeline.ID
      description: The merge request's pipeline's ID.
    - type: String
      contextPath: GitLab.MergeRequest.pipeline.sha
      description: The merge request's pipeline's SHA.
    - type: String
      contextPath: GitLab.MergeRequest.pipeline.ref
      description: The merge request's pipeline's reference.
    - type: String
      contextPath: GitLab.MergeRequest.pipeline.status
      description: The merge request's pipeline's status.
    - type: String
      contextPath: GitLab.MergeRequest.pipeline.web_url
      description: The merge request's pipeline's web URL.
    - type: String
      contextPath: GitLab.MergeRequest.diff_refs.base_sha
      description: The reference's base SHA.
    - type: String
      contextPath: GitLab.MergeRequest.diff_refs.head_sha
      description: The reference's head SHA.
    - type: String
      contextPath: GitLab.MergeRequest.diff_refs.start_sha
      description: The reference's start SHA.
    - type: Integer
      contextPath: GitLab.MergeRequest.diverged_commits_count
      description: The number of diverged commits.
    - type: Integer
      contextPath: GitLab.MergeRequest.task_completion_status.count
      description: The status count of the number of task completion.
    - type: Integer
      contextPath: GitLab.MergeRequest.task_completion_status.completed_count
      description: The number of tasks completed completions.
  - name: gitlab-issue-note-create
    description: Creates a new note for a single project issue.
    arguments:
    - name: issue_iid
      description: The internal ID of an issue.
      required: true
    - name: body
      description: The content of a note. Limited to 1,000,000 characters.
      required: true
      type: String
    - name: confidential
      description: Will be removed in GitLab 16.0 and renamed to internal. The confidential flag of a note. Default is false.
      type: Boolean
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - contextPath: GitLab.IssueNote.id
      description: The issue note ID.
      type: Number
    - contextPath: GitLab.IssueNote.type
      description: The type of the Project - private, internal, or public.
      type: Unknown
    - contextPath: GitLab.IssueNote.body
      description: The issue note's body.
      type: String
    - contextPath: GitLab.IssueNote.attachment
      description: The attachment of the issue note.
      type: Unknown
    - contextPath: GitLab.IssueNote.author.id
      description: The issue note's author's ID.
      type: Number
    - contextPath: GitLab.IssueNote.author.username
      description: The issue note's author's username.
      type: String
    - contextPath: GitLab.IssueNote.author.name
      description: The issue note's author's name.
      type: String
    - contextPath: GitLab.IssueNote.author.state
      description: The issue note's author's state.
      type: String
    - contextPath: GitLab.IssueNote.author.avatar_url
      description: The issue note's author's avatar URL.
      type: String
    - contextPath: GitLab.IssueNote.author.web_url
      description: The issue note's author's web URL.
      type: String
    - contextPath: GitLab.IssueNote.created_at
      description: Date the issue note was created.
      type: Date
    - contextPath: GitLab.IssueNote.updated_at
      description: Date the issue note was updated.
      type: Date
    - contextPath: GitLab.IssueNote.system
      description: Whether the issue note is about changes to the project.
      type: Boolean
    - contextPath: GitLab.IssueNote.noteable_id
      description: The noteable ID of the issue note.
      type: Number
    - contextPath: GitLab.IssueNote.noteable_type
      description: The noteable type of the issue note.
      type: String
    - contextPath: GitLab.IssueNote.resolvable
      description: Whether the issue is resolvable.
      type: Boolean
    - contextPath: GitLab.IssueNote.confidential
      description: Whether the issue is confidential.
      type: Boolean
    - contextPath: GitLab.IssueNote.internal
      description: Whether the issue is internal.
      type: Boolean
    - contextPath: GitLab.IssueNote.noteable_iid
      description: The noteable internal ID.
      type: Number
  - name: gitlab-issue-note-delete
    description: Deletes an existing note of an issue.
    arguments:
    - name: issue_iid
      description: The internal ID of an issue.
      required: true
    - name: note_id
      description: The ID of a note.
      required: true
    outputs:
    - type: String
      contextPath: GitLab.IssueNote.message
      description: The issue note message.
  - name: gitlab-issue-note-update
    description: Modify an existing note of an issue.
    arguments:
    - name: issue_iid
      description: The issue internal ID.
      required: true
    - name: note_id
      description: The note ID.
      required: true
    - name: body
      description: The content of a note.
      required: true
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.IssueNote.id
      description: The issue note ID.
    - type: Unknown
      contextPath: GitLab.IssueNote.type
      description: The issue note type.
    - type: String
      contextPath: GitLab.IssueNote.body
      description: The content of a note.
    - type: Unknown
      contextPath: GitLab.IssueNote.attachment
      description: Attachment to the issue note.
    - type: Number
      contextPath: GitLab.IssueNote.author.id
      description: The issue note's author's ID.
    - type: String
      contextPath: GitLab.IssueNote.author.username
      description: The issue note's author's username.
    - type: String
      contextPath: GitLab.IssueNote.author.name
      description: The issue note's author's name.
    - type: String
      contextPath: GitLab.IssueNote.author.state
      description: The issue note's author's state.
    - type: String
      contextPath: GitLab.IssueNote.author.avatar_url
      description: The issue note's author's avatar URL.
    - type: String
      contextPath: GitLab.IssueNote.author.web_url
      description: The issue note's author's web URL.
    - type: Date
      contextPath: GitLab.IssueNote.created_at
      description: Date the issue note was created.
    - type: Date
      contextPath: GitLab.IssueNote.updated_at
      description: Date the issue note was updated.
    - type: Boolean
      contextPath: GitLab.IssueNote.system
      description: Whether the note is about changes to the object.
    - type: Number
      contextPath: GitLab.IssueNote.noteable_id
      description: The notable ID.
    - type: String
      contextPath: GitLab.IssueNote.noteable_type
      description: The notable type.
    - type: Boolean
      contextPath: GitLab.IssueNote.resolvable
      description: Whether the thread is resolvable.
    - type: Boolean
      contextPath: GitLab.IssueNote.confidential
      description: Whether the thread is confidential.
    - type: Boolean
      contextPath: GitLab.IssueNote.internal
      description: Whether the thread is internal.
    - type: Number
      contextPath: GitLab.IssueNote.noteable_iid
      description: The notable internal ID.
  - name: gitlab-issue-note-list
    description: Gets a list of all notes for a single issue.
    arguments:
    - name: issue_iid
      description: The internal ID of an issue in order to get a specific issue note.
      required: true
    - auto: PREDEFINED
      name: order_by
      defaultValue: created_at
      description: The method used to order the retrieved issues.
      predefined:
      - created_at
      - updated_at
    - auto: PREDEFINED
      name: sort
      defaultValue: desc
      description: Return requests sorted in asc or desc order.
      predefined:
      - desc
      - asc
    - defaultValue: 50
      description: Maximum number of results to return.
      name: limit
    - defaultValue: 1
      description: The page number of the results to retrieve. Minimum value is 1.
      name: page
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.IssueNote.id
      description: The issue note ID.
    - type: Unknown
      contextPath: GitLab.IssueNote.type
      description: The issue note type.
    - type: String
      contextPath: GitLab.IssueNote.body
      description: The content of a note.
    - type: Unknown
      contextPath: GitLab.IssueNote.attachment
      description: Attachment to the issue note.
    - type: Number
      contextPath: GitLab.IssueNote.author.id
      description: The issue note's author's ID.
    - type: String
      contextPath: GitLab.IssueNote.author.username
      description: The issue note's author's username.
    - type: String
      contextPath: GitLab.IssueNote.author.name
      description: The issue note's author's name.
    - type: String
      contextPath: GitLab.IssueNote.author.state
      description: The issue note's author's state.
    - type: String
      contextPath: GitLab.IssueNote.author.avatar_url
      description: The issue note's author's avatar URL.
    - type: String
      contextPath: GitLab.IssueNote.author.web_url
      description: The issue note's author's web URL.
    - type: Date
      contextPath: GitLab.IssueNote.created_at
      description: Date the issue note was created.
    - type: Date
      contextPath: GitLab.IssueNote.updated_at
      description: Date the issue note was updated.
    - type: Boolean
      contextPath: GitLab.IssueNote.system
      description: Whether the note is about changes to the object.
    - type: Number
      contextPath: GitLab.IssueNote.noteable_id
      description: The notable ID.
    - type: String
      contextPath: GitLab.IssueNote.noteable_type
      description: The notable type.
    - type: Boolean
      contextPath: GitLab.IssueNote.resolvable
      description: Whether the thread is resolvable.
    - type: Boolean
      contextPath: GitLab.IssueNote.confidential
      description: Whether the thread is confidential.
    - type: Boolean
      contextPath: GitLab.IssueNote.internal
      description: Whether the thread is internal.
    - type: Number
      contextPath: GitLab.IssueNote.noteable_iid
      description: The notable internal ID.
  - name: gitlab-merge-request-note-list
    description: Gets a list of all notes for a single merge request.
    arguments:
    - name: merge_request_iid
      description: The internal ID of a project merge request.
      required: true
    - auto: PREDEFINED
      defaultValue: desc
      name: sort
      description: Return merge request notes sorted in asc or desc order.
      predefined:
      - desc
      - asc
    - auto: PREDEFINED
      defaultValue: created_at
      name: order_by
      description: The method used to order the retrieved issues.
      predefined:
      - created_at
      - updated_at
    - defaultValue: 50
      name: limit
      description: Total merge requests to show.
    - defaultValue: 1
      name: page
      description: The page number of the results to retrieve. Minimum value is 1.
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.MergeRequestNote.id
      description: The ID of the merge request note.
    - type: Unknown
      contextPath: GitLab.MergeRequestNote.type
      description: The merge request note's type.
    - type: String
      contextPath: GitLab.MergeRequestNote.body
      description: The merge request note's body.
    - type: Unknown
      contextPath: GitLab.MergeRequestNote.attachment
      description: The merge request note's attachment.
    - type: Number
      contextPath: GitLab.MergeRequestNote.author.id
      description: The merge request note's author's ID.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.username
      description: The merge request note's author's username.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.name
      description: The merge request note's author's name.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.state
      description: The merge request note's author's state.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.avatar_url
      description: The merge request note's author's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.web_url
      description: The merge request note's author's web URL.
    - type: Date
      contextPath: GitLab.MergeRequestNote.created_at
      description: The merge request note's creation time.
    - type: Date
      contextPath: GitLab.MergeRequestNote.updated_at
      description: The time of the last update of the merge request note.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.system
      description: Whether the note is about changes to the object.
    - type: Number
      contextPath: GitLab.MergeRequestNote.noteable_id
      description: The merge request's noteable ID.
    - type: String
      contextPath: GitLab.MergeRequestNote.noteable_type
      description: The merge request's noteable type.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.resolvable
      description: Whether the merge request is resolvable.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.confidential
      description: Whether the merge request is confidential.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.internal
      description: Whether the merge request is internal.
    - type: Number
      contextPath: GitLab.MergeRequestNote.noteable_iid
      description: The merge request's noteable internal ID.
  - name: gitlab-merge-request-note-create
    description: Creates a new note for a single merge request.
    arguments:
    - name: merge_request_iid
      description: The internal ID of a project merge request.
      required: true
    - name: body
      description: The content of a note. Limited to 1,000,000 characters.
      required: true
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.MergeRequestNote.id
      description: The ID of the merge request note.
    - type: Unknown
      contextPath: GitLab.MergeRequestNote.type
      description: The merge request note's type.
    - type: String
      contextPath: GitLab.MergeRequestNote.body
      description: The merge request note's body.
    - type: Unknown
      contextPath: GitLab.MergeRequestNote.attachment
      description: The merge request note's attachment.
    - type: Number
      contextPath: GitLab.MergeRequestNote.author.id
      description: The merge request note's author's ID.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.username
      description: The merge request note's author's username.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.name
      description: The merge request note's author's name.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.state
      description: The merge request note's author's state.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.avatar_url
      description: The merge request note's author's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.web_url
      description: The merge request note's author's web URL.
    - type: Date
      contextPath: GitLab.MergeRequestNote.created_at
      description: The merge request note's creation time.
    - type: Date
      contextPath: GitLab.MergeRequestNote.updated_at
      description: The time of the last update of the merge request note.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.system
      description: Whether the note is about changes to the object.
    - type: Number
      contextPath: GitLab.MergeRequestNote.noteable_id
      description: The merge request's noteable ID.
    - type: String
      contextPath: GitLab.MergeRequestNote.noteable_type
      description: The merge request's noteable type.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.resolvable
      description: Whether the merge request is resolvable.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.confidential
      description: Whether the merge request is confidential.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.internal
      description: Whether the merge request is internal.
    - type: Number
      contextPath: GitLab.MergeRequestNote.noteable_iid
      description: The merge request's noteable internal ID.
  - name: gitlab-merge-request-note-update
    description: Modify existing note of a merge request.
    arguments:
    - name: merge_request_iid
      description: The internal ID of a project merge request.
      required: true
    - name: note_id
      description: The ID of a note.
      required: true
    - name: body
      description: The content of a note. Limited to 1,000,000 characters.
      required: true
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.MergeRequestNote.id
      description: The ID of the merge request note.
    - type: Unknown
      contextPath: GitLab.MergeRequestNote.type
      description: The merge request note's type.
    - type: String
      contextPath: GitLab.MergeRequestNote.body
      description: The merge request note's body.
    - type: Unknown
      contextPath: GitLab.MergeRequestNote.attachment
      description: The merge request note's attachment.
    - type: Number
      contextPath: GitLab.MergeRequestNote.author.id
      description: The merge request note's author's ID.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.username
      description: The merge request note's author's username.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.name
      description: The merge request note's author's name.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.state
      description: The merge request note's author's state.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.avatar_url
      description: The merge request note's author's avatar URL.
    - type: String
      contextPath: GitLab.MergeRequestNote.author.web_url
      description: The merge request note's author's web URL.
    - type: Date
      contextPath: GitLab.MergeRequestNote.created_at
      description: The merge request note's creation time.
    - type: Date
      contextPath: GitLab.MergeRequestNote.updated_at
      description: The time of the last update of the merge request note.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.system
      description: Whether the note is about changes to the object.
    - type: Number
      contextPath: GitLab.MergeRequestNote.noteable_id
      description: The merge request's noteable ID.
    - type: String
      contextPath: GitLab.MergeRequestNote.noteable_type
      description: The merge request's noteable type.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.resolvable
      description: Whether the merge request is resolvable.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.confidential
      description: Whether the merge request is confidential.
    - type: Boolean
      contextPath: GitLab.MergeRequestNote.internal
      description: Whether the merge request is internal.
    - type: Number
      contextPath: GitLab.MergeRequestNote.noteable_iid
      description: The merge request's noteable internal ID.
  - name: gitlab-merge-request-note-delete
    description: Deletes an existing note of a merge request.
    arguments:
    - name: merge_request_iid
      description: The internal ID of a merge request.
      required: true
    - name: note_id
      description: The ID of a note.
      required: true
    outputs: []
  - name: gitlab-issue-create
    description: Create an issue.
    arguments:
    - name: labels
      description: Comma-separated list of labels to add to the issue.
    - name: title
      description: The issue title.
      required: true
    - name: description
      description: The issue description.
      required: true
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - contextPath: GitLab.Issue.iid
      description: The issue's internal ID.
      type: Number
    - contextPath: GitLab.Issue.created_at
      description: The issue's creation date.
      type: Date
    - contextPath: GitLab.Issue.title
      description: The issue's title.
      type: String
    - contextPath: GitLab.Issue.state
      description: The issue's state.
      type: String
    - contextPath: GitLab.Issue.assignee
      description: The issue's assignee.
      type: Unknown
    - contextPath: GitLab.Issue.type
      description: The issue's type.
      type: String
    - contextPath: GitLab.Issue.labels
      description: The issue's labels.
      type: String
    - contextPath: GitLab.Issue.upvotes
      description: The issue's upvotes.
      type: Number
    - contextPath: GitLab.Issue.downvotes
      description: The issue's downvotes.
      type: Number
    - contextPath: GitLab.Issue.merge_requests_count
      description: The issue's number of merge requests.
      type: Number
    - contextPath: GitLab.Issue.author.name
      description: The issue author's name.
      type: String
    - contextPath: GitLab.Issue.author.avatar_url
      description: The issue author's avatar URL.
      type: Unknown
    - contextPath: GitLab.Issue.author.state
      description: The issue author's state.
      type: String
    - contextPath: GitLab.Issue.author.web_url
      description: The issue author's web URL.
      type: String
    - contextPath: GitLab.Issue.author.id
      description: The issue author's ID.
      type: Number
    - contextPath: GitLab.Issue.author.username
      description: The issue author's username.
      type: String
    - contextPath: GitLab.Issue.description
      description: The issue's description.
      type: Unknown
    - contextPath: GitLab.Issue.updated_at
      description: The issue's last update date.
      type: Date
    - contextPath: GitLab.Issue.closed_at
      description: The date that the issue was closed.
      type: Unknown
    - contextPath: GitLab.Issue.closed_by
      description: The user who closed the issue if closed.
      type: Unknown
    - contextPath: GitLab.Issue.milestone
      description: The issue's milestone title.
      type: Unknown
    - contextPath: GitLab.Issue.subscribed
      description: Whether the user is subscribed to the issue (received notifications about the issue).
      type: Boolean
    - contextPath: GitLab.Issue.user_notes_count
      description: The number of the user notes.
      type: Number
    - contextPath: GitLab.Issue.due_date
      description: The due date of the issue.
      type: Unknown
    - contextPath: GitLab.Issue.web_url
      description: The issue's web URL.
      type: String
    - contextPath: GitLab.Issue.references.short
      description: The issue's short references.
      type: String
    - contextPath: GitLab.Issue.references.relative
      description: The issue's relative references.
      type: String
    - contextPath: GitLab.Issue.references.full
      description: The issue's full references.
      type: String
    - contextPath: GitLab.Issue.time_stats.time_estimate
      description: The time estimate to solve the issue.
      type: Number
    - contextPath: GitLab.Issue.time_stats.total_time_spent
      description: The time that was spent to solve the issue.
      type: Number
    - contextPath: GitLab.Issue.time_stats.human_time_estimate
      description: The human time estimate to solve the issue.
      type: Unknown
    - contextPath: GitLab.Issue.time_stats.human_total_time_spent
      description: The human time that was spent to solve the issue.
      type: Unknown
    - contextPath: GitLab.Issue.confidential
      description: Whether the issue is confidential.
      type: Boolean
    - contextPath: GitLab.Issue.discussion_locked
      description: Whether the discussion is locked.
      type: Boolean
    - contextPath: GitLab.Issue.issue_type
      description: The issue's type.
      type: String
    - contextPath: GitLab.Issue.severity
      description: The issue's severity.
      type: String
    - contextPath: GitLab.Issue._links.self
      description: The issue's link.
      type: String
    - contextPath: GitLab.Issue._links.notes
      description: The issue's notes' link.
      type: String
    - contextPath: GitLab.Issue._links.award_emoji
      description: The issue's award emoji's link.
      type: String
    - contextPath: GitLab.Issue._links.project
      description: The issue's project's link.
      type: String
    - contextPath: GitLab.Issue._links.closed_as_duplicate_of
      description: Link to a closed duplicate of the project.
      type: String
    - contextPath: GitLab.Issue.task_completion_status.count
      description: The issue's completion status counter.
      type: Number
    - contextPath: GitLab.Issue.task_completion_status.completed_count
      description: The issue's completion status completed counter.
      type: Number
  - name: gitlab-project-list
    description: Get a list of all visible projects across GitLab for the authenticated user. When accessed without authentication, only public projects with simple fields are returned.
    arguments:
    - name: membership
      description: Limit by projects that the current user is a member of.
    - auto: PREDEFINED
      name: order_by
      defaultValue: created_at
      description: The method used to order the retrieved issues. repository_size, storage_size, packages_size or wiki_size fields are only allowed for administrators.
      predefined:
      - id
      - name
      - path
      - created_at
      - updated_at
      - last_activity_at
      - similarity
    - name: owned
      description: Whether to limit by projects explicitly owned by the current user.
      type: boolean
    - name: search
      description: Return list of projects matching the search criteria.
    - auto: PREDEFINED
      name: sort
      description: Return projects sorted in asc or desc order.
      defaultValue: desc
      predefined:
      - asc
      - desc
    - name: visibility
      description: Limit by visibility public, internal, or private.
      type: Boolean
    - name: with_issues_enabled
      description: Whether to limit by enabled issues feature.
      type: boolean
    - name: with_merge_requests_enabled
      type: boolean
      description: Whether to limit by enabled merge requests feature.
    - name: page
      defaultValue: 1
      description: The page number of the results to retrieve. Minimum value is 1.
    - defaultValue: 50
      description: Maximum number of results to return.
      name: limit
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - type: Number
      contextPath: GitLab.Project.id
      description: The project's ID.
    - type: Unknown
      contextPath: GitLab.Project.description
      description: The project's description.
    - type: String
      contextPath: GitLab.Project.name
      description: The project's name.
    - type: String
      contextPath: GitLab.Project.name_with_namespace
      description: The project's name with namespace.
    - type: String
      contextPath: GitLab.Project.path
      description: The project's path.
    - type: String
      contextPath: GitLab.Project.path_with_namespace
      description: The project's path with namespace.
    - type: Date
      contextPath: GitLab.Project.created_at
      description: The project's creation date.
    - type: String
      contextPath: GitLab.Project.default_branch
      description: The project's default branch.
    - type: String
      contextPath: GitLab.Project.ssh_url_to_repo
      description: The project's SSH URL to repository.
    - type: String
      contextPath: GitLab.Project.http_url_to_repo
      description: The project's HTTP URL to repository.
    - type: String
      contextPath: GitLab.Project.web_url
      description: The project's web URL.
    - type: String
      contextPath: GitLab.Project.readme_url
      description: The project's readme URL.
    - type: Unknown
      contextPath: GitLab.Project.avatar_url
      description: The project's avatar URL.
    - type: Number
      contextPath: GitLab.Project.forks_count
      description: Number of forks of the project.
    - type: Number
      contextPath: GitLab.Project.star_count
      description: Number of stars of the project.
    - type: Number
      contextPath: GitLab.Project.last_activity_at
      description: The last activity time.
    - type: Number
      contextPath: GitLab.Project.namespace.id
      description: Namespace ID.
    - type: String
      contextPath: GitLab.Project.namespace.name
      description: The project's namespace name.
    - type: String
      contextPath: GitLab.Project.namespace.path
      description: The project's namespace path.
    - type: String
      contextPath: GitLab.Project.namespace.kind
      description: The project's namespace kind.
    - type: String
      contextPath: GitLab.Project.namespace.full_path
      description: The project's namespace full path.
    - type: Unknown
      contextPath: GitLab.Project.namespace.parent_id
      description: The project's namespace parent ID.
    - type: Unknown
      contextPath: GitLab.Project.namespace.avatar_url
      description: The project's namespace avatar URL.
    - type: String
      contextPath: GitLab.Project.namespace.web_url
      description: The project's namespace web URL.
    - type: String
      contextPath: GitLab.Project.container_registry_image_prefix
      description: The project container registry image prefix.
    - type: Dictionary
      contextPath: GitLab.Project._links.self
      description: Dictionary of links for the list project.
    - type: String
      contextPath: GitLab.Project._links.issues
      description: Link to the issues of the project.
    - type: String
      contextPath: GitLab.Project._links.merge_requests
      description: Link to the merge requests of the project.
    - type: String
      contextPath: GitLab.Project._links.repo_branches
      description: Link to the repository branches of the project.
    - type: String
      contextPath: GitLab.Project._links.labels
      description: Link to the labels of the project.
    - type: String
      contextPath: GitLab.Project._links.events
      description: Link to the events of the project.
    - type: String
      contextPath: GitLab.Project._links.members
      description: Link to the members of the project.
    - type: String
      contextPath: GitLab.Project._links.cluster_agents
      description: Link to the cluster agents of the project.
    - type: String
      contextPath: GitLab.Project.packages_enabled
      description: Whether the packages are enabled.
    - type: Boolean
      contextPath: GitLab.Project.empty_repo
      description: Whether the repository is empty.
    - type: Boolean
      contextPath: GitLab.Project.archived
      description: Whether the project is archived.
    - type: String
      contextPath: GitLab.Project.visibility
      description: The visibility of the project Public, Private or Internal.
    - type: Boolean
      contextPath: GitLab.Project.resolve_outdated_diff_discussions
      description: The resolve outdated diff discussions.
    - type: String
      contextPath: GitLab.Project.container_expiration_policy.cadence
      description: The cadence of the container expiration policy.
    - type: Boolean
      contextPath: GitLab.Project.container_expiration_policy.enabled
      description: Whether the container expiration policy enabled.
    - type: Number
      contextPath: GitLab.Project.container_expiration_policy.keep_n
      description: Keep n value.
    - type: String
      contextPath: GitLab.Project.container_expiration_policy.older_than
      description: Number of days of the container expiration policy.
    - type: String
      contextPath: GitLab.Project.container_expiration_policy.name_regex
      description: Regex name of the container expiration policy.
    - type: Unknown
      contextPath: GitLab.Project.container_expiration_policy.name_regex_keep
      description: Value of the regex keep of the container expiration policy.
    - type: Date
      contextPath: GitLab.Project.container_expiration_policy.next_run_at
      description: Next run at value of the container expiration policy.
    - type: Boolean
      contextPath: GitLab.Project.issues_enabled
      description: Whether issues are enabled.
    - type: Boolean
      contextPath: GitLab.Project.merge_requests_enabled
      description: Whether the merge requests are enabled.
    - type: Boolean
      contextPath: GitLab.Project.wiki_enabled
      description: Whether the wiki is enabled.
    - type: Boolean
      contextPath: GitLab.Project.jobs_enabled
      description: Whether the jobs are enabled.
    - type: Boolean
      contextPath: GitLab.Project.snippets_enabled
      description: Whether snippets are enabled.
    - type: Boolean
      contextPath: GitLab.Project.container_registry_enabled
      description: Whether the container registry is enabled.
    - type: Boolean
      contextPath: GitLab.Project.service_desk_enabled
      description: Whether the service desk is enabled.
    - type: String
      contextPath: GitLab.Project.service_desk_address
      description: The service desk address.
    - type: Boolean
      contextPath: GitLab.Project.can_create_merge_request_in
      description: Whether creating a merge request is an option.
    - type: String
      contextPath: GitLab.Project.issues_access_level
      description: The access level of the issues.
    - type: String
      contextPath: GitLab.Project.repository_access_level
      description: The repository access level.
    - type: String
      contextPath: GitLab.Project.merge_requests_access_level
      description: The merge requests access level.
    - type: String
      contextPath: GitLab.Project.forking_access_level
      description: The forking access level.
    - type: String
      contextPath: GitLab.Project.wiki_access_level
      description: The wiki access level.
    - type: String
      contextPath: GitLab.Project.builds_access_level
      description: The builds access level.
    - type: String
      contextPath: GitLab.Project.snippets_access_level
      description: The snippets access level.
    - type: String
      contextPath: GitLab.Project.pages_access_level
      description: The pages access level.
    - type: String
      contextPath: GitLab.Project.operations_access_level
      description: The operations access level.
    - type: String
      contextPath: GitLab.Project.analytics_access_level
      description: The analytics access level.
    - type: String
      contextPath: GitLab.Project.container_registry_access_level
      description: The container registry access level.
    - type: String
      contextPath: GitLab.Project.security_and_compliance_access_level
      description: The security and compliance access level.
    - type: Unknown
      contextPath: GitLab.Project.emails_disabled
      description: Whether the emails are disabled.
    - type: Boolean
      contextPath: GitLab.Project.shared_runners_enabled
      description: Whether shared runners is enabled.
    - type: Boolean
      contextPath: GitLab.Project.lfs_enabled
      description: Whether LFS is enabled.
    - type: Number
      contextPath: GitLab.Project.creator_id
      description: The creator ID.
    - type: Unknown
      contextPath: GitLab.Project.import_url
      description: The import URL of the list project.
    - type: Unknown
      contextPath: GitLab.Project.import_type
      description: The import type of the list project.
    - type: String
      contextPath: GitLab.Project.import_status
      description: The import status.
    - type: Number
      contextPath: GitLab.Project.open_issues_count
      description: The number of open issues.
    - type: Number
      contextPath: GitLab.Project.ci_default_git_depth
      description: The CI default git depth value.
    - type: Boolean
      contextPath: GitLab.Project.ci_forward_deployment_enabled
      description: Whether the CI forward deployment is enabled.
    - type: Boolean
      contextPath: GitLab.Project.ci_job_token_scope_enabled
      description: Whether the CI job token scope is enabled.
    - type: Boolean
      contextPath: GitLab.Project.ci_separated_caches
      description: Whether the CI separated caches is enabled.
    - type: Boolean
      contextPath: GitLab.Project.public_jobs
      description: Whether there are public jobs in the project.
    - type: Number
      contextPath: GitLab.Project.build_timeout
      description: The timeout of the build.
    - type: String
      contextPath: GitLab.Project.auto_cancel_pending_pipelines
      description: Whether the auto cancel pending pipelines is enabled.
    - type: String
      contextPath: GitLab.Project.ci_config_path
      description: The CI config path of the project list.
    - type: Boolean
      contextPath: GitLab.Project.only_allow_merge_if_pipeline_succeeds
      description: Whether to only allow merges if the pipeline succeeds.
    - type: Boolean
      contextPath: GitLab.Project.ci_allow_fork_pipelines_to_run_in_parent_project
      description: Whether to allow fork pipelines to run in parent project.
    - type: Unknown
      contextPath: GitLab.Project.allow_merge_on_skipped_pipeline
      description: Whether to allow a merge on a skipped pipeline.
    - type: Boolean
      contextPath: GitLab.Project.restrict_user_defined_variables
      description: Whether to restrict user defined variables.
    - type: Boolean
      contextPath: GitLab.Project.request_access_enabled
      description: Whether to enable access requests.
    - type: Boolean
      contextPath: GitLab.Project.only_allow_merge_if_all_discussions_are_resolved
      description: Whether to only allow a merge if all discussions are resolved.
    - type: Boolean
      contextPath: GitLab.Project.remove_source_branch_after_merge
      description: Whether to remove the source branch after a merge.
    - type: Boolean
      contextPath: GitLab.Project.printing_merge_request_link_enabled
      description: Whether a printing merge request link is enabled.
    - type: String
      contextPath: GitLab.Project.merge_method
      description: The merge method.
    - type: String
      contextPath: GitLab.Project.squash_option
      description: The squash option.
    - type: Boolean
      contextPath: GitLab.Project.enforce_auth_checks_on_uploads
      description: Whether a printing merge request link is enabled.
    - type: Unknown
      contextPath: GitLab.Project.suggestion_commit_message
      description: Suggestion commit message.
    - type: Unknown
      contextPath: GitLab.Project.merge_commit_template
      description: The merge commit template.
    - type: Unknown
      contextPath: GitLab.Project.squash_commit_template
      description: The squash commit template.
    - type: Boolean
      contextPath: GitLab.Project.auto_devops_enabled
      description: Whether to default to the Auto DevOps pipeline for all projects within this group.
    - type: String
      contextPath: GitLab.Project.auto_devops_deploy_strategy
      description: The auto devops deploy strategy.
    - type: Boolean
      contextPath: GitLab.Project.autoclose_referenced_issues
      description: Whether to autoclose the referenced issues.
    - type: Boolean
      contextPath: GitLab.Project.keep_latest_artifact
      description: Whether to keep the latest artifact.
    - type: Unknown
      contextPath: GitLab.Project.runner_token_expiration_interval
      description: The runner token expiration interval.
    - type: String
      contextPath: GitLab.Project.external_authorization_classification_label
      description: The external authorization classification label.
    - type: Boolean
      contextPath: GitLab.Project.requirements_enabled
      description: Whether the requirements are enabled.
    - type: String
      contextPath: GitLab.Project.requirements_access_level
      description: The requirements access level.
    - type: Boolean
      contextPath: GitLab.Project.security_and_compliance_enabled
      description: The requirements access level.
    - type: Number
      contextPath: GitLab.Project.permissions.project_access.access_level
      description: The access level of the project.
    - type: Unknown
      contextPath: GitLab.Project.permissions.project_access.notification_level
      description: The project notification level.
    - type: Number
      contextPath: GitLab.Project.permissions.group_access.access_level
      description: The access level of the group level.
    - type: Number
      contextPath: GitLab.Project.permissions.group_access.notification_level
      description: The notification level of the group(permissions).
    - type: Unknown
      contextPath: GitLab.Project.permissions.project_access
      description: The project access (permissions).
  - name: gitlab-group-project-list
    description: Get the list of projects of a given group.
    arguments:
    - description: Group ID from which to retrieve the projects.
      name: group_id
      required: true
    - defaultValue: 50
      description: Total number of projects for display.
      name: limit
    - defaultValue: 1
      description: The page number of the results to retrieve. Minimum value is 1.
      name: page
    outputs:
    - type: Number
      contextPath: GitLab.GroupProject.id
      description: The project's ID.
    - type: String
      contextPath: GitLab.GroupProject.description
      description: The project's description.
    - type: String
      contextPath: GitLab.GroupProject.name
      description: The project's name.
    - type: String
      contextPath: GitLab.GroupProject.name_with_namespace
      description: The project's name with namespace.
    - type: String
      contextPath: GitLab.GroupProject.path
      description: The file's path.
    - type: String
      contextPath: GitLab.GroupProject.path_with_namespace
      description: The project's path with namespace.
    - type: Date
      contextPath: GitLab.GroupProject.created_at
      description: The project date of creation.
    - type: String
      contextPath: GitLab.GroupProject.default_branch
      description: The project default branch.
    - type: String
      contextPath: GitLab.GroupProject.ssh_url_to_repo
      description: Group project SSH URL to repository.
    - type: String
      contextPath: GitLab.GroupProject.http_url_to_repo
      description: Group project HTTP URL to repository.
    - type: String
      contextPath: GitLab.GroupProject.web_url
      description: Group project web URL.
    - type: String
      contextPath: GitLab.GroupProject.readme_url
      description: Group project readme URL.
    - type: Unknown
      contextPath: GitLab.GroupProject.avatar_url
      description: Group project avatar URL.
    - type: Number
      contextPath: GitLab.GroupProject.forks_count
      description: Group project web url forks count.
    - type: Number
      contextPath: GitLab.GroupProject.star_count
      description: Group project web URL star count.
    - type: Date
      contextPath: GitLab.GroupProject.last_activity_at
      description: The date of the last activity of the project.
    - type: Number
      contextPath: GitLab.GroupProject.namespace.id
      description: The namespace ID.
    - type: String
      contextPath: GitLab.GroupProject.namespace.name
      description: The namespace name.
    - type: String
      contextPath: GitLab.GroupProject.namespace.path
      description: The namespace path.
    - type: String
      contextPath: GitLab.GroupProject.namespace.kind
      description: The namespace kind.
    - type: String
      contextPath: GitLab.GroupProject.namespace.full_path
      description: The full path of the namespace.
    - type: Unknown
      contextPath: GitLab.GroupProject.namespace.parent_id
      description: The parent ID.
    - type: Unknown
      contextPath: GitLab.GroupProject.namespace.avatar_url
      description: The avatar URL.
    - type: String
      contextPath: GitLab.GroupProject.namespace.web_url
      description: The name web URL.
    - type: String
      contextPath: GitLab.GroupProject.container_registry_image_prefix
      description: The container registry image prefix.
    - type: String
      contextPath: GitLab.GroupProject._links.self
      description: The group project's links.
    - type: String
      contextPath: GitLab.GroupProject._links.issues
      description: The group project's issues.
    - type: String
      contextPath: GitLab.GroupProject._links.merge_requests
      description: The group project's merge requests.
    - type: String
      contextPath: GitLab.GroupProject._links.repo_branches
      description: The group project's repository branches.
    - type: String
      contextPath: GitLab.GroupProject._links.labels
      description: The group project's links labels.
    - type: String
      contextPath: GitLab.GroupProject._links.events
      description: The group project's links events.
    - type: String
      contextPath: GitLab.GroupProject._links.members
      description: The group project's links members.
    - type: String
      contextPath: GitLab.GroupProject._links.cluster_agents
      description: The group project's links cluster agents.
    - type: Boolean
      contextPath: GitLab.GroupProject.packages_enabled
      description: Whether the group project's packages are enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.empty_repo
      description: Whether the group project's repository is empty.
    - type: Boolean
      contextPath: GitLab.GroupProject.archived
      description: Whether the group project is archived.
    - type: String
      contextPath: GitLab.GroupProject.visibility
      description: The group project's visibility.
    - type: Boolean
      contextPath: GitLab.GroupProject.resolve_outdated_diff_discussions
      description: Whether the group project's outdated different discussions were resolved.
    - type: String
      contextPath: GitLab.GroupProject.container_expiration_policy.cadence
      description: The group project's container expiration policy cadence.
    - type: Boolean
      contextPath: GitLab.GroupProject.container_expiration_policy.enabled
      description: Whether the group project's container expiration policy is enabled.
    - type: Number
      contextPath: GitLab.GroupProject.container_expiration_policy.keep_n
      description: Whether to keep the group project expiration policy.
    - type: String
      contextPath: GitLab.GroupProject.container_expiration_policy.older_than
      description: Whether the group project expiration policy is older than X.
    - type: String
      contextPath: GitLab.GroupProject.container_expiration_policy.name_regex
      description: The group project expiration policy regex name.
    - type: String
      contextPath: GitLab.GroupProject.container_expiration_policy.name_regex_keep
      description: Whether to keep the group project expiration policy regex name.
    - type: Date
      contextPath: GitLab.GroupProject.container_expiration_policy.next_run_at
      description: The date on which the group project expiration policy will run next.
    - type: Boolean
      contextPath: GitLab.GroupProject.issues_enabled
      description: Whether to enabled issues.
    - type: Boolean
      contextPath: GitLab.GroupProject.merge_requests_enabled
      description: Whether to enable merge requests.
    - type: Boolean
      contextPath: GitLab.GroupProject.wiki_enabled
      description: Whether wiki is enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.jobs_enabled
      description: Whether jobs are enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.snippets_enabled
      description: Whether snippets are enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.container_registry_enabled
      description: Whether the container registry is enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.service_desk_enabled
      description: Whether service desk is enabled.
    - type: String
      contextPath: GitLab.GroupProject.service_desk_address
      description: The group project service desk address.
    - type: Boolean
      contextPath: GitLab.GroupProject.can_create_merge_request_in
      description: Whether creating a merge request is an option.
    - type: String
      contextPath: GitLab.GroupProject.issues_access_level
      description: The group project issues access level.
    - type: String
      contextPath: GitLab.GroupProject.repository_access_level
      description: The group project repository access level.
    - type: String
      contextPath: GitLab.GroupProject.merge_requests_access_level
      description: The group project merge requests access level.
    - type: String
      contextPath: GitLab.GroupProject.forking_access_level
      description: The group project forking access level.
    - type: String
      contextPath: GitLab.GroupProject.wiki_access_level
      description: The group project wiki access level.
    - type: String
      contextPath: GitLab.GroupProject.builds_access_level
      description: The group project builds access level.
    - type: String
      contextPath: GitLab.GroupProject.snippets_access_level
      description: The group project snippets access level.
    - type: String
      contextPath: GitLab.GroupProject.pages_access_level
      description: The group project pages access level.
    - type: String
      contextPath: GitLab.GroupProject.operations_access_level
      description: The group project operations access level.
    - type: String
      contextPath: GitLab.GroupProject.analytics_access_level
      description: The group project analytics access level.
    - type: String
      contextPath: GitLab.GroupProject.container_registry_access_level
      description: The group project container registry access level.
    - type: String
      contextPath: GitLab.GroupProject.security_and_compliance_access_level
      description: The group project security and compliance access level.
    - type: Unknown
      contextPath: GitLab.GroupProject.emails_disabled
      description: Whether group project emails are disabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.shared_runners_enabled
      description: Whether group project shared runners are enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.lfs_enabled
      description: Whether group project LFS are enabled.
    - type: Number
      contextPath: GitLab.GroupProject.creator_id
      description: The group project creator ID.
    - type: string
      contextPath: GitLab.GroupProject.import_url
      description: The group project import URL.
    - type: Unknown
      contextPath: GitLab.GroupProject.import_type
      description: The group project import type.
    - type: String
      contextPath: GitLab.GroupProject.import_status
      description: The group project import status.
    - type: Number
      contextPath: GitLab.GroupProject.open_issues_count
      description: The group project issues count.
    - type: Number
      contextPath: GitLab.GroupProject.ci_default_git_depth
      description: The group project default git depth.
    - type: Boolean
      contextPath: GitLab.GroupProject.ci_forward_deployment_enabled
      description: Whether the CI forward deployment are  enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.ci_job_token_scope_enabled
      description: Whether the group project CI job token scope is enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.ci_separated_caches
      description: Whether the group project CI separated caches.
    - type: Boolean
      contextPath: GitLab.GroupProject.ci_opt_in_jwt
      description: Whether the group project CI opt in JWT value.
    - type: Boolean
      contextPath: GitLab.GroupProject.ci_allow_fork_pipelines_to_run_in_parent_project
      description: Whether group project CI allows fork pipelines to run in a parent project.
    - type: Boolean
      contextPath: GitLab.GroupProject.public_jobs
      description: Whether the group project imports public jobs.
    - type: Number
      contextPath: GitLab.GroupProject.build_timeout
      description: The group project build timeout.
    - type: String
      contextPath: GitLab.GroupProject.auto_cancel_pending_pipelines
      description: The group project auto cancel pending pipelines.
    - type: String
      contextPath: GitLab.GroupProject.ci_config_path
      description: The group project CI config path.
    - type: Boolean
      contextPath: GitLab.GroupProject.only_allow_merge_if_pipeline_succeeds
      description: Whether to only allow a merge if the pipeline succeeds.
    - type: Boolean
      contextPath: GitLab.GroupProject.allow_merge_on_skipped_pipeline
      description: Whether to only allow a merge on a skipped pipeline.
    - type: Boolean
      contextPath: GitLab.GroupProject.restrict_user_defined_variables
      description: Whether the group project restricts user defined variables.
    - type: Boolean
      contextPath: GitLab.GroupProject.request_access_enabled
      description: Whether the request access is enabled.
    - type: Boolean
      contextPath: GitLab.GroupProject.only_allow_merge_if_all_discussions_are_resolved
      description: Whether to only allow a merge if all discussions are resolved.
    - type: Boolean
      contextPath: GitLab.GroupProject.remove_source_branch_after_merge
      description: Whether to remove a source branch after a merge.
    - type: Boolean
      contextPath: GitLab.GroupProject.printing_merge_request_link_enabled
      description: Whether the printing merge request link is enabled.
    - type: String
      contextPath: GitLab.GroupProject.merge_method
      description: The merge method.
    - type: String
      contextPath: GitLab.GroupProject.squash_option
      description: The squash option.
    - type: Boolean
      contextPath: GitLab.GroupProject.enforce_auth_checks_on_uploads
      description: Whether to enforce authentication checks on uploads.
    - type: Unknown
      contextPath: GitLab.GroupProject.suggestion_commit_message
      description: The suggestion commit message.
    - type: Unknown
      contextPath: GitLab.GroupProject.merge_commit_template
      description: The merge commit template.
    - type: Unknown
      contextPath: GitLab.GroupProject.squash_commit_template
      description: The squash commit template.
    - type: Boolean
      contextPath: GitLab.GroupProject.auto_devops_enabled
      description: Whether to default to the Auto DevOps pipeline for all projects within this group.
    - type: String
      contextPath: GitLab.GroupProject.auto_devops_deploy_strategy
      description: The auto devops deploy strategy.
    - type: Boolean
      contextPath: GitLab.GroupProject.autoclose_referenced_issues
      description: Whether to autoclose the referenced issues.
    - type: Boolean
      contextPath: GitLab.GroupProject.keep_latest_artifact
      description: Whether keeping the latest artifact is enabled.
    - type: Unknown
      contextPath: GitLab.GroupProject.runner_token_expiration_interval
      description: The runner token expiration interval.
    - type: String
      contextPath: GitLab.GroupProject.external_authorization_classification_label
      description: The external authorization classification label.
    - type: Boolean
      contextPath: GitLab.GroupProject.requirements_enabled
      description: Whether the requirements are enabled.
    - type: String
      contextPath: GitLab.GroupProject.requirements_access_level
      description: The requirements access level.
    - type: Boolean
      contextPath: GitLab.GroupProject.security_and_compliance_enabled
      description: Whether the security and compliance is enabled.
  - name: gitlab-raw-file-get
    description: Get the file in a raw format.
    arguments:
    - description: The file path.
      name: file_path
      required: true
    - description: The branch to retrieve the file from. Default is master.
      name: ref
    outputs:
    - contextPath: GitLab.File.ref
      description: The branch the file's content was taken from.
      type: String
    - contextPath: GitLab.File.path
      description: The file path.
      type: String
    - contextPath: GitLab.File.content
      description: The file content.
      type: String
  - name: gitlab-branch-create
    description: Creates a new branch in the repository.
    arguments:
    - description: The name of the branch.
      name: branch
      required: true
    - description: Branch name, or commit SHA to create a branch from.
      name: ref
      required: true
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - contextPath: GitLab.Branch.commit.author_email
      description: The branch commit's author's email.
      type: String
    - contextPath: GitLab.Branch.commit.author_name
      description: The branch commit's author's name.
      type: String
    - contextPath: GitLab.Branch.commit.authored_date
      description: The branch commit's authored date.
      type: Date
    - contextPath: GitLab.Branch.commit.committed_date
      description: The branch commit's committed date.
      type: Date
    - contextPath: GitLab.Branch.commit.committer_email
      description: The branch commit's committer email.
      type: String
    - contextPath: GitLab.Branch.commit.committer_name
      description: The branch commit's committer name.
      type: String
    - contextPath: GitLab.Branch.commit.id
      description: The branch commit's ID.
      type: String
    - contextPath: GitLab.Branch.commit.short_id
      description: The branch commit's short ID.
      type: String
    - contextPath: GitLab.Branch.commit.title
      description: The branch commit's title.
      type: String
    - contextPath: GitLab.Branch.commit.message
      description: The branch commit's message.
      type: String
    - contextPath: GitLab.Branch.commit.parent_ids
      description: The branch commit's parent ID.
      type: String
    - contextPath: GitLab.Branch.name
      description: The branch's name.
      type: String
    - contextPath: GitLab.Branch.merged
      description: Whether the branch was merged.
      type: Boolean
    - contextPath: GitLab.Branch.protected
      description: Whether the branch is protected.
      type: Boolean
    - contextPath: GitLab.Branch.default
      description: Whether the branch is the default branch.
      type: Boolean
    - contextPath: GitLab.Branch.developers_can_push
      description: Whether the branch's developers can push.
      type: Boolean
    - contextPath: GitLab.Branch.developers_can_merge
      description: Whether the branch's developers can merge.
      type: Boolean
    - contextPath: GitLab.Branch.can_push
      description: Whether push is possible.
      type: Boolean
    - contextPath: GitLab.Branch.web_url
      description: The branch's web URL.
      type: String
  - name: gitlab-branch-delete
    description: Deletes a branch from the repository.
    arguments:
    - description: The name of the branch.
      name: branch
      required: true
  - description: Deletes all branches that are merged into the project's default branch.
    name: gitlab-merged-branch-delete
  - name: gitlab-branch-list
    description: Get a list of repository branches from a project, alphabetically sorted by name.
    arguments:
    - name: branch_name
      description: Optional URL parameter. If you provide a value, a single branch will be returned.
    - name: search
      description: Returns a list of branches containing the search string. You can use ^term and term$ to find branches that begin and end with term respectively.
    - defaultValue: 50
      name: limit
      description: Maximum number of results to return.
    - defaultValue: 1
      name: page
      description: The page number of the results to retrieve. Minimum value is 1.
    - auto: PREDEFINED
      name: partial_response
      defaultValue: 'true'
      description: Return a partial API response in context data if true, otherwise returns full API response.
      predefined:
      - 'true'
      - 'false'
    outputs:
    - contextPath: GitLab.Branch.name
      description: The branch's name.
      type: String
    - contextPath: GitLab.Branch.merged
      description: Whether the branch was merged.
      type: Boolean
    - contextPath: GitLab.Branch.protected
      description: Whether the branch is protected.
      type: Boolean
    - contextPath: GitLab.Branch.default
      description: Whether the branch is the default branch of the project.
      type: Boolean
    - contextPath: GitLab.Branch.developers_can_push
      description: Whether developers can push to the branch.
      type: Boolean
    - contextPath: GitLab.Branch.developers_can_merge
      description: Whether developers can merge.
      type: Boolean
    - contextPath: GitLab.Branch.can_push
      description: Whether the branch can be pushed.
      type: Boolean
    - contextPath: GitLab.Branch.web_url
      description: The branch web URL.
      type: String
    - contextPath: GitLab.Branch.commit.author_email
      description: The branch commit's author's email.
      type: String
    - contextPath: GitLab.Branch.commit.author_name
      description: The branch commit's author's name.
      type: String
    - contextPath: GitLab.Branch.commit.authored_date
      description: The branch commit's authored date.
      type: Date
    - contextPath: GitLab.Branch.commit.committed_date
      description: The branch commit's committed date.
      type: Date
    - contextPath: GitLab.Branch.commit.committer_email
      description: The branch commit's committer's email.
      type: String
    - contextPath: GitLab.Branch.commit.committer_name
      description: The branch commit's committer's name.
      type: String
    - contextPath: GitLab.Branch.commit.id
      description: The branch commit's ID.
      type: String
    - contextPath: GitLab.Branch.commit.short_id
      description: The branch commit's short ID.
      type: String
    - contextPath: GitLab.Branch.commit.title
      description: The branch commit's title.
      type: String
    - contextPath: GitLab.Branch.commit.message
      description: The branch commit's message.
      type: String
    - contextPath: GitLab.Branch.commit.parent_ids
      description: The branch commit's parent IDs.
      type: String
  - name: gitlab-group-list
    description: Get a list of visible groups for the authenticated user. When accessed without authentication, only public groups are returned. By default, this request returns 20 results at a time because the API results are paginated.
    arguments:
    - name: skip_groups
      description: A comma-separated list of group IDs to skip.
      isArray: true
    - name: all_available
      description: Show all the groups you have access to. (Defaults to false for authenticated users, true for administrators).
    - name: search
      description: Return the list of authorized groups matching the search criteria.
    - auto: PREDEFINED
      defaultValue: name
      name: order_by
      description: The method used to order the retrieved issues.
      predefined:
      - name
      - path
      - similarity
    - auto: PREDEFINED
      defaultValue: asc
      name: sort
      description: Order groups in asc or desc order.
      predefined:
      - asc
      - desc
    - name: owned
      description: Limit to groups explicitly owned by the current user.
      type: Boolean
    - name: min_access_level
      description: Limit to groups where the current user has at least this access level.
    - name: top_level_only
      description: Limit to top level groups, excluding all subgroups.
    - defaultValue: 50
      name: limit
      description: Maximum number of results to return.
    - defaultValue: 1
      name: page
      description: The page number of the results to retrieve. Minimum value is 1.
    outputs:
    - contextPath: GitLab.Group.id
      description: The group's ID.
      type: Number
    - contextPath: GitLab.Group.name
      description: The group's name.
      type: String
    - contextPath: GitLab.Group.path
      description: The group's path.
      type: String
    - contextPath: GitLab.Group.description
      description: The group's description.
      type: String
    - contextPath: GitLab.Group.visibility
      description: The group's visibility.
      type: String
    - contextPath: GitLab.Group.share_with_group_lock
      description: Whether to prevent sharing a project with another group within this group.
      type: Boolean
    - contextPath: GitLab.Group.require_two_factor_authentication
      description: Whether the group requires two factor authentication.
      type: Boolean
    - contextPath: GitLab.Group.two_factor_grace_period
      description: The group's two factor grace period.
      type: Number
    - contextPath: GitLab.Group.project_creation_level
      description: The group's project's creation level.
      type: String
    - contextPath: GitLab.Group.auto_devops_enabled
      description: Whether to default to the Auto DevOps pipeline for all projects within this group.
      type: Unknown
    - contextPath: GitLab.Group.subgroup_creation_level
      description: The group's subgroup creation level.
      type: String
    - contextPath: GitLab.Group.emails_disabled
      description: Whether the group's emails are disabled.
      type: Unknown
    - contextPath: GitLab.Group.mentions_disabled
      description: Whether the group mentions are disabled.
      type: Unknown
    - contextPath: GitLab.Group.lfs_enabled
      description: Whether the group LFS are enabled.
      type: Boolean
    - contextPath: GitLab.Group.default_branch_protection
      description: The group's default branch protection.
      type: Number
    - contextPath: GitLab.Group.avatar_url
      description: The group's avatar URL.
      type: String
    - contextPath: GitLab.Group.web_url
      description: The group's web URL.
      type: String
    - contextPath: GitLab.Group.request_access_enabled
      description: Whether the group's request access is enabled.
      type: Boolean
    - contextPath: GitLab.Group.full_name
      description: The group's full name.
      type: String
    - contextPath: GitLab.Group.full_path
      description: The group's full path.
      type: String
    - contextPath: GitLab.Group.file_template_project_id
      description: The group's file template project ID.
      type: Number
    - contextPath: GitLab.Group.parent_id
      description: The group's parent ID.
      type: Unknown
    - contextPath: GitLab.Group.created_at
      description: The group's creation time.
      type: Date
  - name: gitlab-group-member-list
    description: Gets a list of group or project members viewable by the authenticated user.
    arguments:
    - name: group_id
      description: The ID of the group owned by the authenticated user.
      required: true
      isArray: true
    outputs:
    - contextPath: GitLab.Group.id
      description: The ID of the group owned by the authenticated user.
      type: Number
    - contextPath: GitLab.Group.username
      description: The user's username.
      type: String
    - contextPath: GitLab.Group.name
      description: The user's name.
      type: String
    - contextPath: GitLab.Group.state
      description: The group's state.
      type: String
    - contextPath: GitLab.Group.avatar_url
      description: The group's avatar URL.
      type: String
    - contextPath: GitLab.Group.web_url
      description: The group's web URL.
      type: String
    - contextPath: GitLab.Group.created_at
      description: The group's creation time.
      type: Date
    - contextPath: GitLab.Group.created_by.id
      description: The group's creator's ID.
      type: Number
    - contextPath: GitLab.Group.created_by.username
      description: The group's creator's username.
      type: String
    - contextPath: GitLab.Group.created_by.name
      description: The group's creator's name.
      type: String
    - contextPath: GitLab.Group.created_by.state
      description: The group's creator's state.
      type: String
    - contextPath: GitLab.Group.created_by.avatar_url
      description: The group's creator's avatar URL.
      type: String
    - contextPath: GitLab.Group.created_by.web_url
      description: The group's creator's web url.
      type: String
    - contextPath: GitLab.Group.expires_at
      description: The group's expiration time.
      type: Date
    - contextPath: GitLab.Group.access_level
      description: The group's access level.
      type: Number
    - contextPath: GitLab.Group.group_saml_identity.extern_uid
      description: The group SAML identity extern UID.
      type: String
    - contextPath: GitLab.Group.group_saml_identity.provider
      description: The group SAML identity provider.
      type: String
    - contextPath: GitLab.Group.group_saml_identity.saml_provider_id
      description: The group SAML identity provider ID.
      type: Number
    - contextPath: GitLab.Group.email
      description: The group member email.
      type: String
  - name: gitlab-code-search
    description: Using Scope blobs. Blob searches are performed on both filenames and contents.
    arguments:
    - name: search
      description: The search query.
      required: true
      isArray: true
    - defaultValue: 50
      name: limit
      description: Maximum number of results to return.
    - defaultValue: 1
      name: page
      description: The page number of the results to retrieve. Minimum value is 1.
    outputs:
    - contextPath: GitLab.Code.basename
      description: The base name of the code.
      type: String
    - contextPath: GitLab.Code.data
      description: Data of the code.
      type: String
    - contextPath: GitLab.Code.path
      description: The code's path.
      type: String
    - contextPath: GitLab.Code.filename
      description: the name of the file.
      type: String
    - contextPath: GitLab.Code.id
      description: The code ID.
      type: Unknown
    - contextPath: GitLab.Code.ref
      description: Branch name.
      type: String
    - contextPath: GitLab.Code.startline
      description: The line in which the search code begins.
      type: Number
    - contextPath: GitLab.Code.project_id
      description: The project's ID.
      type: Number
  - name: gitlab-project-user-list
    description: Get the users list of a project.
    arguments:
    - name: search
      description: The search query.
      isArray: true
    - defaultValue: 50
      name: limit
      description: Maximum number of results to return.
    - defaultValue: 1
      name: page
      description: The page number of the results to retrieve. Minimum value is 1.
    outputs:
    - contextPath: GitLab.User.id
      description: The user's ID.
      type: Number
    - contextPath: GitLab.User.username
      description: The user's username.
      type: String
    - contextPath: GitLab.User.name
      description: The user's name.
      type: String
    - contextPath: GitLab.User.state
      description: The user's state.
      type: String
    - contextPath: GitLab.User.avatar_url
      description: The user's avatar URL.
      type: String
    - contextPath: GitLab.User.web_url
      description: The user's web URL.
      type: String
  - name: gitlab-pipelines-list
    arguments:
    - description: Project ID from which to retrieve pipelines.
      name: project_id
    - description: ID of specific pipeline from which to retrieve its details.
      name: pipeline_id
    - description: Reference name of the pipelines, e.g., 'master'.
      name: ref
    - auto: PREDEFINED
      description: Retrieves pipelines of which status matches the given status.
      name: status
      predefined:
      - 'created'
      - 'waiting_for_resource'
      - 'preparing'
      - 'pending'
      - 'running'
      - 'success'
      - 'failed'
      - 'canceled'
      - 'skipped'
      - 'manual'
      - 'scheduled'
    description: Gets the details of the pipelines.
    outputs:
    - contextPath: GitLab.Pipeline.id
      description: Pipeline ID.
      type: Number
    - contextPath: GitLab.Pipeline.project_id
      description: Project ID that the pipeline belongs to.
      type: Number
    - contextPath: GitLab.Pipeline.status
      description: Status of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.ref
      description: Reference of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.sha
      description: SHA of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.created_at
      description: Time when the pipeline was created.
      type: Date
    - contextPath: GitLab.Pipeline.updated_at
      description: Time when the pipeline was last updated.
      type: Date
    - contextPath: GitLab.Pipeline.started_at
      description: Time when the pipeline was started.
      type: Date
    - contextPath: GitLab.Pipeline.finished_at
      description: Time when the pipeline was finished.
      type: Date
    - contextPath: GitLab.Pipeline.duration
      description: Duration of the pipeline in seconds.
      type: Number
    - contextPath: GitLab.Pipeline.web_url
      description: Web URL of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.name
      description: Name of the user who triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.username
      description: Username that triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.id
      description: ID of the user who triggered the pipeline.
      type: Number
    - contextPath: GitLab.Pipeline.user.state
      description: State of the user who triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.avatar_url
      description: Avatar URL of the user who trigerred the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.web_url
      description: Web URL of the user who triggered the pipeline.
      type: String
  - name: gitlab-pipelines-schedules-list
    arguments:
    - description: Project ID from which to retrieve pipeline schedules.
      name: project_id
    - description: ID of the specific pipeline schedule from which to retrieve its details.
      name: pipeline_schedule_id
    description: Gets the details of the pipeline schedules.
    outputs:
    - contextPath: GitLab.PipelineSchedule.id
      description: Pipeline schedule ID.
      type: Number
    - contextPath: GitLab.PipelineSchedule.description
      description: Pipeline schedule description.
      type: String
    - contextPath: GitLab.PipelineSchedule.ref
      description: Pipeline schedule reference.
      type: String
    - contextPath: GitLab.PipelineSchedule.next_run_at
      description: Pipeline schedule next run scheduled time.
      type: Date
    - contextPath: GitLab.PipelineSchedule.active
      description: Whether pipeline schedule is active.
      type: Boolean
    - contextPath: GitLab.PipelineSchedule.created_at
      description: When pipeline schedule was created.
      type: Date
    - contextPath: GitLab.PipelineSchedule.updated_at
      description: When the pipeline schedule was last updated.
      type: Date
    - contextPath: GitLab.PipelineSchedule.last_pipeline.id
      description: ID of the last pipeline that was run by the scheduled pipeline. Relevant only when the pipeline schedule ID is given.
      type: Number
    - contextPath: GitLab.PipelineSchedule.last_pipeline.sha
      description: SHA of the last pipeline that was run by the scheduled pipeline. Relevant only when the pipeline schedule ID is given.
      type: String
    - contextPath: GitLab.PipelineSchedule.last_pipeline.ref
      description: Reference of the last pipeline that was run by the scheduled pipeline. Relevant only when the pipeline schedule ID is given.
      type: String
    - contextPath: GitLab.PipelineSchedule.last_pipeline.status
      description: Status of the last pipeline that was run by the scheduled pipeline. Relevant only when the pipeline schedule ID is given.
      type: String
  - name: gitlab-jobs-list
    arguments:
    - description: Project ID from which to retrieve jobs details.
      name: project_id
    - description: ID of the pipeline from which to retrieve its jobs.
      name: pipeline_id
      required: true
    description: Gets job details.
    outputs:
    - contextPath: GitLab.Job.status
      description: The status of the job.
      type: String
    - contextPath: GitLab.Job.created_at
      description: Time the job was created.
      type: Date
    - contextPath: GitLab.Job.started_at
      description: Time the job was started.
      type: Date
    - contextPath: GitLab.Job.finished_at
      description: Time the job was finished.
      type: Date
    - contextPath: GitLab.Job.duration
      description: Duration of the job in seconds.
      type: Number
    - contextPath: GitLab.Job.id
      description: ID of the job.
      type: Number
    - contextPath: GitLab.Job.name
      description: Name of the job.
      type: String
    - contextPath: GitLab.Job.pipeline.id
      description: Pipeline the job belongs to.
      type: Number
    - contextPath: GitLab.Job.pipeline.project_id
      description: Project ID the job belongs to.
      type: Number
    - contextPath: GitLab.Job.pipeline.ref
      description: Reference of the pipeline the job belongs to.
      type: String
    - contextPath: GitLab.Job.pipeline.sha
      description: SHA of the pipeline the job belongs to.
      type: String
    - contextPath: GitLab.Job.pipeline.status
      description: Status of the pipeline the job belongs to.
      type: String
    - contextPath: GitLab.Job.ref
      description: Reference name of the job.
      type: String
    - contextPath: GitLab.Job.stage
      description: Stage of the job.
      type: String
    - contextPath: GitLab.Job.web_url
      description: Web URL of the job.
      type: String
  - name: gitlab-artifact-get
    arguments:
    - description: Project ID from which to retrieve an artifact.
      name: project_id
    - description: ID of a specific job from which to retrieve its artifact.
      name: job_id
      required: true
    - description: Suffix to the path of an artifact from which to retrieve its data.
      name: artifact_path_suffix
      required: true
    description: Gets an artifact from a given artifact path, corresponding to a given job ID.
    outputs:
    - contextPath: GitLab.Artifact.job_id
      description: Job ID from which the artifact was taken.
      type: String
    - contextPath: GitLab.Artifact.artifact_path_suffix
      description: Suffix of the given artifact path.
      type: String
    - contextPath: GitLab.Artifact.artifact_data
      description: Data of the artifact requested.
      type: String
  - name: gitlab-trigger-pipeline
    arguments:
    - description: Project ID on which to run the pipeline.
      name: project_id
    - description: The branch on which to run the pipeline. Default is 'master'.
      name: ref_branch
    - description: JSON containing the pipeline variables.
      name: trigger_variables
    description: Triggers a GitLab pipeline on a selected project and branch.
    outputs:
    - contextPath: GitLab.Pipeline.id
      description: Pipeline ID.
      type: Number
    - contextPath: GitLab.Pipeline.project_id
      description: Project ID that the pipeline belongs to.
      type: Number
    - contextPath: GitLab.Pipeline.status
      description: Status of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.ref
      description: Reference of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.sha
      description: SHA of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.created_at
      description: Time when the pipeline was created.
      type: Date
    - contextPath: GitLab.Pipeline.updated_at
      description: Time when the pipeline was last updated.
      type: Date
    - contextPath: GitLab.Pipeline.started_at
      description: Time when the pipeline was started.
      type: Date
    - contextPath: GitLab.Pipeline.finished_at
      description: Time when the pipeline was finished.
      type: Date
    - contextPath: GitLab.Pipeline.duration
      description: Duration of the pipeline in seconds.
      type: Number
    - contextPath: GitLab.Pipeline.web_url
      description: Web URL of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.name
      description: Name of the user who triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.username
      description: Username that triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.id
      description: ID of the user who triggered the pipeline.
      type: Number
    - contextPath: GitLab.Pipeline.user.state
      description: State of the user who triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.avatar_url
      description: Avatar URL of the user who triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.web_url
      description: Web URL of the user who triggered the pipeline.
      type: String
  - name: gitlab-cancel-pipeline
    arguments:
    - description: Project ID on which to cancel the pipeline.
      name: project_id
    - description: The pipline ID to cancel.
      name: pipeline_id
      required: true
    description: Cancels a GitLab pipeline.
    outputs:
    - contextPath: GitLab.Pipeline.id
      description: Pipeline ID.
      type: Number
    - contextPath: GitLab.Pipeline.project_id
      description: Project ID that the pipeline belongs to.
      type: Number
    - contextPath: GitLab.Pipeline.status
      description: Status of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.ref
      description: Reference of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.sha
      description: SHA of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.created_at
      description: Time when the pipeline was created.
      type: Date
    - contextPath: GitLab.Pipeline.updated_at
      description: Time when the pipeline was last updated.
      type: Date
    - contextPath: GitLab.Pipeline.started_at
      description: Time when the pipeline was started.
      type: Date
    - contextPath: GitLab.Pipeline.finished_at
      description: Time when the pipeline was finished.
      type: Date
    - contextPath: GitLab.Pipeline.duration
      description: Duration of the pipeline in seconds.
      type: Number
    - contextPath: GitLab.Pipeline.web_url
      description: Web URL of the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.name
      description: Name of the user who triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.username
      description: Username that triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.id
      description: ID of the user who triggered the pipeline.
      type: Number
    - contextPath: GitLab.Pipeline.user.state
      description: State of the user who triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.avatar_url
      description: Avatar URL of the user who triggered the pipeline.
      type: String
    - contextPath: GitLab.Pipeline.user.web_url
      description: Web URL of the user who triggered the pipeline.
      type: String
  runonce: false
  script: '-'
  type: python
  subtype: python3
  dockerimage: demisto/python3:3.12.8.1983910
fromversion: 6.5.0
tests:
- Test-GitLab-v2
