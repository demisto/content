category: Utilities
commonfields:
  id: RemoteAccessV2
  version: -1
configuration:
- display: Default Hostname Or IP Address
  name: hostname
  required: true
  type: 0
- display: User
  additionalinfo: For example, "root".
  name: credentials
  type: 9
  required: false
- display: Terminal Type
  defaultvalue: xterm
  name: terminal_type
  required: true
  type: 0
- additionalinfo: TODO
  defaultvalue: 'false'
  display: Interactive Terminal Mode
  hidden: false
  name: interactive_terminal_mode
  required: false
  type: 8
- display: Trust any certificate (not secure)
  name: insecure
  required: false
  type: 8
- display: Use system proxy settings
  name: proxy
  required: false
  type: 8
description: '[Enter a comprehensive, yet concise, description of what the integration
  does, what use cases it is designed for, etc.]'
display: RemoteAccessV2
name: RemoteAccessV2
script:
  commands:
  - arguments:
    - default: false
      description: Command to run on remote machine.
      isArray: false
      name: command
      required: true
      secret: false
    deprecated: false
    description: Run command on remote system with ssh
    execution: false
    name: remote-access-ssh
    outputs:
    - contextPath: RemoteAccess.Command.stdout
      description: STD output of the given command.
      type: String
    - contextPath: RemoteAccess.Command.std_error
      description: STD error output of the given command.
      type: String
  - arguments:
    - default: false
      description: Entry ID of the file to be copied from Cortex XSOAR to remote machine.
      isArray: false
      name: entry_id
      required: true
      secret: false
    - default: false
      description: Destination of the path of the copied file in the remote machine. Defaults to the `entry_id` file path if not given
      isArray: false
      name: destination_path
      required: false
      secret: false
    deprecated: false
    description: Run command on remote system with ssh
    execution: false
    name: remote-access-copy-to
    outputs: []
  - arguments:
    - default: false
      description: Path of the file in remote machine to be copied to Cortex XSOAR.
      isArray: false
      name: file_path
      required: true
      secret: false
    - default: false
      description: Name of the file to be saved in Cortex XSOAR. Defaults to the file name in `file_path` if not given. E.g, if `file_path` is "a/b/c.txt", the file name will be c.txt.
      isArray: false
      name: file_name
      required: false
      secret: false
    deprecated: false
    description: Run command on remote system with ssh
    execution: false
    name: remote-access-copy-from
    outputs: []
  isfetch: false
  runonce: false
  script: '-'
  type: python
  subtype: python3
  dockerimage: demisto/netmiko:1.0.0.15047
fromversion: 5.5.0
tests:
- No tests (auto formatted)
