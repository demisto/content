Collect events from Box's logs.

To configure an instance, provide the Box application's credentials JSON (from your Box custom app's server authentication configuration) in the **Credentials JSON** field.
