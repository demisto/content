#### Integrations

##### DFIRe

- Added `DFIRe_test.py` — a pytest unit-test suite that mocks `Client._http_request` and exercises every command. Tests are parametrised across the lifecycle, bulk-indicator, lifecycle-style POST, and reference-data list families. Regression tests are pinned for each of the v1.1.1–v1.1.3 fixes (`item_id` as UUID string, `case-generate-summary` GET + text response, `case-todo-list` derived from `case.todo_checklist`, `case-todo-note-set` PUT verb, `timeline-create` default `event_datetime`, `investigation-report-{generate,ready-for-qa}` `section_id` argument, `case-update-report` explicit fields, `case-create` `create_slack_channel=false` default).
- Regenerated the integration README from the YAML via `demisto-sdk generate-docs` so all 75 commands now have full arg/output tables, and restored the human-friendly intro section.
- Commented out the `register_module_line` calls so the module can be imported by the test harness; they were not load-bearing.
