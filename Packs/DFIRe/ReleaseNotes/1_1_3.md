#### Integrations

##### DFIRe

- Documented `dfire-attachment-upload`'s `category` argument. `general` (the default) routes uploads to the encrypted file store. `evidence` is for evidence photos and routes to the image gallery instead of the file store — pick it deliberately when uploading photographs, not for arbitrary files.
