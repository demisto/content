#### Integrations

##### ManageEngine

- Fixed an issue where the **fetch-events** command would advance the time cursor even when no events were returned, causing the integration to always query a narrow ~60-second window and miss events with ingestion delay in the ManageEngine API.
