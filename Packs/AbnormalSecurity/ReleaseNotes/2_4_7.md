
#### Integrations

##### Abnormal Security

- **Fetch incidents** now tracks a separate cursor for each data type (threats, abuse campaigns, and account takeover cases), so a failing or lagging data type no longer blocks the others. Each type's cursor advances only when that type completes successfully.
- Added a per-run time budget so a large backlog drains cleanly across runs instead of timing out the Docker container.
