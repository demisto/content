#### Modeling Rules

##### Hydden Mesh Modeling Rule

Aligned the schema with the XDM mapping for `hydden_mesh_raw`. Nested Mesh fields are read from the `mesh` column; flat actor, target, outcome, MFA, and collector columns are declared in the schema.

#### Playbooks

##### Hydden - Blast Radius

Clarified that the playbook takes the account ID from the Cortex issue and returns `Hydden.Identity.blast_radius`.
