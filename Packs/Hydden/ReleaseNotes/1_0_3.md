#### Integrations

##### Hydden Control

`hydden-blast-radius` now reads the Hydden `score` field (and still accepts a flat `blast_radius` value) and writes the full JSON payload into `Hydden.Identity` context, not only the headline string.
