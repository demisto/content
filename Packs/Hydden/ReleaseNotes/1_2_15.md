#### Modeling Rules

##### Hydden Mesh Modeling Rule

Fixed **xdm.event.duration** being populated with **to_float**, which is not valid for an integer XDM field and caused the tenant to reject the modeling rule and fail the whole pack installation. It now uses **to_integer**.
