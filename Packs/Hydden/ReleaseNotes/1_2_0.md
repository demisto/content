#### Playbooks

XSIAM silent-playbook format (`issilent: true`) for Account Probing, Dormant User Login, and Resolve Identity Context.

#### Integrations

##### New: Hydden Control

Resolves Hydden accounts (`hydden-resolve`) and disables them across the identity fabric (`hydden-disable`).

#### XSIAM Dashboards

##### New: Hydden Identity System of Record

XQL dashboard over `hydden_mesh_raw`: new accounts, top events, actions, actors, and targets.
