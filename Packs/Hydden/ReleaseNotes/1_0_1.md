#### Integrations

##### Hydden Control

Commands now authenticate with an OAuth 2.0 client-credentials bearer token from `POST /oauth/token` instead of HTTP basic auth. Tokens are cached until shortly before they expire.
