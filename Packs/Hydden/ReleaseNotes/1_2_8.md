#### Integrations

##### Hydden Control

Replaced the API key parameter with **Client ID** and **Client Secret**.
Fixed the request path, which repeated `v1` when the API URL already ended in `/v1`.
Updated the API URL parameter to the versioned public API root.
