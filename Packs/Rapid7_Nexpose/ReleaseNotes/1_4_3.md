#### Integrations

##### Rapid7 InsightVM

- Fixed an issue where asset commands (e.g. ***nexpose-get-asset***, ***nexpose-get-assets***) could fail with an HTTP 500 error when the InsightVM Security Console returned a gzip-compressed response. The integration now requests uncompressed responses (`Accept-Encoding: identity`).
