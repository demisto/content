## Pack Metadata

- Added `exposure_management` to the pack's `supportedModules` so the pack is available in Cortex platform tenants with the Exposure Management module enabled.

## Integrations

##### Rapid7 InsightVM

- Added `exposure_management` to the integration's `supportedModules` so the integration can be installed and configured on Cortex platform tenants with the Exposure Management module enabled.
