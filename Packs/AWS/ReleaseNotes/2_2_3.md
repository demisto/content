#### Integrations

##### Amazon Web Services

- Added support for the **Enable Control Plane Logging** quick action, which enables control plane logging for an EKS cluster.
- Added support for the **Set EKS cluster to Private / Disable Public Access** quick action, which disables public access to the EKS cluster's API endpoint, making it only accessible from within the cluster's VPC.