
#### Integrations

##### CrowdStrike Falcon

- Fixed an issue where the Spotlight vulnerabilities snapshot was sealed with an empty request, causing the dataset to be marked as updated but remain unqueryable in XQL. The sealing batch now carries real vulnerability records together with the actual total count, so the snapshot is validated correctly.
