
#### Integrations

##### CrowdStrike Falcon

- Fixed an issue where the ***cs-falcon-apply-quarantine-file-action*** command failed with `400 Bad Request - Validation error` when list-typed search arguments (e.g., **filename**) were used. It now returns a clear "no files matched" message when the search yields no results.
