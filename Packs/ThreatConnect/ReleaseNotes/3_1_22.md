#### Integrations

##### ThreatConnect v3

- Fixed an issue where the ***tc-get-indicator*** command displayed raw JSON instead of formatted tables in the War Room when using the *fields_to_return* argument (e.g., `tags`, `attributes`, `associatedGroups`).
