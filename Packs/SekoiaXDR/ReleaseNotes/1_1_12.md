
#### Integrations

##### Sekoia XDR

- **Fixed URL parameter typo** in the `http_request` command to properly support the `url_suffix` parameter
- **Resolved alert loss issue** caused by missing millisecond precision in alert timestamps
- **Fixed Query Events command** that was returning `400 Bad Request` error with message "Earliest time must be anterior to latest time" by improving ISO 8601 timestamp formatting and validation in API requests 