args:
- description: The Netskope.FileScanReport context output from netskopev2-get-scan-report (a dict, or a single-element list if XSOAR wraps it) - bind this directly to ${Netskope.FileScanReport}.
  name: scan_result
  required: true
comment: Builds the setIncident addLabels JSON string from a netskopev2-get-scan-report result (jobid, status, verdict, md5, sha256) - kept separate from the large nested fields (dropped/network/observed_behavior/process_tree) already excluded from the command's own readable output.
commonfields:
  id: NetskopeFileScanLabels
  version: -1
name: NetskopeFileScanLabels
outputs:
- contextPath: NetskopeFileScanLabels.labels_json
  description: JSON string ready to pass as setIncident's addLabels argument.
  type: String
- contextPath: NetskopeFileScanLabels.summary
  description: Human-readable summary of what was recorded.
  type: String
script: ''
type: python
subtype: python3
tags: []
timeout: '0'
dockerimage: demisto/python3:3.12.8.3296088
fromversion: 6.0.0
tests:
- No tests (auto formatted)
