import demistomock as demisto
from CommonServerPython import *  # noqa # pylint: disable=unused-wildcard-import
from CommonServerUserPython import *  # noqa

import urllib3
import traceback
from typing import Dict, Any, List

# Disable insecure warnings
urllib3.disable_warnings()  # pylint: disable=no-member


''' CONSTANTS '''

DATE_FORMAT = '%Y-%m-%dT%H:%M:%SZ'  # ISO8601 format with UTC, default in XSOAR
# Relevant for api version v1
TIME_PERIOD_MAPPING = {
    'Last 60 Minutes': 3600,
    'Last 24 Hours': 86400,
    'Last 7 Days': 604800,
    'Last 30 Days': 2592000,
    'Last 60 Days': 5184000,
    'Last 90 Days': 7776000
}

EVENT_TYPES_V1 = ['application', 'audit', 'network']  # api version - v1
EVENT_TYPES_V2 = ['alert', 'application', 'audit', 'network']  # api version v2


''' CLIENT CLASS '''


class Client(BaseClient):
    """
    Client for Netskope RESTful API.

    Args:
        base_url (str): The base URL of Netskope.
        token (str): The token to authenticate against Netskope API.
        validate_certificate (bool): Specifies whether to verify the SSL certificate or not.
        proxy (bool): Specifies if to use XSOAR proxy settings.
    """

    def __init__(self, base_url: str, token: str, api_version: str, validate_certificate: bool, proxy: bool):
        super().__init__(base_url, verify=validate_certificate, proxy=proxy)
        if api_version == 'v1':
            self._session.params['token'] = token
        else:
            self.headers = {'Netskope-Api-Token': token}

    def get_events_request_v1(self, timeperiod: Optional[int] = None, limit: Optional[int] = None) -> List[Any] | Any:
        """
        Get all events extracted from Saas traffic and or logs.
        Args:
            timeperiod (Optional[int]): Get alerts from certain time period.
            limit (Optional[int]): The maximum number of alerts to return (up to 10000).
        Returns:
            events (list).
        """
        events = []
        url_suffix = 'events'
        for event_type in EVENT_TYPES_V1:
            body = {'timeperiod': timeperiod, 'limit': limit, 'type': event_type}
            response = self._http_request(method='GET', url_suffix=url_suffix, json_data=body)
            if response.get('status') == 'success':
                events.extend(response.get('data', []))

        return events

    def v1_get_alerts_request(self, timeperiod: Optional[int] = None, limit: Optional[int] = None) -> list[Any] | Any:
        """
        Get alerts generated by Netskope, including policy, DLP, and watch list alerts.

        Args:
            timeperiod (Optional[int]): Get alerts from certain time period.
            limit (Optional[int]): The maximum number of alerts to return (up to 10000).

        Returns:
            List[str, Any]: Netskope alerts.
        """

        url_suffix = 'alerts'
        body = {'timeperiod': timeperiod, 'limit': limit}
        response = self._http_request(method='GET', url_suffix=url_suffix, json_data=body)
        if response.get('status') == 'success':
            return response.get('data', [])
        return

    def get_events_request_v2(self, timeperiod: Optional[int] = None, limit: Optional[int] = None,
                              is_test: bool = False) -> List:
        """
        Get all events extracted from Saas traffic and or logs.
        Args:
            timeperiod (Optional[int]): Get alerts from certain time period.
            limit (Optional[int]): The maximum number of alerts to return (up to 10000).
            is_test (bool): if the request runs on test module or not.
        Returns:
            events (list).
        """
        events = []
        params = {'timeperiod': timeperiod, 'limit': limit}
        for event_type in EVENT_TYPES_V2:
            url_suffix = f'events/data/{event_type}'
            response = self._http_request(method='GET', url_suffix=url_suffix, headers=self.headers, params=params)
            if response.get('ok') == 1:
                events.extend(response.get('result', []))
                if is_test:
                    return events
        return events


''' HELPER FUNCTIONS '''


def arg_to_seconds_timestamp(arg: Optional[str]) -> Optional[int]:
    """
    Converts an XSOAR date string argument to a timestamp in seconds.

    Args:
        arg (Optional[str]): The argument to convert.

    Returns:
        Optional[int]: A timestamp if arg can be converted,
        or None if arg is None.
    """

    if arg is None:
        return None

    return date_to_seconds_timestamp(arg_to_datetime(arg))


def date_to_seconds_timestamp(date_str_or_dt: Union[str, datetime]) -> int:
    """
    Converts date string or datetime object to a timestamp in seconds.

    Args:
        date_str_or_dt (Union[str, datetime]): The datestring or datetime.

    Returns:
        int: The timestamp in seconds.
    """

    return date_to_timestamp(date_str_or_dt) // 1000


''' COMMAND FUNCTIONS '''


def test_module(client: Client, api_version: str) -> str:
    if api_version == 'v1':
        response = client.v1_get_alerts_request(limit=1, timeperiod=2592000)  # timeperiod - Last 30 days
    else:
        response = client.get_events_request_v2(timeperiod=2592000, limit=1, is_test=True)

    if response:
        return 'ok'
    else:
        return f'Test failed - {response.get("errorCode")}, {response.get("errors")}'


def v1_get_all_events(client: Client, limit: int, timeperiod: int) -> List:

    events = client.get_events_request_v1(timeperiod, limit)
    alert_results = client.v1_get_alerts_request(timeperiod, limit)
    events.extend(alert_results)

    return events


def v1_get_events_command(client: Client, args: Dict[str, Any]) -> CommandResults:
    limit = arg_to_number(args.get('limit', 20))
    timeperiod = TIME_PERIOD_MAPPING.get(args.get('timeperiod'))

    events = v1_get_all_events(client, limit, timeperiod)

    for event in events:
        event['timestamp'] = timestamp_to_datestring(event['timestamp'] * 1000)

    readable_output = tableToMarkdown('Events List:', events,
                                      removeNull=True,
                                      headers=['_id', 'timestamp', 'type', 'access_method', 'app', 'traffic_type'],
                                      headerTransform=string_to_table_header)

    return CommandResults(outputs_prefix='Netskope.Event',
                          outputs_key_field='_id',
                          outputs=events,
                          readable_output=readable_output,
                          raw_response=events)


def fetch_events_v1(client: Client, first_fetch: str, max_fetch: int) -> list:
    last_run = demisto.getLastRun() or {}
    first_fetch = arg_to_seconds_timestamp(first_fetch)
    last_event_time = last_run.get('last_event_time') or first_fetch

    events = v1_get_all_events(client, limit=max_fetch, timeperiod=last_event_time)
    demisto.setLastRun({
        'last_event_time': events[0]['timestamp'] + 1 if events else last_event_time
    })

    return events


def v2_get_events_command(client: Client, args: Dict[str, Any]) -> CommandResults:
    limit = arg_to_number(args.get('limit', 50))
    timeperiod = TIME_PERIOD_MAPPING.get(args.get('timeperiod'))

    events = client.get_events_request_v2(timeperiod, limit)
    for event in events:
        event['timestamp'] = timestamp_to_datestring(event['timestamp'] * 1000)

    readable_output = tableToMarkdown('Events List:', events,
                                      removeNull=True,
                                      headers=['_id', 'timestamp', 'type', 'access_method', 'app', 'traffic_type'],
                                      headerTransform=string_to_table_header)

    return CommandResults(outputs_prefix='Netskope.Event',
                          outputs_key_field='_id',
                          outputs=events,
                          readable_output=readable_output,
                          raw_response=events)


''' MAIN FUNCTION '''


def main() -> None:
    params = demisto.params()

    url = params.get('url')
    api_version = params.get('api_version')
    token = demisto.params().get('credentials', {}).get('password')
    base_url = urljoin(url, f'/api/{api_version}/')
    verify_certificate = not demisto.params().get('insecure', False)
    proxy = demisto.params().get('proxy', False)
    first_fetch = params.get('first_fetch')
    max_fetch = params.get('max_fetch')
    vendor, product = params.get('vendor'), params.get('product')

    demisto.debug(f'Command being called is {demisto.command()}')
    try:
        client = Client(base_url, token, api_version, verify_certificate, proxy)

        if demisto.command() == 'test-module':
            # This is the call made when pressing the integration Test button.
            result = test_module(client, api_version)
            return_results(result)

        elif demisto.command() == 'netskope-get-events':
            if api_version == 'v1':
                return_results(v1_get_events_command(client, demisto.args()))
            else:
                return_results(v2_get_events_command(client, demisto.args()))
        elif demisto.command() == 'fetch-events':
            if api_version == 'v1':
                send_events_to_xsiam(
                    events=fetch_events_v1(client, first_fetch, max_fetch),
                    vendor=vendor,
                    product=product)

    # Log exceptions and return errors
    except Exception as e:
        demisto.error(traceback.format_exc())  # print the traceback
        return_error(f'Failed to execute {demisto.command()} command.\nError:\n{str(e)}')


''' ENTRY POINT '''


if __name__ in ('__main__', '__builtin__', 'builtins'):
    main()
