#### Playbooks
##### IAM - Activate User In Active Directory
- The user will now be created in Active Directory if it doesn't exist and needs to be activated.
- The playbook was fixed to search for the User Profile in XSOAR once instead of twice.
- Added graceful error handling with proper display in the layout.
- Fixed a bug where the playbook would initialize an already active user, changing its password.

#### Layout Containers
##### IAM - AD User Activation 
- Added a new layout for IAM - AD User Activation incidents.
