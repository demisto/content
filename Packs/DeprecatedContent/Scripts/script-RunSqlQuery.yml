args:
- default: true
  description: SQL query to preform
  name: query
  required: true
- description: 'Limit amount of rows results to be displayed, Default: 100'
  name: limit
comment: Query a relational DB using SQL
commonfields:
  id: RunSqlQuery
  version: -1
dependson:
  must:
  - query
deprecated: true
name: RunSqlQuery
script: "resp = demisto.executeCommand(\"query\", demisto.args())\n\nif isError(resp[0]):\n\
  \    demisto.results(resp)\nelse:\n    data = demisto.get(resp[0], \"Contents\"\
  )\n    if data:\n        data = data if isinstance(data, list) else [data]\n   \
  \     data = [{k: formatCell(row[k]) for k in row} for row in data]\n        demisto.results({\"\
  ContentsFormat\": formats[\"table\"], \"Type\": entryTypes[\"note\"], \"Contents\"\
  : data} )\n    else:\n        demisto.results(\"No results.\")\n"
scripttarget: 0
subtype: python2
system: true
tags:
- sql
timeout: 0s
toversion: 4.1.9
type: python
