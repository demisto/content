args:
- default: true
  description: Scan ID as it appears in Scan List
  name: scanId
  required: true
- description: If specified, these targets will be scanned instead of the default.
    Value can be single host or of comma separated targets.
  name: targets
comment: Launch an existing scan.
commonfields:
  id: NessusLaunchScan
  version: -1
dependson:
  must:
  - Nessus|||scan-launch
deprecated: true
name: NessusLaunchScan
script: "from time import ctime\nres = []\n\ndArgs = demisto.args()\nresCmd = demisto.executeCommand(\"\
  scan-launch\", dArgs)\ntry:\n    for entry in resCmd:\n        if isError(entry):\n\
  \            res = resCmd\n            break\n        else:\n            res.append({'ContentsFormat':\
  \ formats['table'], 'Type': entryTypes['note'], 'Contents': entry['Contents']})\n\
  except Exception as ex:\n    res.append({\"Type\": entryTypes[\"error\"], \"ContentsFormat\"\
  : formats[\"text\"],\n                \"Contents\": \"Error occurred while parsing\
  \ output from command. Exception info:\\n\" + str(ex) + \"\\n\\nInvalid output:\\\
  n\" + str(resCmd)})\ndemisto.results(res)\n"
scripttarget: 0
subtype: python2
system: true
tags:
- nessus
timeout: 0s
toversion: 4.1.9
type: python
