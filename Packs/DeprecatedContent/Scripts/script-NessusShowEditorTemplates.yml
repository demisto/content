args: []
comment: Show templates of the scan editor including the UUIDs needed to create a
  new scan.
commonfields:
  id: NessusShowEditorTemplates
  version: -1
dependson:
  must:
  - nessus-get-scans-editors
deprecated: true
name: NessusShowEditorTemplates
script: "from time import ctime\nres = []\ndArgs = demisto.args()\nresCmd = demisto.executeCommand(\"\
  nessus-get-scans-editors\", dArgs)\ntry:\n    for entry in resCmd:\n        if isError(entry):\n\
  \            res = resCmd\n            break\n        else:\n            info =\
  \ demisto.get(entry, 'Contents.templates')\n            info = [{k: formatCell(row[k])\
  \ for k in row} for row in info]\n            res.append({'ContentsFormat': formats['table'],\
  \ 'Type': entryTypes['note'], 'Contents': info})\nexcept Exception as ex:\n    res.append({\"\
  Type\": entryTypes[\"error\"], \"ContentsFormat\": formats[\"text\"],\n        \
  \        \"Contents\": \"Error occurred while parsing output from command. Exception\
  \ info:\\n\" + str(ex) + \"\\n\\nInvalid output:\\n\" + str(resCmd)})\ndemisto.results(res)\n"
scripttarget: 0
subtype: python2
system: true
tags:
- nessus
timeout: 0s
toversion: 4.1.9
type: python
