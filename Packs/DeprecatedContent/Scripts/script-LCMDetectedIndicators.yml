comment: This script is deprecated. LightCyber Magna is no longer available.
commonfields:
  id: LCMDetectedIndicators
  version: -1
dependson:
  must:
  - lcm-indicators
deprecated: true
name: LCMDetectedIndicators
script: "args = demisto.args()\nresp = demisto.executeCommand(\"lcm-indicators\",\
  \ args)\n\nif isError(resp[0]):\n    demisto.results(resp)\nelse:\n    data = demisto.get(resp[0],\
  \ \"Contents.results\")\n    if data:\n        data = data if isinstance(data, list)\
  \ else [data]\n        data = [{k: formatCell(row[k]) for k in row} for row in data]\n\
  \        demisto.results({\"ContentsFormat\": formats[\"table\"], \"Type\": entryTypes[\"\
  note\"], \"Contents\": data} )\n    else:\n        demisto.results(\"No results.\"\
  )\n"
scripttarget: 0
subtype: python2
system: true
tags:
- lightcyber
timeout: 0s
toversion: 4.1.9
type: python
