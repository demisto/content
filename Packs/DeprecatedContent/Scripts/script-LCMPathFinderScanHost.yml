args:
- default: true
  description: The host's ID
  name: id
  required: true
comment: This script is deprecated. LightCyber Magna is no longer available.
commonfields:
  id: LCMPathFinderScanHost
  version: -1
dependson:
  must:
  - lcm-pathfinder-scan
deprecated: true
name: LCMPathFinderScanHost
script: "args = demisto.args()\nresp = demisto.executeCommand(\"lcm-pathfinder-scan\"\
  , args)\n\nif isError(resp[0]):\n    demisto.results(resp)\nelif str(resp[0]['Contents']).lower()\
  \ == 'true':\n    demisto.results('Host was added to PathFinder scan queue.')\n\
  elif str(resp[0]['Contents']).lower() == 'false':\n    demisto.results( { \"Type\"\
  \ : entryTypes[\"error\"], \"ContentsFormat\" : formats[\"text\"], \"Contents\"\
  \ : \"LightCyber Magna could not add the host to the PathFinder scan queue.\" }\
  \ )\nelse:\n    demisto.results(resp)\n"
scripttarget: 0
subtype: python2
system: true
tags:
- lightcyber
timeout: 0s
toversion: 4.1.9
type: python
