args:
- default: true
  description: (Optional) Name of host to find
  name: name
- description: (Optional) IP of host to find
  name: ip
comment: This script is deprecated. LightCyber Magna is no longer available.
commonfields:
  id: LCMHosts
  version: -1
dependson:
  should:
  - lcm-hosts
  - lcm-hostbyname
  - lcm-hostbyip
deprecated: true
name: LCMHosts
script: "args = demisto.args()\nif demisto.get(args, 'ip') and demisto.get(args, 'name'):\n\
  \    demisto.results( { \"Type\" : entryTypes[\"error\"], \"ContentsFormat\" : formats[\"\
  text\"], \"Contents\" : 'You cannot specify both name and IP. Pick one way to find\
  \ the host you need.' } )\n\nif demisto.get(args, 'ip'):\n    cmd = 'lcm-hostbyip'\n\
  elif demisto.get(args, 'name'):\n    cmd = 'lcm-hostbyname'\nelse:\n    cmd = 'lcm-hosts'\n\
  \nresp = demisto.executeCommand(cmd, args)\nif isError(resp[0]):\n    demisto.results(resp)\n\
  else:\n    data = demisto.get(resp[0], \"Contents.results\")\n    if data:\n   \
  \     data = data if isinstance(data, list) else [data]\n        data = [{k: formatCell(row[k])\
  \ for k in row} for row in data]\n        demisto.results({\"ContentsFormat\": formats[\"\
  table\"], \"Type\": entryTypes[\"note\"], \"Contents\": data} )\n    else:\n   \
  \     demisto.results(\"No results.\")\n"
scripttarget: 0
subtype: python2
system: true
tags:
- lightcyber
timeout: 0s
toversion: 4.1.9
type: python
