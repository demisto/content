
#### Integrations

##### Monday Event Collector

- Fixed an issue where the activity logs API returned 404 errors due to Monday.com API deprecation by pinning the API version header.
- Fixed an issue where fetching activity logs failed with a `KeyError` when board IDs were not initialized in the `last_run` state on subsequent fetches.
- Fixed an issue where the `monday-auth-test` command failed with `list index out of range` when the API returned an empty boards list.
