#### Integrations
##### Office 365 Feed
- Fixed an issue where an unsuccessful fetch would complete successfully.
- In some cases, the Germany endpoints can be unavailable for some users. Now by default, we exclude Germany to prevent the fetch indicators from failing.
