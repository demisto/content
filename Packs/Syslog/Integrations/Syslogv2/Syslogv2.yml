category: Analytics & SIEM
commonfields:
  id: Syslog v2
  version: -1
configuration:
- name: certificate
  display: Certificate (Required for HTTPS)
  additionalinfo: Required for HTTPS, if not using server rerouting
  type: 12
  required: false
- display: Private Key (Required for HTTPS)
  additionalinfo: Required for HTTPS, if not using server rerouting
  name: private_key
  required: false
  type: 14
- diplaypassword: Private Key Password (required when given encrypted private key)
  additionalinfo: When given an encrypted private key, the password required to open the encrypted key file must be supplied.
  name: private_key_password
  required: false
  type: 9
  hiddenusername: true
- display: Incoming Log Format
  defaultvalue: Auto
  hidden: false
  name: log_format
  options:
  - Auto
  - RFC5424
  - RFC3164
  - RFC6587
  required: true
  type: 15
  additionalinfo: The format of the received logs from Syslog server
- display: Message Regex Filter For Incidents Creation.
  name: message_regex
  required: false
  type: 0
  additionalinfo: Will create an incident in Cortex XSOAR for every received log message that matches this regex.
description: A Syslog server provides the ability to automatically open incidents from Syslog clients. This integration provides the ability to filter which logs are to be converted to incidents (or choose to convert all logs).
display: Syslog v2
name: Syslog v2
script:
  commands: []
  isfetch: false
  runonce: false
  script: '-'
  type: python
  subtype: python3
  dockerimage: devdemisto/syslog:1.0.0.24366
  feed: false
  longRunning: true
  longRunningPort: true
  isFetchSamples: true
  ismappable: true
fromversion: 5.5.0
tests:
- No tests (auto formatted)
