
#### Integrations

##### Armis Event Collector

- Fixed an issue where *Activities* events were ingested into the *armis_security_activity_raw* dataset instead of the *armis_security_activities_raw* dataset.
- Fixed an issue where an instance configured to fetch only *Devices* stopped ingesting device events, because the device fetch state was not preserved between fetch cycles.
