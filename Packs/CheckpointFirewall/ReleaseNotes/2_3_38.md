
#### Integrations

##### CheckPoint Firewall v2

- Updated the Docker image to: *demisto/python3:3.12.13.7444307*.
- Added support for **checkpoint-nat-rule-update** command that update an existing nat rule.
- Added support for **checkpoint-network-add** command that create a new network object.
- Added support for **checkpoint-icmp-service-update** command that update an existing icmp service object.
- Added support for **checkpoint-udp-service-update** command that update an existing udp service object.
- Added support for **checkpoint-network-update** command that update an existing network object.
- Added support for **checkpoint-nat-rule-get** command that gets full data for the specified nat rule.
- Added support for **checkpoint-tcp-service-add** command that create a new tcp service object.
- Added support for **checkpoint-icmp-service-add** command that create a new icmp service object.
- Added support for **checkpoint-nat-rule-list** command that retrieve a list of nat rules from the rulebase.
- Added support for **checkpoint-service-delete** command that delete a service object.
- Added support for **checkpoint-network-list** command that retrieve a list of network objects.
- Added support for **checkpoint-service-get** command that gets full data for the specified service object.
- Added support for **checkpoint-service-list** command that retrieve a list of service objects. when identifier is provided, returns a single service object.
- Added support for **checkpoint-tcp-service-update** command that update an existing tcp service object.
- Added support for **checkpoint-nat-rule-delete** command that delete a nat rule.
- Added support for **checkpoint-udp-service-add** command that create a new udp service object.
- Added support for **checkpoint-network-delete** command that delete a network object.
- Added support for **checkpoint-nat-rule-add** command that add a new nat rule.
- Added support for **checkpoint-network-get** command that gets full data for the specified network object.
