#### Integrations

##### AWS SNS SMS Communication

- Enhanced debug logging throughout for better troubleshooting:
  - Added descriptive logs for entitlement parsing with parsed components (guid, incident_id, task_id)
  - Added logging for active entitlements search with counts
  - Added logging when marking entitlements as answered
  - Added logging for available reply codes lookup
  - Added feedback settings logging (success/failure enabled status)
  - Added detailed logging when feedback SMS is sent or skipped (with reason)
  - Added AWS client creation confirmation with service and region
  - Added STS AssumeRole credential expiration logging
  - Added full configuration dump at long-running execution startup
