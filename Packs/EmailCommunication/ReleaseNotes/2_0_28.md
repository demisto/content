
#### Scripts

##### SendEmailReply

- Added option to dynamically add domain to EmailDomainAllowList every time a new email thread to a new domain is sent. Prevents users from having to manually maintain an allowlist.
##### PreprocessEmail
- Added functionality to check an email domain allowlist to prevent spam from creating incidents as pre-processing rules to do so cannot currently be used in conjuction with the PreprocessEmail preprocessing script. 

- %%UPDATE_RN%%
