test:
  override:
    - ls
  post:
    - mkdir bundle
    - cp Playbooks/* bundle/
    - cp Reports/* bundle/
    - cp Scripts/* bundle/
    - cd bundle/ && tar -zcvf ../content.tar .
    - cp content.tar $CIRCLE_ARTIFACTS/content.tar
