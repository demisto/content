test:
  override:
    - ls
  post:
    - mkdir bundle
    - cp Playbooks/* bundle/
    - cp Reports/* bundle/
    - cp $(find Scripts -type f -print) bundle/
    - cd bundle/ && tar -zcvf ../content.tar .
    - cp content.tar $CIRCLE_ARTIFACTS/content.tar
