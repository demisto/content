# yaml-language-server: $schema=../../schema/connection.schema.json
metadata:
  title: Connection
  description: Enter the credentials to securely authorize the connection
view_groups:
- id: integration
  label: Integration
profiles:
- id: api_key.simpleconn
  type: api_key
  view_group: integration
  title: API Key
  description: API key authentication profile for SimpleConn.
  configurations:
  - fields:
    - id: api_key
      title: API Key
      field_type: input
      metadata:
        auth:
          parameter: apikey
      options:
        mask: true
        create_modifiers:
          required: true
          hidden: false
        edit_modifiers:
          required: true
          hidden: false
