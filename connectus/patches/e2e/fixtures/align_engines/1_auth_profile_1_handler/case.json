{
  "description": "align_engines: single auth profile, single handler (topdesk). The engine trio (engine_mode/engine/engine_group) is merged into one fields block; every engine_group token is renamed to engineGroup (connection field id + engine_mode dropdown key; triggers value + effect id + child id); engine + engineGroup get create/edit required: true.",
  "connector": "topdesk",
  "csv": "connectus-migration-pipeline.csv",
  "script": "patches/align_engines.py",
  "args": [
    "--csv", "{csv}",
    "--connectors-dir", "{connectors_dir}",
    "--path", "{path}",
    "{dry_run}"
  ],
  "expect_modified": true
}
