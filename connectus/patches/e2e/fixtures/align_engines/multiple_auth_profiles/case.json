{
  "description": "align_engines: multiple auth profiles / handlers (cisco-umbrella: cloud-security-v2, enforcement, investigate, reporting). Every profile's engine trio is merged into one fields block; every engine_group token is renamed to engineGroup (connection field id + engine_mode dropdown key; triggers value + effect id + child id; serializer field_mappings id, including prefixed variants); engine + engineGroup get create/edit required: true.",
  "connector": "cisco-umbrella",
  "csv": "connectus-migration-pipeline.csv",
  "script": "patches/align_engines.py",
  "args": [
    "--csv", "{csv}",
    "--connectors-dir", "{connectors_dir}",
    "--path", "{path}",
    "{dry_run}"
  ],
  "expect_modified": true
}
