category: IT Services
commonfields:
  id: MicrosoftGraphGroups
  version: -1
configuration:
- defaultvalue: https://graph.microsoft.com
  display: Server URL
  name: url
  required: true
  type: 0
- display: ID (received from the admin consent - see Detailed Instructions (?)
  name: auth_id
  required: true
  type: 4
- display: Token (received from the admin consent - see Detailed Instructions (?)
    section)
  name: tenant_id
  required: true
  type: 4
- display: Key (received from the admin consent - see Detailed Instructions (?)
  name: enc_key
  required: true
  type: 4
- display: Trust any certificate (not secure)
  name: insecure
  required: false
  type: 8
- display: Use system proxy settings
  name: proxy
  required: false
  type: 8
description: Microsoft Graph Groups provide the foundational membership service for
  users to share conversations, files, notes, calendars, plans, and many other assets.
display: Microsoft Graph Groups
name: MicrosoftGraphGroups
script:
  commands:
  - arguments:
    - default: false
      description: 'Sort groups in an organization by the field values. e.g: displayName'
      isArray: false
      name: order_by
      required: false
      secret: false
    - default: false
      description: URL to the next page of results
      isArray: false
      name: next_link
      required: false
      secret: false
    deprecated: false
    description: List groups.
    execution: false
    name: msgraph-groups-list-groups
    outputs:
    - contextPath: MSGraphGroups.Classification
      description: Describes a classification for the group (such as low, medium or
        high business impact).
      type: String
    - contextPath: MSGraphGroups.CreatedDateTime
      description: Timestamp of when the group was created.
      type: String
    - contextPath: MSGraphGroups.DeletedDateTime
      description: Timestamp of when the group was deleted.
      type: String
    - contextPath: MSGraphGroups.Description
      description: An optional description for the group.
      type: String
    - contextPath: MSGraphGroups.GroupTypes
      description: |-
        Specifies the group type and its membership.
        If the collection contains Unified then the group is an Office 365 group; otherwise it's a security group.
        If the collection includes DynamicMembership, the group has dynamic membership; otherwise, membership is static.
      type: String
    - contextPath: MSGraphGroups.ID
      description: The unique identifier for the group.
      type: String
    - contextPath: MSGraphGroup.IsAssignableToRole
      description: Is the group assigned to a specific role.
      type: String
    - contextPath: MSGraphGroup.Mail
      description: The SMTP address for the group, for example, "serviceadmins@contoso.onmicrosoft.com".
      type: String
    - contextPath: MSGraphGroup.MailEnabled
      description: Specifies whether the group is mail-enabled.
      type: Boolean
    - contextPath: MSGraphGroup.MailNickname
      description: The mail alias for the group, unique in the organization.
      type: String
    - contextPath: MSGraphGroup.OnPremisesDomainName
      description: Contains the on-premises domain FQDN, also called dnsDomainName
        synchronized from the on-premises directory.
      type: String
    - contextPath: MSGraphGroup.OnPremisesLastSyncDateTime
      description: 'Indicates the last time at which the group was synced with the
        on-premises directory.The Timestamp type represents date and time information
        using ISO 8601 format and is always in UTC time. For example, midnight UTC
        on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroup.OnPremisesSyncEnabled
      description: |
        true if this group is synced from an on-premises directory; false if this group was originally synced from an on-premises directory but is no longer synced; null if this object has never been synced from an on-premises directory (default).
      type: String
    - contextPath: MSGraphGroup.ProxyAddresses
      description: 'Email addresses for the group that direct to the same group mailbox.
        For example: ["SMTP: bob@contoso.com", "smtp: bob@sales.contoso.com"].'
      type: String
    - contextPath: MSGraphGroup.RenewedDateTime
      description: 'Timestamp of when the group was last renewed. This cannot be modified
        directly and is only updated via the renew service action. The Timestamp type
        represents date and time information using ISO 8601 format and is always in
        UTC time. For example, midnight UTC on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroup.SecurityEnabled
      description: Specifies whether the group is a security group.
      type: Boolean
    - contextPath: MSGraphGroup.Visibility
      description: 'Specifies the visibility of an Office 365 group. Possible values
        are: Private, Public, or Hiddenmembership; blank values are treated as public.'
      type: String
    - contextPath: MSGraphGroup.NextLink
      description: URL to the next page of results.
      type: String
  - arguments:
    - default: false
      description: Group Id.
      isArray: false
      name: id
      required: true
      secret: false
    deprecated: false
    description: Get a group.
    execution: false
    name: msgraph-groups-get-group
    outputs:
    - contextPath: MSGraphGroups.Classification
      description: Describes a classification for the group (such as low, medium or
        high business impact).
      type: String
    - contextPath: MSGraphGroups.CreatedDateTime
      description: Timestamp of when the group was created.
      type: String
    - contextPath: MSGraphGroups.DeletedDateTime
      description: Timestamp of when the group was deleted.
      type: String
    - contextPath: MSGraphGroups.Description
      description: An optional description for the group.
      type: String
    - contextPath: MSGraphGroups.GroupTypes
      description: |-
        Specifies the group type and its membership.
        If the collection contains Unified then the group is an Office 365 group; otherwise it's a security group.
        If the collection includes DynamicMembership, the group has dynamic membership; otherwise, membership is static.
      type: String
    - contextPath: MSGraphGroups.ID
      description: The unique identifier for the group.
      type: String
    - contextPath: MSGraphGroup.IsAssignableToRole
      description: Is the group assigned to a specific role.
      type: String
    - contextPath: MSGraphGroup.Mail
      description: The SMTP address for the group, for example, "serviceadmins@contoso.onmicrosoft.com".
      type: String
    - contextPath: MSGraphGroup.MailEnabled
      description: Specifies whether the group is mail-enabled.
      type: Boolean
    - contextPath: MSGraphGroup.MailNickname
      description: The mail alias for the group, unique in the organization.
      type: String
    - contextPath: MSGraphGroup.OnPremisesDomainName
      description: Contains the on-premises domain FQDN, also called dnsDomainName
        synchronized from the on-premises directory.
      type: String
    - contextPath: MSGraphGroup.OnPremisesLastSyncDateTime
      description: 'Indicates the last time at which the group was synced with the
        on-premises directory.The Timestamp type represents date and time information
        using ISO 8601 format and is always in UTC time. For example, midnight UTC
        on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroup.OnPremisesSyncEnabled
      description: |
        true if this group is synced from an on-premises directory; false if this group was originally synced from an on-premises directory but is no longer synced; null if this object has never been synced from an on-premises directory (default).
      type: String
    - contextPath: MSGraphGroup.ProxyAddresses
      description: 'Email addresses for the group that direct to the same group mailbox.
        For example: ["SMTP: bob@contoso.com", "smtp: bob@sales.contoso.com"].'
      type: String
    - contextPath: MSGraphGroup.RenewedDateTime
      description: 'Timestamp of when the group was last renewed. This cannot be modified
        directly and is only updated via the renew service action. The Timestamp type
        represents date and time information using ISO 8601 format and is always in
        UTC time. For example, midnight UTC on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroup.SecurityEnabled
      description: Specifies whether the group is a security group.
      type: Boolean
    - contextPath: MSGraphGroup.Visibility
      description: 'Specifies the visibility of an Office 365 group. Possible values
        are: Private, Public, or Hiddenmembership; blank values are treated as public.'
      type: String
  - arguments:
    - default: false
      description: Group display name.
      isArray: false
      name: display_name
      required: true
      secret: false
    - auto: PREDEFINED
      default: false
      description: Set to true for mail-enabled groups. false for groups without an
        email.
      isArray: false
      name: mail_enabled
      predefined:
      - 'false'
      - 'true'
      required: false
      secret: false
    - default: false
      description: The mail alias for the group.
      isArray: false
      name: mail_nickname
      required: true
      secret: false
    - auto: PREDEFINED
      default: false
      description: Set to true for security groups. false for non security groups(regular
        groups).
      isArray: false
      name: security_enabled
      predefined:
      - 'false'
      - 'true'
      required: true
      secret: false
    deprecated: false
    description: Create group.
    execution: false
    name: msgraph-groups-create-group
    outputs:
    - contextPath: MSGraphGroups.Classification
      description: Describes a classification for the group (such as low, medium or
        high business impact).
      type: String
    - contextPath: MSGraphGroups.CreatedDateTime
      description: Timestamp of when the group was created.
      type: String
    - contextPath: MSGraphGroups.DeletedDateTime
      description: Timestamp of when the group was deleted.
      type: String
    - contextPath: MSGraphGroups.Description
      description: An optional description for the group.
      type: String
    - contextPath: MSGraphGroups.GroupTypes
      description: |-
        Specifies the group type and its membership.
        If the collection contains Unified then the group is an Office 365 group; otherwise it's a security group.
        If the collection includes DynamicMembership, the group has dynamic membership; otherwise, membership is static.
      type: String
    - contextPath: MSGraphGroups.ID
      description: The unique identifier for the group.
      type: String
    - contextPath: MSGraphGroup.IsAssignableToRole
      description: Is the group assigned to a specific role.
      type: String
    - contextPath: MSGraphGroup.Mail
      description: The SMTP address for the group, for example, "serviceadmins@contoso.onmicrosoft.com".
      type: String
    - contextPath: MSGraphGroup.MailEnabled
      description: Specifies whether the group is mail-enabled.
      type: Boolean
    - contextPath: MSGraphGroup.MailNickname
      description: The mail alias for the group, unique in the organization.
      type: String
    - contextPath: MSGraphGroup.OnPremisesDomainName
      description: Contains the on-premises domain FQDN, also called dnsDomainName
        synchronized from the on-premises directory.
      type: String
    - contextPath: MSGraphGroup.OnPremisesLastSyncDateTime
      description: 'Indicates the last time at which the group was synced with the
        on-premises directory.The Timestamp type represents date and time information
        using ISO 8601 format and is always in UTC time. For example, midnight UTC
        on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroup.OnPremisesSyncEnabled
      description: |
        true if this group is synced from an on-premises directory; false if this group was originally synced from an on-premises directory but is no longer synced; null if this object has never been synced from an on-premises directory (default).
      type: String
    - contextPath: MSGraphGroup.ProxyAddresses
      description: 'Email addresses for the group that direct to the same group mailbox.
        For example: ["SMTP: bob@contoso.com", "smtp: bob@sales.contoso.com"].'
      type: String
    - contextPath: MSGraphGroup.RenewedDateTime
      description: 'Timestamp of when the group was last renewed. This cannot be modified
        directly and is only updated via the renew service action. The Timestamp type
        represents date and time information using ISO 8601 format and is always in
        UTC time. For example, midnight UTC on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroup.SecurityEnabled
      description: Specifies whether the group is a security group.
      type: Boolean
    - contextPath: MSGraphGroup.Visibility
      description: 'Specifies the visibility of an Office 365 group. Possible values
        are: Private, Public, or Hiddenmembership; blank values are treated as public.'
      type: String
  - arguments:
    - default: false
      description: The group ID.
      isArray: false
      name: group_id
      required: true
      secret: false
    deprecated: false
    description: Delete a group.
    execution: false
    name: msgraph-groups-delete-group
    outputs:
    - contextPath: MSGraphGroups.Classification
      description: Describes a classification for the group (such as low, medium or
        high business impact).
      type: String
    - contextPath: MSGraphGroups.CreatedDateTime
      description: Timestamp of when the group was created.
      type: String
    - contextPath: MSGraphGroups.DeletedDateTime
      description: Timestamp of when the group was deleted.
      type: String
    - contextPath: MSGraphGroups.Description
      description: An optional description for the group.
      type: String
    - contextPath: MSGraphGroups.GroupTypes
      description: |-
        Specifies the group type and its membership.
        If the collection contains Unified then the group is an Office 365 group; otherwise it's a security group.
        If the collection includes DynamicMembership, the group has dynamic membership; otherwise, membership is static.
      type: String
    - contextPath: MSGraphGroups.ID
      description: The unique identifier for the group.
      type: String
    - contextPath: MSGraphGroup.IsAssignableToRole
      description: Is the group assigned to a specific role.
      type: String
    - contextPath: MSGraphGroup.Mail
      description: The SMTP address for the group, for example, "serviceadmins@contoso.onmicrosoft.com".
      type: String
    - contextPath: MSGraphGroup.MailEnabled
      description: Specifies whether the group is mail-enabled.
      type: Boolean
    - contextPath: MSGraphGroup.MailNickname
      description: The mail alias for the group, unique in the organization.
      type: String
    - contextPath: MSGraphGroup.OnPremisesDomainName
      description: Contains the on-premises domain FQDN, also called dnsDomainName
        synchronized from the on-premises directory.
      type: String
    - contextPath: MSGraphGroup.OnPremisesLastSyncDateTime
      description: 'Indicates the last time at which the group was synced with the
        on-premises directory.The Timestamp type represents date and time information
        using ISO 8601 format and is always in UTC time. For example, midnight UTC
        on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroup.OnPremisesSyncEnabled
      description: |
        true if this group is synced from an on-premises directory; false if this group was originally synced from an on-premises directory but is no longer synced; null if this object has never been synced from an on-premises directory (default).
      type: String
    - contextPath: MSGraphGroup.ProxyAddresses
      description: 'Email addresses for the group that direct to the same group mailbox.
        For example: ["SMTP: bob@contoso.com", "smtp: bob@sales.contoso.com"].'
      type: String
    - contextPath: MSGraphGroup.RenewedDateTime
      description: 'Timestamp of when the group was last renewed. This cannot be modified
        directly and is only updated via the renew service action. The Timestamp type
        represents date and time information using ISO 8601 format and is always in
        UTC time. For example, midnight UTC on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroup.SecurityEnabled
      description: Specifies whether the group is a security group.
      type: Boolean
    - contextPath: MSGraphGroup.Visibility
      description: 'Specifies the visibility of an Office 365 group. Possible values
        are: Private, Public, or Hiddenmembership; blank values are treated as public.'
      type: String
    - contextPath: MSGraphGroup.Deleted
      description: Specifies whether the group was deleted.
      type: Boolean
  - arguments:
    - default: false
      description: Group properties to return, 'ID' is always returned.
      isArray: false
      name: properties
      required: false
      secret: false
    - default: false
      description: URL to the next page of results
      isArray: false
      name: next_link
      required: false
      secret: false
    deprecated: false
    description: Get newly created, updated, or deleted groups, including group membership
      changes.
    execution: false
    name: msgraph-groups-get-group-delta
    outputs:
    - contextPath: MSGraphGroupsDelta.NextLink
      description: URL to the next page of results or to learn about new changes to
        groups.
      type: String
    - contextPath: MSGraphGroupsDelta.Classification
      description: Describes a classification for the group (such as low, medium or
        high business impact).
      type: String
    - contextPath: MSGraphGroupsDelta.CreatedDateTime
      description: Timestamp of when the group was created.
      type: String
    - contextPath: MSGraphGroupsDelta.DeletedDateTime
      description: Timestamp of when the group was deleted.
      type: String
    - contextPath: MSGraphGroupsDelta.Description
      description: An optional description for the group.
      type: String
    - contextPath: MSGraphGroupsDelta.GroupTypes
      description: |-
        Specifies the group type and its membership.
        If the collection contains Unified then the group is an Office 365 group; otherwise it's a security group.
        If the collection includes DynamicMembership, the group has dynamic membership; otherwise, membership is static.
      type: String
    - contextPath: MSGraphGroupsDelta.ID
      description: The unique identifier for the group.
      type: String
    - contextPath: MSGraphGroupsDelta.IsAssignableToRole
      description: Is the group assigned to a specific role.
      type: String
    - contextPath: MSGraphGroupsDelta.Mail
      description: The SMTP address for the group, for example, "serviceadmins@contoso.onmicrosoft.com".
      type: String
    - contextPath: MSGraphGroupsDelta.MailEnabled
      description: Specifies whether the group is mail-enabled.
      type: Boolean
    - contextPath: MSGraphGroupsDelta.MailNickname
      description: The mail alias for the group, unique in the organization.
      type: String
    - contextPath: MSGraphGroupsDelta.OnPremisesDomainName
      description: Contains the on-premises domain FQDN, also called dnsDomainName
        synchronized from the on-premises directory.
      type: String
    - contextPath: MSGraphGroupsDelta.OnPremisesLastSyncDateTime
      description: 'Indicates the last time at which the group was synced with the
        on-premises directory.The Timestamp type represents date and time information
        using ISO 8601 format and is always in UTC time. For example, midnight UTC
        on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroupsDelta.OnPremisesSyncEnabled
      description: |
        true if this group is synced from an on-premises directory; false if this group was originally synced from an on-premises directory but is no longer synced; null if this object has never been synced from an on-premises directory (default).
      type: String
    - contextPath: MSGraphGroupsDelta.ProxyAddresses
      description: 'Email addresses for the group that direct to the same group mailbox.
        For example: ["SMTP: bob@contoso.com", "smtp: bob@sales.contoso.com"].'
      type: String
    - contextPath: MSGraphGroupsDelta.RenewedDateTime
      description: 'Timestamp of when the group was last renewed. This cannot be modified
        directly and is only updated via the renew service action. The Timestamp type
        represents date and time information using ISO 8601 format and is always in
        UTC time. For example, midnight UTC on Jan 1, 2014 would look like this: ''2014-01-01T00:00:00Z''.'
      type: String
    - contextPath: MSGraphGroupsDelta.SecurityEnabled
      description: Specifies whether the group is a security group.
      type: Boolean
    - contextPath: MSGraphGroupsDelta.Visibility
      description: 'Specifies the visibility of an Office 365 group. Possible values
        are: Private, Public, or Hiddenmembership; blank values are treated as public.'
      type: String
  - arguments:
    - default: false
      description: The group ID.
      isArray: false
      name: group_id
      required: true
      secret: false
    - default: false
      description: URL to the next page of results.
      isArray: false
      name: next_link
      required: false
      secret: false
    deprecated: false
    description: List group members.
    execution: false
    name: msgraph-groups-list-members
  - arguments:
    - default: false
      description: The group ID.
      isArray: false
      name: group_id
      required: true
      secret: false
    - default: false
      description: The user ID.
      isArray: false
      name: user_id
      required: true
      secret: false
    deprecated: false
    description: Add a member to a group.
    execution: false
    name: msgraph-groups-add-member
  - arguments:
    - default: false
      description: The group ID.
      isArray: false
      name: group_id
      required: true
      secret: false
    - default: false
      description: The user ID.
      isArray: false
      name: user_id
      required: true
      secret: false
    deprecated: false
    description: Remove a member from a group.
    execution: false
    name: msgraph-groups-remove-member
  dockerimage: demisto/crypto:1.0.0.303
  isfetch: false
  longRunning: false
  longRunningPort: false
  runonce: false
  script: '-'
  subtype: python3
  type: python
tests:
- MicrosoftGraphMail-Test
