commonfields:
  id: RunPollingCommand
  version: -1
name: RunPollingCommand
comment: "-"
fromversion: 4.0.0
script: |-
  if not isinstance(ids, list):
      ids = [ids]
  demisto.results(demisto.executeCommand(demisto.getArg('pollingCommand'), {demisto.getArg('pollingCommandArgName'): ','.join(argToList(demisto.getArg('ids')))}))
type: python
tags: []
enabled: true
args:
- name: ids
  description: List of IDs to poll
  required: true
  description: List of IDs to poll
- name: pollingCommand
  required: true
  description: Name of the polling command to run
- name: pollingCommandArgName
  description: Name of the argument of the polling command
  required: true
  defaultValue: ids
scripttarget: 0
runonce: false
