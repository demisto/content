.release-sdk:
  rules:
    - if: '$SDK_RELEASE == "true"'


trigger-nightlies:
  stage: trigger-nightlies
  extends:
    - .release-sdk
    - .default-job-settings
  variables:
    BRANCH_NAME: $BRANCH_NAME
    CI_TOKEN: $CI_TOKEN
    SLACK_CHANNEL: $SLACK_CHANNEL
    RELEASE_VERSION: $RELEASE_VERSION
  script:
    - git clone https://github.com/demisto/demisto-sdk.git /demisto-sdk
    - cd /demisto-sdk
    - git pull
    - git checkout -b $_release_branch
    - git push origin $_release_branch
    - echo "test"
#    - python3 ./Tests/sdk_release/trigger_nightlies.py
#    - ./Utils/gitlab_triggers/trigger_content_nightly_build.sh -ct $CI_TOKEN
#    - ./Tests/sdk_release/create_release_branch.sh --release-branch $RELEASE_VERSION


