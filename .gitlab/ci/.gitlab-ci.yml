variables:
  CURRENT_BRANCH_NAME: exclude_managed_content_from_upload

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    ref: exclude_managed_content_from_upload
    project: "${CI_PROJECT_NAMESPACE}/infra"
