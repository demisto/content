variables:
  CURRENT_BRANCH_NAME: pydantic-v2-upgrade # FIXME revert before merge! $INFRA_BRANCH

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    ref: pydantic-v2-upgrade # FIXME revert before merge! $INFRA_BRANCH
    project: "${CI_PROJECT_NAMESPACE}/infra"
