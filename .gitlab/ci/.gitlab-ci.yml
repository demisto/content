variables:
  # CURRENT_BRANCH_NAME: $INFRA_BRANCH
  CURRENT_BRANCH_NAME: fix-platform-core-packs-validation

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    # ref: 
    ref: fix-platform-core-packs-validation
    project: "${CI_PROJECT_NAMESPACE}/infra"
