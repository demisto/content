variables:
  CURRENT_BRANCH_NAME: add_cloning_two_new_repositories

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    ref: add_cloning_two_new_repositories
    project: "${CI_PROJECT_NAMESPACE}/infra"
