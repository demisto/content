variables:
  CURRENT_BRANCH_NAME: wait-in-line  # FIXME don't commit this

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    ref: wait-in-line  # FIXME don't commit this
    project: "${CI_PROJECT_NAMESPACE}/infra"
