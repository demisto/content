variables:
  CURRENT_BRANCH_NAME: add-platform-to-TestUploadFlow

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    ref: add-platform-to-TestUploadFlow
    project: "${CI_PROJECT_NAMESPACE}/infra"
