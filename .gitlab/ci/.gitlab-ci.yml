variables:
  CURRENT_BRANCH_NAME: "content_override_graph_checkout"

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    ref: "content_override_graph_checkout"
    project: "${CI_PROJECT_NAMESPACE}/infra"
