variables:
  # CURRENT_BRANCH_NAME: $INFRA_BRANCH
  CURRENT_BRANCH_NAME: fix-platform-core-packs-validation-test-dependencies

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    # ref: $INFRA_BRANCH
    ref: fix-platform-core-packs-validation-test-dependencies
    project: "${CI_PROJECT_NAMESPACE}/infra"
