variables:
  # CURRENT_BRANCH_NAME: $INFRA_BRANCH
  CURRENT_BRANCH_NAME: CIAC-12739/add-platform-mp-to-upload-flow

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    # ref: $INFRA_BRANCH
    ref: CIAC-12739/add-platform-mp-to-upload-flow
    project: "${CI_PROJECT_NAMESPACE}/infra"
