variables:
  # CURRENT_BRANCH_NAME: $INFRA_BRANCH
  CURRENT_BRANCH_NAME: "stage/platform-content-support"

include:
  - file: "/.gitlab/ci/content-ci/ci/.gitlab-ci.yml"
    # ref: $INFRA_BRANCH
    ref: "stage/platform-content-support"
    project: "${CI_PROJECT_NAMESPACE}/infra"
